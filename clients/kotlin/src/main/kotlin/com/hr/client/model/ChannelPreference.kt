/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package com.hr.client.model

import com.hr.client.model.NotificationChannel

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

/**
 * 
 *
 * @param eventKey The event this applies to, e.g. `leave.approved`.
 * @param channel 
 * @param enabled 
 * @param digestMode DIGEST batches the event into one daily summary. If any channel for an event is set to digest, the whole event is digested — a summary cannot deliver one channel sooner than another, so the stricter reading wins and nothing arrives more often than asked. 
 */
@Serializable

data class ChannelPreference (

    /* The event this applies to, e.g. `leave.approved`. */
    @SerialName(value = "eventKey")
    val eventKey: kotlin.String,

    @Contextual @SerialName(value = "channel")
    val channel: NotificationChannel,

    @SerialName(value = "enabled")
    val enabled: kotlin.Boolean,

    /* DIGEST batches the event into one daily summary. If any channel for an event is set to digest, the whole event is digested — a summary cannot deliver one channel sooner than another, so the stricter reading wins and nothing arrives more often than asked.  */
    @SerialName(value = "digestMode")
    val digestMode: ChannelPreference.DigestMode? = DigestMode.IMMEDIATE

) {

    /**
     * DIGEST batches the event into one daily summary. If any channel for an event is set to digest, the whole event is digested — a summary cannot deliver one channel sooner than another, so the stricter reading wins and nothing arrives more often than asked. 
     *
     * Values: IMMEDIATE,DIGEST
     */
    @Serializable
    enum class DigestMode(val value: kotlin.String) {
        @SerialName(value = "IMMEDIATE") IMMEDIATE("IMMEDIATE"),
        @SerialName(value = "DIGEST") DIGEST("DIGEST");
    }

}

