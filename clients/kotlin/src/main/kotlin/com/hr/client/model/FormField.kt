/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package com.hr.client.model

import com.hr.client.model.FieldOption
import com.hr.client.model.FieldValidation

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

/**
 * 
 *
 * @param key 
 * @param label Already localised. Falls back through region, then English, then the field key — never blank, because a form with an unlabelled field is unusable.
 * @param type 
 * @param required 
 * @param editable False when the caller may see the value but not change it. Distinct from omitting the field — a read-only salary grade still belongs on the profile, it simply is not the employee's to edit. 
 * @param helpText 
 * @param validation 
 * @param options For DROPDOWN, MULTI_SELECT and RADIO.
 * @param referenceTable For REFERENCE fields: which taxonomy supplies the options, e.g. `nationality`. The client already caches these, so the schema names the taxonomy rather than inlining several hundred options. 
 * @param custom True for tenant-defined fields. Clients need not care; support does.
 */
@Serializable

data class FormField (

    @SerialName(value = "key")
    val key: kotlin.String,

    /* Already localised. Falls back through region, then English, then the field key — never blank, because a form with an unlabelled field is unusable. */
    @SerialName(value = "label")
    val label: kotlin.String,

    @SerialName(value = "type")
    val type: FormField.Type,

    @SerialName(value = "required")
    val required: kotlin.Boolean? = false,

    /* False when the caller may see the value but not change it. Distinct from omitting the field — a read-only salary grade still belongs on the profile, it simply is not the employee's to edit.  */
    @SerialName(value = "editable")
    val editable: kotlin.Boolean? = true,

    @SerialName(value = "helpText")
    val helpText: kotlin.String? = null,

    @SerialName(value = "validation")
    val validation: FieldValidation? = null,

    /* For DROPDOWN, MULTI_SELECT and RADIO. */
    @SerialName(value = "options")
    val options: kotlin.collections.List<FieldOption>? = null,

    /* For REFERENCE fields: which taxonomy supplies the options, e.g. `nationality`. The client already caches these, so the schema names the taxonomy rather than inlining several hundred options.  */
    @SerialName(value = "referenceTable")
    val referenceTable: kotlin.String? = null,

    /* True for tenant-defined fields. Clients need not care; support does. */
    @SerialName(value = "custom")
    val custom: kotlin.Boolean? = false

) {

    /**
     * 
     *
     * Values: TEXT,MULTILINE_TEXT,NUMBER,DATE,DROPDOWN,MULTI_SELECT,RADIO,CHECKBOX,ATTACHMENT,EMPLOYEE,REFERENCE,EMAIL,PHONE
     */
    @Serializable
    enum class Type(val value: kotlin.String) {
        @SerialName(value = "TEXT") TEXT("TEXT"),
        @SerialName(value = "MULTILINE_TEXT") MULTILINE_TEXT("MULTILINE_TEXT"),
        @SerialName(value = "NUMBER") NUMBER("NUMBER"),
        @SerialName(value = "DATE") DATE("DATE"),
        @SerialName(value = "DROPDOWN") DROPDOWN("DROPDOWN"),
        @SerialName(value = "MULTI_SELECT") MULTI_SELECT("MULTI_SELECT"),
        @SerialName(value = "RADIO") RADIO("RADIO"),
        @SerialName(value = "CHECKBOX") CHECKBOX("CHECKBOX"),
        @SerialName(value = "ATTACHMENT") ATTACHMENT("ATTACHMENT"),
        @SerialName(value = "EMPLOYEE") EMPLOYEE("EMPLOYEE"),
        @SerialName(value = "REFERENCE") REFERENCE("REFERENCE"),
        @SerialName(value = "EMAIL") EMAIL("EMAIL"),
        @SerialName(value = "PHONE") PHONE("PHONE");
    }

}

