//
// AttendanceAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class AttendanceAPI {

    /**
     Shift roster schedule for authenticated employee
     
     - parameter startDate: (query)  (optional)
     - parameter endDate: (query)  (optional)
     - returns: ShiftRosterResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getMyShiftRoster(startDate: Date? = nil, endDate: Date? = nil) async throws -> ShiftRosterResponse {
        return try await getMyShiftRosterWithRequestBuilder(startDate: startDate, endDate: endDate).execute().body
    }

    /**
     Shift roster schedule for authenticated employee
     - GET /v1/attendance/me/roster
     - Returns the caller's rostered work shifts, rest days, and holidays across a date window. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter startDate: (query)  (optional)
     - parameter endDate: (query)  (optional)
     - returns: RequestBuilder<ShiftRosterResponse> 
     */
    open class func getMyShiftRosterWithRequestBuilder(startDate: Date? = nil, endDate: Date? = nil) -> RequestBuilder<ShiftRosterResponse> {
        let localVariablePath = "/v1/attendance/me/roster"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "startDate": (wrappedValue: startDate?.encodeToJSON(), isExplode: true),
            "endDate": (wrappedValue: endDate?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ShiftRosterResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Today's attendance status and shift rules for authenticated employee
     
     - returns: TodayAttendanceResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getMyTodayAttendance() async throws -> TodayAttendanceResponse {
        return try await getMyTodayAttendanceWithRequestBuilder().execute().body
    }

    /**
     Today's attendance status and shift rules for authenticated employee
     - GET /v1/attendance/me/today
     - Returns the caller's active shift schedule for today, morning check-in grace limits, current punch status, punch timeline, and branch geofence boundary coordinates. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - returns: RequestBuilder<TodayAttendanceResponse> 
     */
    open class func getMyTodayAttendanceWithRequestBuilder() -> RequestBuilder<TodayAttendanceResponse> {
        let localVariablePath = "/v1/attendance/me/today"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TodayAttendanceResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Team shift coverage for a selected work date
     
     - parameter date: (query)  
     - returns: TeamCoverageResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getTeamShiftCoverage(date: Date) async throws -> TeamCoverageResponse {
        return try await getTeamShiftCoverageWithRequestBuilder(date: date).execute().body
    }

    /**
     Team shift coverage for a selected work date
     - GET /v1/attendance/team/coverage
     - Returns scheduled and active team members on a specified date with shift codes, roles, and duty status. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter date: (query)  
     - returns: RequestBuilder<TeamCoverageResponse> 
     */
    open class func getTeamShiftCoverageWithRequestBuilder(date: Date) -> RequestBuilder<TeamCoverageResponse> {
        let localVariablePath = "/v1/attendance/team/coverage"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "date": (wrappedValue: date.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TeamCoverageResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Ingest a clock punch from mobile or biometric source
     
     - parameter attendancePunchRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: AttendancePunchResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func ingestAttendancePunch(attendancePunchRequest: AttendancePunchRequest, idempotencyKey: String? = nil) async throws -> AttendancePunchResponse {
        return try await ingestAttendancePunchWithRequestBuilder(attendancePunchRequest: attendancePunchRequest, idempotencyKey: idempotencyKey).execute().body
    }

    /**
     Ingest a clock punch from mobile or biometric source
     - POST /v1/attendance/punches
     - Records a clock-in, clock-out, or break punch. Accepts single-use Idempotency-Key header for reliable offline outbox retry. Evaluates branch geofence radius and flags mock GPS providers. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter attendancePunchRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: RequestBuilder<AttendancePunchResponse> 
     */
    open class func ingestAttendancePunchWithRequestBuilder(attendancePunchRequest: AttendancePunchRequest, idempotencyKey: String? = nil) -> RequestBuilder<AttendancePunchResponse> {
        let localVariablePath = "/v1/attendance/punches"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: attendancePunchRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "Idempotency-Key": idempotencyKey?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<AttendancePunchResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Request attendance punch regularisation
     
     - parameter attendanceRegulariseRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: AttendanceRegulariseResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func requestAttendanceRegularisation(attendanceRegulariseRequest: AttendanceRegulariseRequest, idempotencyKey: String? = nil) async throws -> AttendanceRegulariseResponse {
        return try await requestAttendanceRegularisationWithRequestBuilder(attendanceRegulariseRequest: attendanceRegulariseRequest, idempotencyKey: idempotencyKey).execute().body
    }

    /**
     Request attendance punch regularisation
     - POST /v1/attendance/me/regularise
     - Submits an attendance adjustment/regularisation request for missed punches to manager approval inbox. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter attendanceRegulariseRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: RequestBuilder<AttendanceRegulariseResponse> 
     */
    open class func requestAttendanceRegularisationWithRequestBuilder(attendanceRegulariseRequest: AttendanceRegulariseRequest, idempotencyKey: String? = nil) -> RequestBuilder<AttendanceRegulariseResponse> {
        let localVariablePath = "/v1/attendance/me/regularise"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: attendanceRegulariseRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "Idempotency-Key": idempotencyKey?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<AttendanceRegulariseResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Submit a shift swap request
     
     - parameter shiftSwapRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: ShiftSwapResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func requestShiftSwap(shiftSwapRequest: ShiftSwapRequest, idempotencyKey: String? = nil) async throws -> ShiftSwapResponse {
        return try await requestShiftSwapWithRequestBuilder(shiftSwapRequest: shiftSwapRequest, idempotencyKey: idempotencyKey).execute().body
    }

    /**
     Submit a shift swap request
     - POST /v1/attendance/roster/swap-request
     - Initiates a shift swap request with another team member. Supports Idempotency-Key for offline outbox sync. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter shiftSwapRequest: (body)  
     - parameter idempotencyKey: (header) Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the offline outbox replays. Reusing a key with a different payload is rejected rather than silently returning the earlier result.  (optional)
     - returns: RequestBuilder<ShiftSwapResponse> 
     */
    open class func requestShiftSwapWithRequestBuilder(shiftSwapRequest: ShiftSwapRequest, idempotencyKey: String? = nil) -> RequestBuilder<ShiftSwapResponse> {
        let localVariablePath = "/v1/attendance/roster/swap-request"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: shiftSwapRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "Idempotency-Key": idempotencyKey?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ShiftSwapResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
