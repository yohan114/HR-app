//
// AuthenticationAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class AuthenticationAPI {

    /**
     Begin enrolment — returns a secret and a QR payload
     
     - returns: MfaEnrolment
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func beginMfaEnrolment() async throws -> MfaEnrolment {
        return try await beginMfaEnrolmentWithRequestBuilder().execute().body
    }

    /**
     Begin enrolment — returns a secret and a QR payload
     - POST /v1/auth/mfa/enrol
     - Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code. The secret is stored immediately but **MFA is not switched on** until `/v1/auth/mfa/enrol/confirm` succeeds.  Two steps rather than one deliberately: a user who mis-scans, or scans into an app they then delete, would otherwise lock themselves out of their own account with no route back except an administrator — which is a support call and a social-engineering path straight past the factor. Calling this again replaces the pending secret, which is what someone does after scanning into the wrong app. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - returns: RequestBuilder<MfaEnrolment> 
     */
    open class func beginMfaEnrolmentWithRequestBuilder() -> RequestBuilder<MfaEnrolment> {
        let localVariablePath = "/v1/auth/mfa/enrol"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<MfaEnrolment>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Exchange a device-sealed refresh token after a biometric assertion
     
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter biometricGrantRequest: (body)  
     - returns: TokenResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func biometricToken(xTenantCode: String, biometricGrantRequest: BiometricGrantRequest) async throws -> TokenResponse {
        return try await biometricTokenWithRequestBuilder(xTenantCode: xTenantCode, biometricGrantRequest: biometricGrantRequest).execute().body
    }

    /**
     Exchange a device-sealed refresh token after a biometric assertion
     - POST /v1/auth/token/biometric
     - The core of the \"no password after enrolment\" experience.  The refresh token is sealed at enrolment in the device's Android Keystore or iOS Secure Enclave under a key that requires user authentication. The OS releases it only after a successful fingerprint or face match, and invalidates the key entirely if the device's enrolled biometrics change.  The server therefore trusts the *presentation* of the sealed token as evidence that the biometric check passed — it never receives or stores biometric data itself.  Two conditions beyond a normal refresh: the token must have been issued to the device presenting it, and that device must have completed biometric enrolment. Together these stop a token exfiltrated from one device being replayed from another. 
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter biometricGrantRequest: (body)  
     - returns: RequestBuilder<TokenResponse> 
     */
    open class func biometricTokenWithRequestBuilder(xTenantCode: String, biometricGrantRequest: BiometricGrantRequest) -> RequestBuilder<TokenResponse> {
        let localVariablePath = "/v1/auth/token/biometric"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: biometricGrantRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Tenant-Code": xTenantCode.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TokenResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }

    /**
     Confirm enrolment with a code from the authenticator
     
     - parameter mfaCodeRequest: (body)  
     - returns: RecoveryCodesResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func confirmMfaEnrolment(mfaCodeRequest: MfaCodeRequest) async throws -> RecoveryCodesResponse {
        return try await confirmMfaEnrolmentWithRequestBuilder(mfaCodeRequest: mfaCodeRequest).execute().body
    }

    /**
     Confirm enrolment with a code from the authenticator
     - POST /v1/auth/mfa/enrol/confirm
     - Proves the app and the server agree before anything depends on it, then switches MFA on.  **Returns the recovery codes, and this is the only time they exist in plaintext.** They are stored as hashes, so neither the server nor a database backup can produce them again. A client that does not show them here has lost them for that user. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter mfaCodeRequest: (body)  
     - returns: RequestBuilder<RecoveryCodesResponse> 
     */
    open class func confirmMfaEnrolmentWithRequestBuilder(mfaCodeRequest: MfaCodeRequest) -> RequestBuilder<RecoveryCodesResponse> {
        let localVariablePath = "/v1/auth/mfa/enrol/confirm"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: mfaCodeRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<RecoveryCodesResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Turn the second factor off
     
     - parameter mfaCodeRequest: (body)  
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func disableMfa(mfaCodeRequest: MfaCodeRequest) async throws {
        return try await disableMfaWithRequestBuilder(mfaCodeRequest: mfaCodeRequest).execute().body
    }

    /**
     Turn the second factor off
     - POST /v1/auth/mfa/disable
     - Requires a current code — possession, not just a live session. Without that, anyone who borrowed an unlocked laptop could switch the factor off from a settings screen, which makes having it decorative. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter mfaCodeRequest: (body)  
     - returns: RequestBuilder<Void> 
     */
    open class func disableMfaWithRequestBuilder(mfaCodeRequest: MfaCodeRequest) -> RequestBuilder<Void> {
        let localVariablePath = "/v1/auth/mfa/disable"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: mfaCodeRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = HRClientAPI.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Public JSON Web Key Set
     
     - returns: JwkSet
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getJwks() async throws -> JwkSet {
        return try await getJwksWithRequestBuilder().execute().body
    }

    /**
     Public JSON Web Key Set
     - GET /v1/auth/.well-known/jwks.json
     - The public half of the key used to sign access tokens, so that other services, an API gateway or a third-party integration can verify our tokens without holding any credential of their own.  The application itself does not call this endpoint — it verifies with the in-process public key rather than issuing an HTTP request to itself.  During a key rotation window this returns both the outgoing and incoming keys, so tokens signed with either continue to verify. 
     - returns: RequestBuilder<JwkSet> 
     */
    open class func getJwksWithRequestBuilder() -> RequestBuilder<JwkSet> {
        let localVariablePath = "/v1/auth/.well-known/jwks.json"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<JwkSet>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }

    /**
     Whether a second factor is enrolled
     
     - returns: MfaStatus
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getMfaStatus() async throws -> MfaStatus {
        return try await getMfaStatusWithRequestBuilder().execute().body
    }

    /**
     Whether a second factor is enrolled
     - GET /v1/auth/mfa
     - Drives the security settings screen: whether to offer enrolment, resume a half-finished one, or warn that recovery codes are running low. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - returns: RequestBuilder<MfaStatus> 
     */
    open class func getMfaStatusWithRequestBuilder() -> RequestBuilder<MfaStatus> {
        let localVariablePath = "/v1/auth/mfa"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<MfaStatus>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Sign in with a password
     
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter passwordGrantRequest: (body)  
     - returns: TokenResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func issueToken(xTenantCode: String, passwordGrantRequest: PasswordGrantRequest) async throws -> TokenResponse {
        return try await issueTokenWithRequestBuilder(xTenantCode: xTenantCode, passwordGrantRequest: passwordGrantRequest).execute().body
    }

    /**
     Sign in with a password
     - POST /v1/auth/token
     - Exchanges credentials for an access and refresh token pair, and registers the calling device in the same round trip so the client can offer biometric enrolment immediately.  Failures are deliberately uniform: a wrong password and an unknown username both return `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated failures lock the account per the tenant's password policy. 
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter passwordGrantRequest: (body)  
     - returns: RequestBuilder<TokenResponse> 
     */
    open class func issueTokenWithRequestBuilder(xTenantCode: String, passwordGrantRequest: PasswordGrantRequest) -> RequestBuilder<TokenResponse> {
        let localVariablePath = "/v1/auth/token"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: passwordGrantRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Tenant-Code": xTenantCode.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TokenResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }

    /**
     List the current user's registered devices
     
     - returns: [Device]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listDevices() async throws -> [Device] {
        return try await listDevicesWithRequestBuilder().execute().body
    }

    /**
     List the current user's registered devices
     - GET /v1/auth/devices
     - Powers the \"where am I signed in?\" screen. The device making the request is flagged with `current: true` so the client can avoid offering the user a way to revoke themselves without warning. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - returns: RequestBuilder<[Device]> 
     */
    open class func listDevicesWithRequestBuilder() -> RequestBuilder<[Device]> {
        let localVariablePath = "/v1/auth/devices"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[Device]>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Sign out
     
     - parameter refreshTokenRequest: (body)  (optional)
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func logout(refreshTokenRequest: RefreshTokenRequest? = nil) async throws {
        return try await logoutWithRequestBuilder(refreshTokenRequest: refreshTokenRequest).execute().body
    }

    /**
     Sign out
     - POST /v1/auth/logout
     - Revokes the token family the presented refresh token belongs to. Other devices stay signed in — signing a user out everywhere is a separate, deliberate action.  The body is optional. Without it the caller's entire set of sessions is revoked, which is the correct fallback when a client has lost track of its refresh token.  Never fails: sign-out is idempotent and a client must always be able to end its session. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter refreshTokenRequest: (body)  (optional)
     - returns: RequestBuilder<Void> 
     */
    open class func logoutWithRequestBuilder(refreshTokenRequest: RefreshTokenRequest? = nil) -> RequestBuilder<Void> {
        let localVariablePath = "/v1/auth/logout"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: refreshTokenRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = HRClientAPI.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Exchange a refresh token for a new access token
     
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter refreshTokenRequest: (body)  
     - returns: TokenResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func refreshToken(xTenantCode: String, refreshTokenRequest: RefreshTokenRequest) async throws -> TokenResponse {
        return try await refreshTokenWithRequestBuilder(xTenantCode: xTenantCode, refreshTokenRequest: refreshTokenRequest).execute().body
    }

    /**
     Exchange a refresh token for a new access token
     - POST /v1/auth/token/refresh
     - Refresh tokens are single-use and rotate on every exchange.  If a token that has already been used is presented again, the most likely explanation is theft, so the entire token family is revoked and every device sharing that login session must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.  Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security, so the tenant must be known before the token can be found. 
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter refreshTokenRequest: (body)  
     - returns: RequestBuilder<TokenResponse> 
     */
    open class func refreshTokenWithRequestBuilder(xTenantCode: String, refreshTokenRequest: RefreshTokenRequest) -> RequestBuilder<TokenResponse> {
        let localVariablePath = "/v1/auth/token/refresh"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: refreshTokenRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Tenant-Code": xTenantCode.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TokenResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }

    /**
     Issue a fresh set of recovery codes
     
     - parameter mfaCodeRequest: (body)  
     - returns: RecoveryCodesResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func regenerateRecoveryCodes(mfaCodeRequest: MfaCodeRequest) async throws -> RecoveryCodesResponse {
        return try await regenerateRecoveryCodesWithRequestBuilder(mfaCodeRequest: mfaCodeRequest).execute().body
    }

    /**
     Issue a fresh set of recovery codes
     - POST /v1/auth/mfa/recovery-codes
     - Invalidates every existing code. Requires a current code, for the same reason as disabling. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter mfaCodeRequest: (body)  
     - returns: RequestBuilder<RecoveryCodesResponse> 
     */
    open class func regenerateRecoveryCodesWithRequestBuilder(mfaCodeRequest: MfaCodeRequest) -> RequestBuilder<RecoveryCodesResponse> {
        let localVariablePath = "/v1/auth/mfa/recovery-codes"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: mfaCodeRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<RecoveryCodesResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Register the current device
     
     - parameter registerDeviceRequest: (body)  
     - returns: Device
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func registerDevice(registerDeviceRequest: RegisterDeviceRequest) async throws -> Device {
        return try await registerDeviceWithRequestBuilder(registerDeviceRequest: registerDeviceRequest).execute().body
    }

    /**
     Register the current device
     - POST /v1/auth/devices
     - Registers or updates a device for the authenticated user, including its push token.  Called on sign-in and again whenever the push token rotates. Idempotent on `(user, deviceId)`: re-registering an existing device updates it rather than creating a duplicate. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter registerDeviceRequest: (body)  
     - returns: RequestBuilder<Device> 
     */
    open class func registerDeviceWithRequestBuilder(registerDeviceRequest: RegisterDeviceRequest) -> RequestBuilder<Device> {
        let localVariablePath = "/v1/auth/devices"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: registerDeviceRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Device>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Resolve an organisation from a work email or org code
     
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter resolveTenantRequest: (body)  
     - returns: ResolveTenantResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func resolveTenant(xTenantCode: String, resolveTenantRequest: ResolveTenantRequest) async throws -> ResolveTenantResponse {
        return try await resolveTenantWithRequestBuilder(xTenantCode: xTenantCode, resolveTenantRequest: resolveTenantRequest).execute().body
    }

    /**
     Resolve an organisation from a work email or org code
     - POST /v1/auth/resolve-tenant
     - The first step of sign-in. Deliberately does **not** reveal whether a user account exists — it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.  This replaces asking the user to type a \"service URL\", which is a common source of support load in comparable products. 
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter resolveTenantRequest: (body)  
     - returns: RequestBuilder<ResolveTenantResponse> 
     */
    open class func resolveTenantWithRequestBuilder(xTenantCode: String, resolveTenantRequest: ResolveTenantRequest) -> RequestBuilder<ResolveTenantResponse> {
        let localVariablePath = "/v1/auth/resolve-tenant"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: resolveTenantRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Tenant-Code": xTenantCode.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ResolveTenantResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }

    /**
     Revoke a device
     
     - parameter id: (path) The device record&#39;s UUID, as returned by &#x60;GET /v1/auth/devices&#x60;. 
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func revokeDevice(id: UUID) async throws {
        return try await revokeDeviceWithRequestBuilder(id: id).execute().body
    }

    /**
     Revoke a device
     - DELETE /v1/auth/devices/{id}
     - Revokes the device and every refresh token bound to it. Takes effect immediately for refresh and biometric grants; an access token already issued remains valid until it expires, at most fifteen minutes.  Note the parameter is the device record's `id` (a UUID), not its `deviceId` (the client-generated string). The two are distinct and naming the path variable `deviceId` was actively misleading. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter id: (path) The device record&#39;s UUID, as returned by &#x60;GET /v1/auth/devices&#x60;. 
     - returns: RequestBuilder<Void> 
     */
    open class func revokeDeviceWithRequestBuilder(id: UUID) -> RequestBuilder<Void> {
        var localVariablePath = "/v1/auth/devices/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = HRClientAPI.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Complete a sign-in with a second factor
     
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter mfaVerifyRequest: (body)  
     - returns: TokenResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func verifyMfa(xTenantCode: String, mfaVerifyRequest: MfaVerifyRequest) async throws -> TokenResponse {
        return try await verifyMfaWithRequestBuilder(xTenantCode: xTenantCode, mfaVerifyRequest: mfaVerifyRequest).execute().body
    }

    /**
     Complete a sign-in with a second factor
     - POST /v1/auth/mfa/verify
     - The second half of a password sign-in. Called after `/v1/auth/token` answers `401 MFA_REQUIRED` with an `mfaToken` in `details`.  Accepts either a TOTP code or a recovery code — the user cannot always tell you which they are holding, and making them choose adds a decision at the worst possible moment. A recovery code is consumed on use.  The challenge token goes in the **body**, not the `Authorization` header: it is not a bearer token for this API, and putting it there invites proxies and client libraries to treat it as one. It carries no roles and no employee id, so even if something did accept it as a session it would grant nothing.  Every failure — wrong code, spent recovery code, account without a second factor — returns the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge which case they are in. 
     - parameter xTenantCode: (header) Organisation code. Required on unauthenticated endpoints, where no token exists yet. 
     - parameter mfaVerifyRequest: (body)  
     - returns: RequestBuilder<TokenResponse> 
     */
    open class func verifyMfaWithRequestBuilder(xTenantCode: String, mfaVerifyRequest: MfaVerifyRequest) -> RequestBuilder<TokenResponse> {
        let localVariablePath = "/v1/auth/mfa/verify"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: mfaVerifyRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Tenant-Code": xTenantCode.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<TokenResponse>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }
}
