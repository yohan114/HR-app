//
// DirectoryAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class DirectoryAPI {

    /**
     Direct reports of an employee
     
     - parameter id: (path)  
     - returns: [DirectoryEntry]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listDirectReports(id: UUID) async throws -> [DirectoryEntry] {
        return try await listDirectReportsWithRequestBuilder(id: id).execute().body
    }

    /**
     Direct reports of an employee
     - GET /v1/directory/employees/{id}/reports
     - One level only. The org chart expands a branch at a time rather than fetching an entire subtree, which keeps the payload small for a deep organisation and matches how the screen is actually used. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter id: (path)  
     - returns: RequestBuilder<[DirectoryEntry]> 
     */
    open class func listDirectReportsWithRequestBuilder(id: UUID) -> RequestBuilder<[DirectoryEntry]> {
        var localVariablePath = "/v1/directory/employees/{id}/reports"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[DirectoryEntry]>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Search the employee directory
     
     - parameter query: (query) Free text. Tolerates whatever a person types — unbalanced quotes and stray operators do not produce an error. (optional)
     - parameter departmentId: (query)  (optional)
     - parameter locationId: (query)  (optional)
     - parameter cursor: (query) Opaque cursor from a previous response. (optional)
     - parameter limit: (query)  (optional, default to 50)
     - returns: DirectoryPage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func searchDirectory(query: String? = nil, departmentId: UUID? = nil, locationId: UUID? = nil, cursor: String? = nil, limit: Int? = nil) async throws -> DirectoryPage {
        return try await searchDirectoryWithRequestBuilder(query: query, departmentId: departmentId, locationId: locationId, cursor: cursor, limit: limit).execute().body
    }

    /**
     Search the employee directory
     - GET /v1/directory/search
     - Available to every authenticated employee — finding a colleague's extension is not a privileged operation, and gating it would mean most of the workforce could not use the feature they open most often.  What makes that safe is what the response omits. Salary, bank details, identity documents, date of birth and home address are never selected by the query, so there is nothing to leak. Anything more sensitive goes through the profile endpoint, which applies , a data-scope check and field-level permissions.  Results are weighted so a name match outranks an employee-code match, which outranks an email match. Exited and not-yet-joined employees are excluded. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter query: (query) Free text. Tolerates whatever a person types — unbalanced quotes and stray operators do not produce an error. (optional)
     - parameter departmentId: (query)  (optional)
     - parameter locationId: (query)  (optional)
     - parameter cursor: (query) Opaque cursor from a previous response. (optional)
     - parameter limit: (query)  (optional, default to 50)
     - returns: RequestBuilder<DirectoryPage> 
     */
    open class func searchDirectoryWithRequestBuilder(query: String? = nil, departmentId: UUID? = nil, locationId: UUID? = nil, cursor: String? = nil, limit: Int? = nil) -> RequestBuilder<DirectoryPage> {
        let localVariablePath = "/v1/directory/search"
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "query": (wrappedValue: query?.encodeToJSON(), isExplode: true),
            "departmentId": (wrappedValue: departmentId?.encodeToJSON(), isExplode: true),
            "locationId": (wrappedValue: locationId?.encodeToJSON(), isExplode: true),
            "cursor": (wrappedValue: cursor?.encodeToJSON(), isExplode: true),
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<DirectoryPage>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
