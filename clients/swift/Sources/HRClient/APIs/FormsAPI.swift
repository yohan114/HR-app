//
// FormsAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class FormsAPI {

    /**
     * enum for parameter entityType
     */
    public enum EntityType_getFormSchema: String, CaseIterable {
        case employee = "employee"
        case company = "company"
        case location = "location"
        case department = "department"
        case designation = "designation"
    }

    /**
     The form schema for an entity type
     
     - parameter entityType: (path)  
     - parameter acceptLanguage: (header) First language tag wins. Labels fall back through region, then English, then the field key. (optional)
     - returns: FormSchema
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getFormSchema(entityType: EntityType_getFormSchema, acceptLanguage: String? = nil) async throws -> FormSchema {
        return try await getFormSchemaWithRequestBuilder(entityType: entityType, acceptLanguage: acceptLanguage).execute().body
    }

    /**
     The form schema for an entity type
     - GET /v1/forms/{entityType}
     - Describes a form well enough for a client to render it without knowing what the fields are — so a tenant can add a field in the admin console and have it appear on Android and iOS on the next sync. No code change, no app release, no store review.  The schema covers the **whole** form: built-in and tenant-defined fields together, in one ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at the bottom — cannot interleave a tenant's field with the built-in one it relates to, and puts a customer's mandatory field below the save button.  **Permissions are already applied.** Fields the caller may not see are absent, not flagged; fields they may see but not change arrive with `editable: false`. The client does not filter, and cannot leak what it never received.  Clients cache by `version` and re-render only when it changes. The version is a hash of the definitions, not a timestamp, so two servers behind a load balancer agree. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter entityType: (path)  
     - parameter acceptLanguage: (header) First language tag wins. Labels fall back through region, then English, then the field key. (optional)
     - returns: RequestBuilder<FormSchema> 
     */
    open class func getFormSchemaWithRequestBuilder(entityType: EntityType_getFormSchema, acceptLanguage: String? = nil) -> RequestBuilder<FormSchema> {
        var localVariablePath = "/v1/forms/{entityType}"
        let entityTypePreEscape = "\(entityType.rawValue)"
        let entityTypePostEscape = entityTypePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{entityType}", with: entityTypePostEscape, options: .literal, range: nil)
        let localVariableURLString = HRClientAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Accept-Language": acceptLanguage?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<FormSchema>.Type = HRClientAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
