//
// ApplicationItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ApplicationItem: Codable, JSONEncodable, Hashable {

    public enum Stage: String, Codable, CaseIterable {
        case applied = "APPLIED"
        case screening = "SCREENING"
        case interviewRound1 = "INTERVIEW_ROUND_1"
        case interviewRound2 = "INTERVIEW_ROUND_2"
        case technicalAssessment = "TECHNICAL_ASSESSMENT"
        case finalInterview = "FINAL_INTERVIEW"
        case offerExtended = "OFFER_EXTENDED"
        case offerAccepted = "OFFER_ACCEPTED"
        case offerDeclined = "OFFER_DECLINED"
        case rejected = "REJECTED"
        case withdrawn = "WITHDRAWN"
    }
    public enum Status: String, Codable, CaseIterable {
        case active = "ACTIVE"
        case archived = "ARCHIVED"
        case hired = "HIRED"
        case rejected = "REJECTED"
    }
    public var id: UUID
    public var applicationNumber: String
    public var vacancyId: UUID
    public var vacancyTitle: String
    public var candidateId: UUID
    public var candidateName: String
    public var candidateEmail: String
    public var stage: Stage
    public var status: Status
    public var rating: Double?
    public var source: String
    public var appliedDate: Date
    public var rejectionReason: String?
    public var createdAt: Date

    public init(id: UUID, applicationNumber: String, vacancyId: UUID, vacancyTitle: String, candidateId: UUID, candidateName: String, candidateEmail: String, stage: Stage, status: Status, rating: Double? = nil, source: String, appliedDate: Date, rejectionReason: String? = nil, createdAt: Date) {
        self.id = id
        self.applicationNumber = applicationNumber
        self.vacancyId = vacancyId
        self.vacancyTitle = vacancyTitle
        self.candidateId = candidateId
        self.candidateName = candidateName
        self.candidateEmail = candidateEmail
        self.stage = stage
        self.status = status
        self.rating = rating
        self.source = source
        self.appliedDate = appliedDate
        self.rejectionReason = rejectionReason
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case applicationNumber
        case vacancyId
        case vacancyTitle
        case candidateId
        case candidateName
        case candidateEmail
        case stage
        case status
        case rating
        case source
        case appliedDate
        case rejectionReason
        case createdAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(applicationNumber, forKey: .applicationNumber)
        try container.encode(vacancyId, forKey: .vacancyId)
        try container.encode(vacancyTitle, forKey: .vacancyTitle)
        try container.encode(candidateId, forKey: .candidateId)
        try container.encode(candidateName, forKey: .candidateName)
        try container.encode(candidateEmail, forKey: .candidateEmail)
        try container.encode(stage, forKey: .stage)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(rating, forKey: .rating)
        try container.encode(source, forKey: .source)
        try container.encode(appliedDate, forKey: .appliedDate)
        try container.encodeIfPresent(rejectionReason, forKey: .rejectionReason)
        try container.encode(createdAt, forKey: .createdAt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ApplicationItem: Identifiable {}
