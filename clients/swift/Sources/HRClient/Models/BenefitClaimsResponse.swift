//
// BenefitClaimsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct BenefitClaimsResponse: Codable, JSONEncodable, Hashable {

    public var totalClaimedAmount: Double
    public var totalApprovedAmount: Double
    public var totalPaidAmount: Double
    public var pendingCount: Int
    public var claims: [BenefitClaimItem]

    public init(totalClaimedAmount: Double, totalApprovedAmount: Double, totalPaidAmount: Double, pendingCount: Int, claims: [BenefitClaimItem]) {
        self.totalClaimedAmount = totalClaimedAmount
        self.totalApprovedAmount = totalApprovedAmount
        self.totalPaidAmount = totalPaidAmount
        self.pendingCount = pendingCount
        self.claims = claims
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalClaimedAmount
        case totalApprovedAmount
        case totalPaidAmount
        case pendingCount
        case claims
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalClaimedAmount, forKey: .totalClaimedAmount)
        try container.encode(totalApprovedAmount, forKey: .totalApprovedAmount)
        try container.encode(totalPaidAmount, forKey: .totalPaidAmount)
        try container.encode(pendingCount, forKey: .pendingCount)
        try container.encode(claims, forKey: .claims)
    }
}

