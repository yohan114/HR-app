//
// ChannelPreference.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ChannelPreference: Codable, JSONEncodable, Hashable {

    public enum DigestMode: String, Codable, CaseIterable {
        case immediate = "IMMEDIATE"
        case digest = "DIGEST"
    }
    public static let eventKeyRule = StringRule(minLength: nil, maxLength: 128, pattern: nil)
    /** The event this applies to, e.g. `leave.approved`. */
    public var eventKey: String
    public var channel: NotificationChannel
    public var enabled: Bool
    /** DIGEST batches the event into one daily summary. If any channel for an event is set to digest, the whole event is digested — a summary cannot deliver one channel sooner than another, so the stricter reading wins and nothing arrives more often than asked.  */
    public var digestMode: DigestMode? = .immediate

    public init(eventKey: String, channel: NotificationChannel, enabled: Bool, digestMode: DigestMode? = .immediate) {
        self.eventKey = eventKey
        self.channel = channel
        self.enabled = enabled
        self.digestMode = digestMode
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case eventKey
        case channel
        case enabled
        case digestMode
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(eventKey, forKey: .eventKey)
        try container.encode(channel, forKey: .channel)
        try container.encode(enabled, forKey: .enabled)
        try container.encodeIfPresent(digestMode, forKey: .digestMode)
    }
}

