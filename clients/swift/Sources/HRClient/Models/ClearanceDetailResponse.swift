//
// ClearanceDetailResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ClearanceDetailResponse: Codable, JSONEncodable, Hashable {

    public var exitNoticeId: UUID
    public var employeeId: UUID
    public var employeeName: String
    public var totalTasks: Int
    public var clearedTasks: Int
    public var pendingTasks: Int
    public var totalRecoverableAmount: Double
    public var tasks: [ClearanceTaskItem]

    public init(exitNoticeId: UUID, employeeId: UUID, employeeName: String, totalTasks: Int, clearedTasks: Int, pendingTasks: Int, totalRecoverableAmount: Double, tasks: [ClearanceTaskItem]) {
        self.exitNoticeId = exitNoticeId
        self.employeeId = employeeId
        self.employeeName = employeeName
        self.totalTasks = totalTasks
        self.clearedTasks = clearedTasks
        self.pendingTasks = pendingTasks
        self.totalRecoverableAmount = totalRecoverableAmount
        self.tasks = tasks
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case exitNoticeId
        case employeeId
        case employeeName
        case totalTasks
        case clearedTasks
        case pendingTasks
        case totalRecoverableAmount
        case tasks
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(exitNoticeId, forKey: .exitNoticeId)
        try container.encode(employeeId, forKey: .employeeId)
        try container.encode(employeeName, forKey: .employeeName)
        try container.encode(totalTasks, forKey: .totalTasks)
        try container.encode(clearedTasks, forKey: .clearedTasks)
        try container.encode(pendingTasks, forKey: .pendingTasks)
        try container.encode(totalRecoverableAmount, forKey: .totalRecoverableAmount)
        try container.encode(tasks, forKey: .tasks)
    }
}

