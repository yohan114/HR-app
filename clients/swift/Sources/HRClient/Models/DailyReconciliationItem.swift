//
// DailyReconciliationItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DailyReconciliationItem: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case matched = "MATCHED"
        case underLogged = "UNDER_LOGGED"
        case overLogged = "OVER_LOGGED"
        case noAttendance = "NO_ATTENDANCE"
    }
    public var date: Date
    public var loggedHours: Double
    public var attendanceHours: Double
    public var varianceHours: Double
    public var status: Status

    public init(date: Date, loggedHours: Double, attendanceHours: Double, varianceHours: Double, status: Status) {
        self.date = date
        self.loggedHours = loggedHours
        self.attendanceHours = attendanceHours
        self.varianceHours = varianceHours
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case date
        case loggedHours
        case attendanceHours
        case varianceHours
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(date, forKey: .date)
        try container.encode(loggedHours, forKey: .loggedHours)
        try container.encode(attendanceHours, forKey: .attendanceHours)
        try container.encode(varianceHours, forKey: .varianceHours)
        try container.encode(status, forKey: .status)
    }
}

