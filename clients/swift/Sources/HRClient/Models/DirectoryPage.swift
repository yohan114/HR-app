//
// DirectoryPage.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DirectoryPage: Codable, JSONEncodable, Hashable {

    public var items: [DirectoryEntry]
    /** Pass as `cursor` on the next call. Absent when there are no more results. */
    public var nextCursor: String?
    public var hasMore: Bool

    public init(items: [DirectoryEntry], nextCursor: String? = nil, hasMore: Bool) {
        self.items = items
        self.nextCursor = nextCursor
        self.hasMore = hasMore
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case items
        case nextCursor
        case hasMore
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(items, forKey: .items)
        try container.encodeIfPresent(nextCursor, forKey: .nextCursor)
        try container.encode(hasMore, forKey: .hasMore)
    }
}

