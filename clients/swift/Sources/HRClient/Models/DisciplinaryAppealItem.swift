//
// DisciplinaryAppealItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DisciplinaryAppealItem: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case submitted = "SUBMITTED"
        case upheld = "UPHELD"
        case reduced = "REDUCED"
        case overturned = "OVERTURNED"
    }
    public var id: UUID
    public var incidentId: UUID
    public var correctiveActionId: UUID
    public var reason: String
    public var appealedAt: Date
    public var outcome: String?
    public var reviewedAt: Date?
    public var status: Status

    public init(id: UUID, incidentId: UUID, correctiveActionId: UUID, reason: String, appealedAt: Date, outcome: String? = nil, reviewedAt: Date? = nil, status: Status) {
        self.id = id
        self.incidentId = incidentId
        self.correctiveActionId = correctiveActionId
        self.reason = reason
        self.appealedAt = appealedAt
        self.outcome = outcome
        self.reviewedAt = reviewedAt
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case incidentId
        case correctiveActionId
        case reason
        case appealedAt
        case outcome
        case reviewedAt
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(incidentId, forKey: .incidentId)
        try container.encode(correctiveActionId, forKey: .correctiveActionId)
        try container.encode(reason, forKey: .reason)
        try container.encode(appealedAt, forKey: .appealedAt)
        try container.encodeIfPresent(outcome, forKey: .outcome)
        try container.encodeIfPresent(reviewedAt, forKey: .reviewedAt)
        try container.encode(status, forKey: .status)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension DisciplinaryAppealItem: Identifiable {}
