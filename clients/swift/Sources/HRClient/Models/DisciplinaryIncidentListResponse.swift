//
// DisciplinaryIncidentListResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DisciplinaryIncidentListResponse: Codable, JSONEncodable, Hashable {

    public var totalCount: Int
    public var openCount: Int
    public var incidents: [DisciplinaryIncidentItem]

    public init(totalCount: Int, openCount: Int, incidents: [DisciplinaryIncidentItem]) {
        self.totalCount = totalCount
        self.openCount = openCount
        self.incidents = incidents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalCount
        case openCount
        case incidents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalCount, forKey: .totalCount)
        try container.encode(openCount, forKey: .openCount)
        try container.encode(incidents, forKey: .incidents)
    }
}

