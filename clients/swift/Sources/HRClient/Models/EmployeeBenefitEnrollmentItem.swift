//
// EmployeeBenefitEnrollmentItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct EmployeeBenefitEnrollmentItem: Codable, JSONEncodable, Hashable {

    public enum CoverageTier: String, Codable, CaseIterable {
        case individual = "INDIVIDUAL"
        case employeeAndSpouse = "EMPLOYEE_AND_SPOUSE"
        case family = "FAMILY"
    }
    public enum Status: String, Codable, CaseIterable {
        case active = "ACTIVE"
        case suspended = "SUSPENDED"
        case expired = "EXPIRED"
        case terminated = "TERMINATED"
    }
    public var id: UUID
    public var policyId: UUID
    public var policyCode: String
    public var policyName: String
    public var categoryName: String
    public var coverageTier: CoverageTier
    public var policyNumber: String
    public var enrollmentYear: Int
    public var startDate: Date
    public var endDate: Date
    public var annualEntitlement: Double
    public var usedAmount: Double
    public var pendingAmount: Double
    public var remainingBalance: Double
    public var currency: String
    public var status: Status
    public var dependents: [BenefitDependentItem]

    public init(id: UUID, policyId: UUID, policyCode: String, policyName: String, categoryName: String, coverageTier: CoverageTier, policyNumber: String, enrollmentYear: Int, startDate: Date, endDate: Date, annualEntitlement: Double, usedAmount: Double, pendingAmount: Double, remainingBalance: Double, currency: String, status: Status, dependents: [BenefitDependentItem]) {
        self.id = id
        self.policyId = policyId
        self.policyCode = policyCode
        self.policyName = policyName
        self.categoryName = categoryName
        self.coverageTier = coverageTier
        self.policyNumber = policyNumber
        self.enrollmentYear = enrollmentYear
        self.startDate = startDate
        self.endDate = endDate
        self.annualEntitlement = annualEntitlement
        self.usedAmount = usedAmount
        self.pendingAmount = pendingAmount
        self.remainingBalance = remainingBalance
        self.currency = currency
        self.status = status
        self.dependents = dependents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case policyId
        case policyCode
        case policyName
        case categoryName
        case coverageTier
        case policyNumber
        case enrollmentYear
        case startDate
        case endDate
        case annualEntitlement
        case usedAmount
        case pendingAmount
        case remainingBalance
        case currency
        case status
        case dependents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(policyId, forKey: .policyId)
        try container.encode(policyCode, forKey: .policyCode)
        try container.encode(policyName, forKey: .policyName)
        try container.encode(categoryName, forKey: .categoryName)
        try container.encode(coverageTier, forKey: .coverageTier)
        try container.encode(policyNumber, forKey: .policyNumber)
        try container.encode(enrollmentYear, forKey: .enrollmentYear)
        try container.encode(startDate, forKey: .startDate)
        try container.encode(endDate, forKey: .endDate)
        try container.encode(annualEntitlement, forKey: .annualEntitlement)
        try container.encode(usedAmount, forKey: .usedAmount)
        try container.encode(pendingAmount, forKey: .pendingAmount)
        try container.encode(remainingBalance, forKey: .remainingBalance)
        try container.encode(currency, forKey: .currency)
        try container.encode(status, forKey: .status)
        try container.encode(dependents, forKey: .dependents)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension EmployeeBenefitEnrollmentItem: Identifiable {}
