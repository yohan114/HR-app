//
// ExpenseClaimItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ExpenseClaimItem: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case draft = "DRAFT"
        case submitted = "SUBMITTED"
        case underReview = "UNDER_REVIEW"
        case approved = "APPROVED"
        case rejected = "REJECTED"
        case reimbursed = "REIMBURSED"
        case cancelled = "CANCELLED"
    }
    public var id: UUID
    public var claimNumber: String
    public var title: String
    public var claimDate: Date
    public var currency: String
    public var totalAmount: Double
    public var approvedAmount: Double?
    public var status: Status
    public var lineCount: Int
    public var rejectionReason: String?
    public var approvedAt: Date?
    public var reimbursedAt: Date?
    public var remarks: String?

    public init(id: UUID, claimNumber: String, title: String, claimDate: Date, currency: String, totalAmount: Double, approvedAmount: Double? = nil, status: Status, lineCount: Int, rejectionReason: String? = nil, approvedAt: Date? = nil, reimbursedAt: Date? = nil, remarks: String? = nil) {
        self.id = id
        self.claimNumber = claimNumber
        self.title = title
        self.claimDate = claimDate
        self.currency = currency
        self.totalAmount = totalAmount
        self.approvedAmount = approvedAmount
        self.status = status
        self.lineCount = lineCount
        self.rejectionReason = rejectionReason
        self.approvedAt = approvedAt
        self.reimbursedAt = reimbursedAt
        self.remarks = remarks
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case claimNumber
        case title
        case claimDate
        case currency
        case totalAmount
        case approvedAmount
        case status
        case lineCount
        case rejectionReason
        case approvedAt
        case reimbursedAt
        case remarks
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(claimNumber, forKey: .claimNumber)
        try container.encode(title, forKey: .title)
        try container.encode(claimDate, forKey: .claimDate)
        try container.encode(currency, forKey: .currency)
        try container.encode(totalAmount, forKey: .totalAmount)
        try container.encodeIfPresent(approvedAmount, forKey: .approvedAmount)
        try container.encode(status, forKey: .status)
        try container.encode(lineCount, forKey: .lineCount)
        try container.encodeIfPresent(rejectionReason, forKey: .rejectionReason)
        try container.encodeIfPresent(approvedAt, forKey: .approvedAt)
        try container.encodeIfPresent(reimbursedAt, forKey: .reimbursedAt)
        try container.encodeIfPresent(remarks, forKey: .remarks)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ExpenseClaimItem: Identifiable {}
