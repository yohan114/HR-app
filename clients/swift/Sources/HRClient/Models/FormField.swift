//
// FormField.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct FormField: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case text = "TEXT"
        case multilineText = "MULTILINE_TEXT"
        case number = "NUMBER"
        case date = "DATE"
        case dropdown = "DROPDOWN"
        case multiSelect = "MULTI_SELECT"
        case radio = "RADIO"
        case checkbox = "CHECKBOX"
        case attachment = "ATTACHMENT"
        case employee = "EMPLOYEE"
        case reference = "REFERENCE"
        case email = "EMAIL"
        case phone = "PHONE"
    }
    public var key: String
    /** Already localised. Falls back through region, then English, then the field key — never blank, because a form with an unlabelled field is unusable. */
    public var label: String
    public var type: ModelType
    public var _required: Bool? = false
    /** False when the caller may see the value but not change it. Distinct from omitting the field — a read-only salary grade still belongs on the profile, it simply is not the employee's to edit.  */
    public var editable: Bool? = true
    public var helpText: String?
    public var validation: FieldValidation?
    /** For DROPDOWN, MULTI_SELECT and RADIO. */
    public var options: [FieldOption]?
    /** For REFERENCE fields: which taxonomy supplies the options, e.g. `nationality`. The client already caches these, so the schema names the taxonomy rather than inlining several hundred options.  */
    public var referenceTable: String?
    /** True for tenant-defined fields. Clients need not care; support does. */
    public var custom: Bool? = false

    public init(key: String, label: String, type: ModelType, _required: Bool? = false, editable: Bool? = true, helpText: String? = nil, validation: FieldValidation? = nil, options: [FieldOption]? = nil, referenceTable: String? = nil, custom: Bool? = false) {
        self.key = key
        self.label = label
        self.type = type
        self._required = _required
        self.editable = editable
        self.helpText = helpText
        self.validation = validation
        self.options = options
        self.referenceTable = referenceTable
        self.custom = custom
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case key
        case label
        case type
        case _required = "required"
        case editable
        case helpText
        case validation
        case options
        case referenceTable
        case custom
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(key, forKey: .key)
        try container.encode(label, forKey: .label)
        try container.encode(type, forKey: .type)
        try container.encodeIfPresent(_required, forKey: ._required)
        try container.encodeIfPresent(editable, forKey: .editable)
        try container.encodeIfPresent(helpText, forKey: .helpText)
        try container.encodeIfPresent(validation, forKey: .validation)
        try container.encodeIfPresent(options, forKey: .options)
        try container.encodeIfPresent(referenceTable, forKey: .referenceTable)
        try container.encodeIfPresent(custom, forKey: .custom)
    }
}

