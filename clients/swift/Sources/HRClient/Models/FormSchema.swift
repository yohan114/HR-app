//
// FormSchema.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct FormSchema: Codable, JSONEncodable, Hashable {

    public var entityType: String
    /** A hash of the tenant's field definitions. Cache against it and skip re-rendering when unchanged. Deliberately not a timestamp — two servers must agree for the same configuration, or a client behind a load balancer sees the schema change on every other request.  */
    public var version: String
    public var sections: [FormSection]

    public init(entityType: String, version: String, sections: [FormSection]) {
        self.entityType = entityType
        self.version = version
        self.sections = sections
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case entityType
        case version
        case sections
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(entityType, forKey: .entityType)
        try container.encode(version, forKey: .version)
        try container.encode(sections, forKey: .sections)
    }
}

