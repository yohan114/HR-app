//
// GrievanceAppealItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GrievanceAppealItem: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case pending = "PENDING"
        case upheld = "UPHELD"
        case modified = "MODIFIED"
        case dismissed = "DISMISSED"
    }
    public var id: UUID
    public var grievanceId: UUID
    public var reason: String
    public var appealedAt: Date
    public var status: Status
    public var outcome: String?
    public var reviewedAt: Date?

    public init(id: UUID, grievanceId: UUID, reason: String, appealedAt: Date, status: Status, outcome: String? = nil, reviewedAt: Date? = nil) {
        self.id = id
        self.grievanceId = grievanceId
        self.reason = reason
        self.appealedAt = appealedAt
        self.status = status
        self.outcome = outcome
        self.reviewedAt = reviewedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case grievanceId
        case reason
        case appealedAt
        case status
        case outcome
        case reviewedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(grievanceId, forKey: .grievanceId)
        try container.encode(reason, forKey: .reason)
        try container.encode(appealedAt, forKey: .appealedAt)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(outcome, forKey: .outcome)
        try container.encodeIfPresent(reviewedAt, forKey: .reviewedAt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension GrievanceAppealItem: Identifiable {}
