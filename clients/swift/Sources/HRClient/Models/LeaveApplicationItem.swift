//
// LeaveApplicationItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct LeaveApplicationItem: Codable, JSONEncodable, Hashable {

    public enum DayPortion: String, Codable, CaseIterable {
        case fullDay = "FULL_DAY"
        case firstHalf = "FIRST_HALF"
        case secondHalf = "SECOND_HALF"
    }
    public enum Status: String, Codable, CaseIterable {
        case submitted = "SUBMITTED"
        case approved = "APPROVED"
        case rejected = "REJECTED"
        case cancelled = "CANCELLED"
        case withdrawn = "WITHDRAWN"
    }
    public var id: UUID
    public var employeeId: UUID
    public var employeeName: String
    public var leaveTypeId: String
    public var leaveTypeCode: String
    public var leaveTypeName: String
    public var startDate: Date
    public var endDate: Date
    public var dayPortion: DayPortion
    public var totalDays: Double
    public var reason: String
    public var status: Status
    public var submittedAt: Date
    public var approvedAt: Date?
    public var days: [LeaveApplicationDayItem]?

    public init(id: UUID, employeeId: UUID, employeeName: String, leaveTypeId: String, leaveTypeCode: String, leaveTypeName: String, startDate: Date, endDate: Date, dayPortion: DayPortion, totalDays: Double, reason: String, status: Status, submittedAt: Date, approvedAt: Date? = nil, days: [LeaveApplicationDayItem]? = nil) {
        self.id = id
        self.employeeId = employeeId
        self.employeeName = employeeName
        self.leaveTypeId = leaveTypeId
        self.leaveTypeCode = leaveTypeCode
        self.leaveTypeName = leaveTypeName
        self.startDate = startDate
        self.endDate = endDate
        self.dayPortion = dayPortion
        self.totalDays = totalDays
        self.reason = reason
        self.status = status
        self.submittedAt = submittedAt
        self.approvedAt = approvedAt
        self.days = days
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case employeeId
        case employeeName
        case leaveTypeId
        case leaveTypeCode
        case leaveTypeName
        case startDate
        case endDate
        case dayPortion
        case totalDays
        case reason
        case status
        case submittedAt
        case approvedAt
        case days
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(employeeId, forKey: .employeeId)
        try container.encode(employeeName, forKey: .employeeName)
        try container.encode(leaveTypeId, forKey: .leaveTypeId)
        try container.encode(leaveTypeCode, forKey: .leaveTypeCode)
        try container.encode(leaveTypeName, forKey: .leaveTypeName)
        try container.encode(startDate, forKey: .startDate)
        try container.encode(endDate, forKey: .endDate)
        try container.encode(dayPortion, forKey: .dayPortion)
        try container.encode(totalDays, forKey: .totalDays)
        try container.encode(reason, forKey: .reason)
        try container.encode(status, forKey: .status)
        try container.encode(submittedAt, forKey: .submittedAt)
        try container.encodeIfPresent(approvedAt, forKey: .approvedAt)
        try container.encodeIfPresent(days, forKey: .days)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension LeaveApplicationItem: Identifiable {}
