//
// LeaveBalanceItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct LeaveBalanceItem: Codable, JSONEncodable, Hashable {

    public var leaveTypeId: String
    public var leaveTypeCode: String
    public var leaveTypeName: String
    public var color: String
    public var entitledDays: Double
    public var accruedDays: Double
    public var takenDays: Double
    public var pendingDays: Double
    public var availableDays: Double

    public init(leaveTypeId: String, leaveTypeCode: String, leaveTypeName: String, color: String, entitledDays: Double, accruedDays: Double, takenDays: Double, pendingDays: Double, availableDays: Double) {
        self.leaveTypeId = leaveTypeId
        self.leaveTypeCode = leaveTypeCode
        self.leaveTypeName = leaveTypeName
        self.color = color
        self.entitledDays = entitledDays
        self.accruedDays = accruedDays
        self.takenDays = takenDays
        self.pendingDays = pendingDays
        self.availableDays = availableDays
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case leaveTypeId
        case leaveTypeCode
        case leaveTypeName
        case color
        case entitledDays
        case accruedDays
        case takenDays
        case pendingDays
        case availableDays
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(leaveTypeId, forKey: .leaveTypeId)
        try container.encode(leaveTypeCode, forKey: .leaveTypeCode)
        try container.encode(leaveTypeName, forKey: .leaveTypeName)
        try container.encode(color, forKey: .color)
        try container.encode(entitledDays, forKey: .entitledDays)
        try container.encode(accruedDays, forKey: .accruedDays)
        try container.encode(takenDays, forKey: .takenDays)
        try container.encode(pendingDays, forKey: .pendingDays)
        try container.encode(availableDays, forKey: .availableDays)
    }
}

