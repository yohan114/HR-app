//
// NotificationSettings.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct NotificationSettings: Codable, JSONEncodable, Hashable {

    /** Only entries differing from the default are stored and returned. An absent (event, channel) pair is at its default: in-app, push and email on, SMS off.  */
    public var preferences: [ChannelPreference]
    public var quietHours: QuietHoursSettings?

    public init(preferences: [ChannelPreference], quietHours: QuietHoursSettings? = nil) {
        self.preferences = preferences
        self.quietHours = quietHours
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case preferences
        case quietHours
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(preferences, forKey: .preferences)
        try container.encodeIfPresent(quietHours, forKey: .quietHours)
    }
}

