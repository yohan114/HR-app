//
// QuietHoursSettings.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct QuietHoursSettings: Codable, JSONEncodable, Hashable {

    /** Local wall-clock time. A `startAt` after `endAt` is a window crossing midnight. */
    public var startAt: String
    public var endAt: String
    /** IANA zone name, not an offset. An offset is wrong twice a year wherever daylight saving applies, and the symptom is notifications arriving an hour into the night for months. Rejected with `VALIDATION_FAILED` if the server cannot resolve it.  */
    public var timezone: String
    /** Weekdays the window applies to, 1 = Monday. Empty means every day. For a window crossing midnight the day is the one the window *opened* on, so `[5]` with 22:00–07:00 covers Friday night into Saturday morning.  */
    public var days: [Int]?
    public var enabled: Bool? = true

    public init(startAt: String, endAt: String, timezone: String, days: [Int]? = nil, enabled: Bool? = true) {
        self.startAt = startAt
        self.endAt = endAt
        self.timezone = timezone
        self.days = days
        self.enabled = enabled
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case startAt
        case endAt
        case timezone
        case days
        case enabled
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(startAt, forKey: .startAt)
        try container.encode(endAt, forKey: .endAt)
        try container.encode(timezone, forKey: .timezone)
        try container.encodeIfPresent(days, forKey: .days)
        try container.encodeIfPresent(enabled, forKey: .enabled)
    }
}

