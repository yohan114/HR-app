//
// RecoveryCodesResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct RecoveryCodesResponse: Codable, JSONEncodable, Hashable {

    /** Shown **once**. Stored only as hashes, so they cannot be retrieved again — a client that does not display them here has lost them for that user.  */
    public var recoveryCodes: [String]

    public init(recoveryCodes: [String]) {
        self.recoveryCodes = recoveryCodes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case recoveryCodes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(recoveryCodes, forKey: .recoveryCodes)
    }
}

