//
// ReferenceItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** A row in a configurable lookup list — blood group, marital status, and so on. */
public struct ReferenceItem: Codable, JSONEncodable, Hashable {

    public var id: UUID
    public var code: String
    public var name: String
    public var description: String?
    /** Display order. Lower first. */
    public var sequence: Int
    /** Inactive items are retained so historical records still resolve, but are not offered for new entry. */
    public var active: Bool

    public init(id: UUID, code: String, name: String, description: String? = nil, sequence: Int, active: Bool) {
        self.id = id
        self.code = code
        self.name = name
        self.description = description
        self.sequence = sequence
        self.active = active
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case code
        case name
        case description
        case sequence
        case active
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(code, forKey: .code)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encode(sequence, forKey: .sequence)
        try container.encode(active, forKey: .active)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ReferenceItem: Identifiable {}
