//
// TodayAttendanceResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct TodayAttendanceResponse: Codable, JSONEncodable, Hashable {

    public var employeeId: UUID
    public var workDate: Date
    /** NOT_CHECKED_IN, PRESENT, HALF_DAY, ON_LEAVE, REST_DAY, HOLIDAY */
    public var dayStatus: String
    /** NOT_CHECKED_IN, CHECKED_IN, ON_BREAK, CHECKED_OUT */
    public var currentStatus: String
    public var shift: AttendanceShiftInfo?
    public var firstInAt: Date?
    public var lastOutAt: Date?
    public var workedMinutes: Int
    public var punches: [AttendancePunchItem]
    public var geofence: BranchGeofenceInfo

    public init(employeeId: UUID, workDate: Date, dayStatus: String, currentStatus: String, shift: AttendanceShiftInfo? = nil, firstInAt: Date? = nil, lastOutAt: Date? = nil, workedMinutes: Int, punches: [AttendancePunchItem], geofence: BranchGeofenceInfo) {
        self.employeeId = employeeId
        self.workDate = workDate
        self.dayStatus = dayStatus
        self.currentStatus = currentStatus
        self.shift = shift
        self.firstInAt = firstInAt
        self.lastOutAt = lastOutAt
        self.workedMinutes = workedMinutes
        self.punches = punches
        self.geofence = geofence
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case employeeId
        case workDate
        case dayStatus
        case currentStatus
        case shift
        case firstInAt
        case lastOutAt
        case workedMinutes
        case punches
        case geofence
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(employeeId, forKey: .employeeId)
        try container.encode(workDate, forKey: .workDate)
        try container.encode(dayStatus, forKey: .dayStatus)
        try container.encode(currentStatus, forKey: .currentStatus)
        try container.encodeIfPresent(shift, forKey: .shift)
        try container.encodeIfPresent(firstInAt, forKey: .firstInAt)
        try container.encodeIfPresent(lastOutAt, forKey: .lastOutAt)
        try container.encode(workedMinutes, forKey: .workedMinutes)
        try container.encode(punches, forKey: .punches)
        try container.encode(geofence, forKey: .geofence)
    }
}

