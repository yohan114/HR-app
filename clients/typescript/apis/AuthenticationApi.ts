/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiErrorResponse,
  BiometricGrantRequest,
  Device,
  JwkSet,
  MfaCodeRequest,
  MfaEnrolment,
  MfaStatus,
  MfaVerifyRequest,
  PasswordGrantRequest,
  RecoveryCodesResponse,
  RefreshTokenRequest,
  RegisterDeviceRequest,
  ResolveTenantRequest,
  ResolveTenantResponse,
  TokenResponse,
} from '../models/index';
import {
    ApiErrorResponseFromJSON,
    ApiErrorResponseToJSON,
    BiometricGrantRequestFromJSON,
    BiometricGrantRequestToJSON,
    DeviceFromJSON,
    DeviceToJSON,
    JwkSetFromJSON,
    JwkSetToJSON,
    MfaCodeRequestFromJSON,
    MfaCodeRequestToJSON,
    MfaEnrolmentFromJSON,
    MfaEnrolmentToJSON,
    MfaStatusFromJSON,
    MfaStatusToJSON,
    MfaVerifyRequestFromJSON,
    MfaVerifyRequestToJSON,
    PasswordGrantRequestFromJSON,
    PasswordGrantRequestToJSON,
    RecoveryCodesResponseFromJSON,
    RecoveryCodesResponseToJSON,
    RefreshTokenRequestFromJSON,
    RefreshTokenRequestToJSON,
    RegisterDeviceRequestFromJSON,
    RegisterDeviceRequestToJSON,
    ResolveTenantRequestFromJSON,
    ResolveTenantRequestToJSON,
    ResolveTenantResponseFromJSON,
    ResolveTenantResponseToJSON,
    TokenResponseFromJSON,
    TokenResponseToJSON,
} from '../models/index';

export interface BiometricTokenRequest {
    xTenantCode: string;
    biometricGrantRequest: BiometricGrantRequest;
}

export interface ConfirmMfaEnrolmentRequest {
    mfaCodeRequest: MfaCodeRequest;
}

export interface DisableMfaRequest {
    mfaCodeRequest: MfaCodeRequest;
}

export interface IssueTokenRequest {
    xTenantCode: string;
    passwordGrantRequest: PasswordGrantRequest;
}

export interface LogoutRequest {
    refreshTokenRequest?: RefreshTokenRequest;
}

export interface RefreshTokenOperationRequest {
    xTenantCode: string;
    refreshTokenRequest: RefreshTokenRequest;
}

export interface RegenerateRecoveryCodesRequest {
    mfaCodeRequest: MfaCodeRequest;
}

export interface RegisterDeviceOperationRequest {
    registerDeviceRequest: RegisterDeviceRequest;
}

export interface ResolveTenantOperationRequest {
    xTenantCode: string;
    resolveTenantRequest: ResolveTenantRequest;
}

export interface RevokeDeviceRequest {
    id: string;
}

export interface VerifyMfaRequest {
    xTenantCode: string;
    mfaVerifyRequest: MfaVerifyRequest;
}

/**
 * AuthenticationApi - interface
 * 
 * @export
 * @interface AuthenticationApiInterface
 */
export interface AuthenticationApiInterface {
    /**
     * Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code. The secret is stored immediately but **MFA is not switched on** until `/v1/auth/mfa/enrol/confirm` succeeds.  Two steps rather than one deliberately: a user who mis-scans, or scans into an app they then delete, would otherwise lock themselves out of their own account with no route back except an administrator — which is a support call and a social-engineering path straight past the factor. Calling this again replaces the pending secret, which is what someone does after scanning into the wrong app. 
     * @summary Begin enrolment — returns a secret and a QR payload
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    beginMfaEnrolmentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MfaEnrolment>>;

    /**
     * Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code. The secret is stored immediately but **MFA is not switched on** until `/v1/auth/mfa/enrol/confirm` succeeds.  Two steps rather than one deliberately: a user who mis-scans, or scans into an app they then delete, would otherwise lock themselves out of their own account with no route back except an administrator — which is a support call and a social-engineering path straight past the factor. Calling this again replaces the pending secret, which is what someone does after scanning into the wrong app. 
     * Begin enrolment — returns a secret and a QR payload
     */
    beginMfaEnrolment(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MfaEnrolment>;

    /**
     * The core of the \"no password after enrolment\" experience.  The refresh token is sealed at enrolment in the device\'s Android Keystore or iOS Secure Enclave under a key that requires user authentication. The OS releases it only after a successful fingerprint or face match, and invalidates the key entirely if the device\'s enrolled biometrics change.  The server therefore trusts the *presentation* of the sealed token as evidence that the biometric check passed — it never receives or stores biometric data itself.  Two conditions beyond a normal refresh: the token must have been issued to the device presenting it, and that device must have completed biometric enrolment. Together these stop a token exfiltrated from one device being replayed from another. 
     * @summary Exchange a device-sealed refresh token after a biometric assertion
     * @param {string} xTenantCode Organisation code. Required on unauthenticated endpoints, where no token exists yet.
     * @param {BiometricGrantRequest} biometricGrantRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    biometricTokenRaw(requestParameters: BiometricTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>>;

    /**
     * The core of the \"no password after enrolment\" experience.  The refresh token is sealed at enrolment in the device\'s Android Keystore or iOS Secure Enclave under a key that requires user authentication. The OS releases it only after a successful fingerprint or face match, and invalidates the key entirely if the device\'s enrolled biometrics change.  The server therefore trusts the *presentation* of the sealed token as evidence that the biometric check passed — it never receives or stores biometric data itself.  Two conditions beyond a normal refresh: the token must have been issued to the device presenting it, and that device must have completed biometric enrolment. Together these stop a token exfiltrated from one device being replayed from another. 
     * Exchange a device-sealed refresh token after a biometric assertion
     */
    biometricToken(requestParameters: BiometricTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse>;

    /**
     * Proves the app and the server agree before anything depends on it, then switches MFA on.  **Returns the recovery codes, and this is the only time they exist in plaintext.** They are stored as hashes, so neither the server nor a database backup can produce them again. A client that does not show them here has lost them for that user. 
     * @summary Confirm enrolment with a code from the authenticator
     * @param {MfaCodeRequest} mfaCodeRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    confirmMfaEnrolmentRaw(requestParameters: ConfirmMfaEnrolmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecoveryCodesResponse>>;

    /**
     * Proves the app and the server agree before anything depends on it, then switches MFA on.  **Returns the recovery codes, and this is the only time they exist in plaintext.** They are stored as hashes, so neither the server nor a database backup can produce them again. A client that does not show them here has lost them for that user. 
     * Confirm enrolment with a code from the authenticator
     */
    confirmMfaEnrolment(requestParameters: ConfirmMfaEnrolmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecoveryCodesResponse>;

    /**
     * Requires a current code — possession, not just a live session. Without that, anyone who borrowed an unlocked laptop could switch the factor off from a settings screen, which makes having it decorative. 
     * @summary Turn the second factor off
     * @param {MfaCodeRequest} mfaCodeRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    disableMfaRaw(requestParameters: DisableMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Requires a current code — possession, not just a live session. Without that, anyone who borrowed an unlocked laptop could switch the factor off from a settings screen, which makes having it decorative. 
     * Turn the second factor off
     */
    disableMfa(requestParameters: DisableMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * The public half of the key used to sign access tokens, so that other services, an API gateway or a third-party integration can verify our tokens without holding any credential of their own.  The application itself does not call this endpoint — it verifies with the in-process public key rather than issuing an HTTP request to itself.  During a key rotation window this returns both the outgoing and incoming keys, so tokens signed with either continue to verify. 
     * @summary Public JSON Web Key Set
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    getJwksRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<JwkSet>>;

    /**
     * The public half of the key used to sign access tokens, so that other services, an API gateway or a third-party integration can verify our tokens without holding any credential of their own.  The application itself does not call this endpoint — it verifies with the in-process public key rather than issuing an HTTP request to itself.  During a key rotation window this returns both the outgoing and incoming keys, so tokens signed with either continue to verify. 
     * Public JSON Web Key Set
     */
    getJwks(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<JwkSet>;

    /**
     * Drives the security settings screen: whether to offer enrolment, resume a half-finished one, or warn that recovery codes are running low. 
     * @summary Whether a second factor is enrolled
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    getMfaStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MfaStatus>>;

    /**
     * Drives the security settings screen: whether to offer enrolment, resume a half-finished one, or warn that recovery codes are running low. 
     * Whether a second factor is enrolled
     */
    getMfaStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MfaStatus>;

    /**
     * Exchanges credentials for an access and refresh token pair, and registers the calling device in the same round trip so the client can offer biometric enrolment immediately.  Failures are deliberately uniform: a wrong password and an unknown username both return `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated failures lock the account per the tenant\'s password policy. 
     * @summary Sign in with a password
     * @param {string} xTenantCode Organisation code. Required on unauthenticated endpoints, where no token exists yet.
     * @param {PasswordGrantRequest} passwordGrantRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    issueTokenRaw(requestParameters: IssueTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>>;

    /**
     * Exchanges credentials for an access and refresh token pair, and registers the calling device in the same round trip so the client can offer biometric enrolment immediately.  Failures are deliberately uniform: a wrong password and an unknown username both return `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated failures lock the account per the tenant\'s password policy. 
     * Sign in with a password
     */
    issueToken(requestParameters: IssueTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse>;

    /**
     * Powers the \"where am I signed in?\" screen. The device making the request is flagged with `current: true` so the client can avoid offering the user a way to revoke themselves without warning. 
     * @summary List the current user\'s registered devices
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    listDevicesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<Device>>>;

    /**
     * Powers the \"where am I signed in?\" screen. The device making the request is flagged with `current: true` so the client can avoid offering the user a way to revoke themselves without warning. 
     * List the current user\'s registered devices
     */
    listDevices(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<Device>>;

    /**
     * Revokes the token family the presented refresh token belongs to. Other devices stay signed in — signing a user out everywhere is a separate, deliberate action.  The body is optional. Without it the caller\'s entire set of sessions is revoked, which is the correct fallback when a client has lost track of its refresh token.  Never fails: sign-out is idempotent and a client must always be able to end its session. 
     * @summary Sign out
     * @param {RefreshTokenRequest} [refreshTokenRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    logoutRaw(requestParameters: LogoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Revokes the token family the presented refresh token belongs to. Other devices stay signed in — signing a user out everywhere is a separate, deliberate action.  The body is optional. Without it the caller\'s entire set of sessions is revoked, which is the correct fallback when a client has lost track of its refresh token.  Never fails: sign-out is idempotent and a client must always be able to end its session. 
     * Sign out
     */
    logout(requestParameters: LogoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Refresh tokens are single-use and rotate on every exchange.  If a token that has already been used is presented again, the most likely explanation is theft, so the entire token family is revoked and every device sharing that login session must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.  Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security, so the tenant must be known before the token can be found. 
     * @summary Exchange a refresh token for a new access token
     * @param {string} xTenantCode Organisation code. Required on unauthenticated endpoints, where no token exists yet.
     * @param {RefreshTokenRequest} refreshTokenRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    refreshTokenRaw(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>>;

    /**
     * Refresh tokens are single-use and rotate on every exchange.  If a token that has already been used is presented again, the most likely explanation is theft, so the entire token family is revoked and every device sharing that login session must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.  Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security, so the tenant must be known before the token can be found. 
     * Exchange a refresh token for a new access token
     */
    refreshToken(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse>;

    /**
     * Invalidates every existing code. Requires a current code, for the same reason as disabling. 
     * @summary Issue a fresh set of recovery codes
     * @param {MfaCodeRequest} mfaCodeRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    regenerateRecoveryCodesRaw(requestParameters: RegenerateRecoveryCodesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecoveryCodesResponse>>;

    /**
     * Invalidates every existing code. Requires a current code, for the same reason as disabling. 
     * Issue a fresh set of recovery codes
     */
    regenerateRecoveryCodes(requestParameters: RegenerateRecoveryCodesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecoveryCodesResponse>;

    /**
     * Registers or updates a device for the authenticated user, including its push token.  Called on sign-in and again whenever the push token rotates. Idempotent on `(user, deviceId)`: re-registering an existing device updates it rather than creating a duplicate. 
     * @summary Register the current device
     * @param {RegisterDeviceRequest} registerDeviceRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    registerDeviceRaw(requestParameters: RegisterDeviceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Device>>;

    /**
     * Registers or updates a device for the authenticated user, including its push token.  Called on sign-in and again whenever the push token rotates. Idempotent on `(user, deviceId)`: re-registering an existing device updates it rather than creating a duplicate. 
     * Register the current device
     */
    registerDevice(requestParameters: RegisterDeviceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Device>;

    /**
     * The first step of sign-in. Deliberately does **not** reveal whether a user account exists — it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.  This replaces asking the user to type a \"service URL\", which is a common source of support load in comparable products. 
     * @summary Resolve an organisation from a work email or org code
     * @param {string} xTenantCode Organisation code. Required on unauthenticated endpoints, where no token exists yet.
     * @param {ResolveTenantRequest} resolveTenantRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    resolveTenantRaw(requestParameters: ResolveTenantOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResolveTenantResponse>>;

    /**
     * The first step of sign-in. Deliberately does **not** reveal whether a user account exists — it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.  This replaces asking the user to type a \"service URL\", which is a common source of support load in comparable products. 
     * Resolve an organisation from a work email or org code
     */
    resolveTenant(requestParameters: ResolveTenantOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResolveTenantResponse>;

    /**
     * Revokes the device and every refresh token bound to it. Takes effect immediately for refresh and biometric grants; an access token already issued remains valid until it expires, at most fifteen minutes.  Note the parameter is the device record\'s `id` (a UUID), not its `deviceId` (the client-generated string). The two are distinct and naming the path variable `deviceId` was actively misleading. 
     * @summary Revoke a device
     * @param {string} id The device record\&#39;s UUID, as returned by &#x60;GET /v1/auth/devices&#x60;.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    revokeDeviceRaw(requestParameters: RevokeDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Revokes the device and every refresh token bound to it. Takes effect immediately for refresh and biometric grants; an access token already issued remains valid until it expires, at most fifteen minutes.  Note the parameter is the device record\'s `id` (a UUID), not its `deviceId` (the client-generated string). The two are distinct and naming the path variable `deviceId` was actively misleading. 
     * Revoke a device
     */
    revokeDevice(requestParameters: RevokeDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * The second half of a password sign-in. Called after `/v1/auth/token` answers `401 MFA_REQUIRED` with an `mfaToken` in `details`.  Accepts either a TOTP code or a recovery code — the user cannot always tell you which they are holding, and making them choose adds a decision at the worst possible moment. A recovery code is consumed on use.  The challenge token goes in the **body**, not the `Authorization` header: it is not a bearer token for this API, and putting it there invites proxies and client libraries to treat it as one. It carries no roles and no employee id, so even if something did accept it as a session it would grant nothing.  Every failure — wrong code, spent recovery code, account without a second factor — returns the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge which case they are in. 
     * @summary Complete a sign-in with a second factor
     * @param {string} xTenantCode Organisation code. Required on unauthenticated endpoints, where no token exists yet.
     * @param {MfaVerifyRequest} mfaVerifyRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthenticationApiInterface
     */
    verifyMfaRaw(requestParameters: VerifyMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>>;

    /**
     * The second half of a password sign-in. Called after `/v1/auth/token` answers `401 MFA_REQUIRED` with an `mfaToken` in `details`.  Accepts either a TOTP code or a recovery code — the user cannot always tell you which they are holding, and making them choose adds a decision at the worst possible moment. A recovery code is consumed on use.  The challenge token goes in the **body**, not the `Authorization` header: it is not a bearer token for this API, and putting it there invites proxies and client libraries to treat it as one. It carries no roles and no employee id, so even if something did accept it as a session it would grant nothing.  Every failure — wrong code, spent recovery code, account without a second factor — returns the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge which case they are in. 
     * Complete a sign-in with a second factor
     */
    verifyMfa(requestParameters: VerifyMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse>;

}

/**
 * 
 */
export class AuthenticationApi extends runtime.BaseAPI implements AuthenticationApiInterface {

    /**
     * Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code. The secret is stored immediately but **MFA is not switched on** until `/v1/auth/mfa/enrol/confirm` succeeds.  Two steps rather than one deliberately: a user who mis-scans, or scans into an app they then delete, would otherwise lock themselves out of their own account with no route back except an administrator — which is a support call and a social-engineering path straight past the factor. Calling this again replaces the pending secret, which is what someone does after scanning into the wrong app. 
     * Begin enrolment — returns a secret and a QR payload
     */
    async beginMfaEnrolmentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MfaEnrolment>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/mfa/enrol`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MfaEnrolmentFromJSON(jsonValue));
    }

    /**
     * Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code. The secret is stored immediately but **MFA is not switched on** until `/v1/auth/mfa/enrol/confirm` succeeds.  Two steps rather than one deliberately: a user who mis-scans, or scans into an app they then delete, would otherwise lock themselves out of their own account with no route back except an administrator — which is a support call and a social-engineering path straight past the factor. Calling this again replaces the pending secret, which is what someone does after scanning into the wrong app. 
     * Begin enrolment — returns a secret and a QR payload
     */
    async beginMfaEnrolment(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MfaEnrolment> {
        const response = await this.beginMfaEnrolmentRaw(initOverrides);
        return await response.value();
    }

    /**
     * The core of the \"no password after enrolment\" experience.  The refresh token is sealed at enrolment in the device\'s Android Keystore or iOS Secure Enclave under a key that requires user authentication. The OS releases it only after a successful fingerprint or face match, and invalidates the key entirely if the device\'s enrolled biometrics change.  The server therefore trusts the *presentation* of the sealed token as evidence that the biometric check passed — it never receives or stores biometric data itself.  Two conditions beyond a normal refresh: the token must have been issued to the device presenting it, and that device must have completed biometric enrolment. Together these stop a token exfiltrated from one device being replayed from another. 
     * Exchange a device-sealed refresh token after a biometric assertion
     */
    async biometricTokenRaw(requestParameters: BiometricTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>> {
        if (requestParameters['xTenantCode'] == null) {
            throw new runtime.RequiredError(
                'xTenantCode',
                'Required parameter "xTenantCode" was null or undefined when calling biometricToken().'
            );
        }

        if (requestParameters['biometricGrantRequest'] == null) {
            throw new runtime.RequiredError(
                'biometricGrantRequest',
                'Required parameter "biometricGrantRequest" was null or undefined when calling biometricToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xTenantCode'] != null) {
            headerParameters['X-Tenant-Code'] = String(requestParameters['xTenantCode']);
        }

        const response = await this.request({
            path: `/v1/auth/token/biometric`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BiometricGrantRequestToJSON(requestParameters['biometricGrantRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokenResponseFromJSON(jsonValue));
    }

    /**
     * The core of the \"no password after enrolment\" experience.  The refresh token is sealed at enrolment in the device\'s Android Keystore or iOS Secure Enclave under a key that requires user authentication. The OS releases it only after a successful fingerprint or face match, and invalidates the key entirely if the device\'s enrolled biometrics change.  The server therefore trusts the *presentation* of the sealed token as evidence that the biometric check passed — it never receives or stores biometric data itself.  Two conditions beyond a normal refresh: the token must have been issued to the device presenting it, and that device must have completed biometric enrolment. Together these stop a token exfiltrated from one device being replayed from another. 
     * Exchange a device-sealed refresh token after a biometric assertion
     */
    async biometricToken(requestParameters: BiometricTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse> {
        const response = await this.biometricTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Proves the app and the server agree before anything depends on it, then switches MFA on.  **Returns the recovery codes, and this is the only time they exist in plaintext.** They are stored as hashes, so neither the server nor a database backup can produce them again. A client that does not show them here has lost them for that user. 
     * Confirm enrolment with a code from the authenticator
     */
    async confirmMfaEnrolmentRaw(requestParameters: ConfirmMfaEnrolmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecoveryCodesResponse>> {
        if (requestParameters['mfaCodeRequest'] == null) {
            throw new runtime.RequiredError(
                'mfaCodeRequest',
                'Required parameter "mfaCodeRequest" was null or undefined when calling confirmMfaEnrolment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/mfa/enrol/confirm`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MfaCodeRequestToJSON(requestParameters['mfaCodeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RecoveryCodesResponseFromJSON(jsonValue));
    }

    /**
     * Proves the app and the server agree before anything depends on it, then switches MFA on.  **Returns the recovery codes, and this is the only time they exist in plaintext.** They are stored as hashes, so neither the server nor a database backup can produce them again. A client that does not show them here has lost them for that user. 
     * Confirm enrolment with a code from the authenticator
     */
    async confirmMfaEnrolment(requestParameters: ConfirmMfaEnrolmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecoveryCodesResponse> {
        const response = await this.confirmMfaEnrolmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Requires a current code — possession, not just a live session. Without that, anyone who borrowed an unlocked laptop could switch the factor off from a settings screen, which makes having it decorative. 
     * Turn the second factor off
     */
    async disableMfaRaw(requestParameters: DisableMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['mfaCodeRequest'] == null) {
            throw new runtime.RequiredError(
                'mfaCodeRequest',
                'Required parameter "mfaCodeRequest" was null or undefined when calling disableMfa().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/mfa/disable`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MfaCodeRequestToJSON(requestParameters['mfaCodeRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Requires a current code — possession, not just a live session. Without that, anyone who borrowed an unlocked laptop could switch the factor off from a settings screen, which makes having it decorative. 
     * Turn the second factor off
     */
    async disableMfa(requestParameters: DisableMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.disableMfaRaw(requestParameters, initOverrides);
    }

    /**
     * The public half of the key used to sign access tokens, so that other services, an API gateway or a third-party integration can verify our tokens without holding any credential of their own.  The application itself does not call this endpoint — it verifies with the in-process public key rather than issuing an HTTP request to itself.  During a key rotation window this returns both the outgoing and incoming keys, so tokens signed with either continue to verify. 
     * Public JSON Web Key Set
     */
    async getJwksRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<JwkSet>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/auth/.well-known/jwks.json`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => JwkSetFromJSON(jsonValue));
    }

    /**
     * The public half of the key used to sign access tokens, so that other services, an API gateway or a third-party integration can verify our tokens without holding any credential of their own.  The application itself does not call this endpoint — it verifies with the in-process public key rather than issuing an HTTP request to itself.  During a key rotation window this returns both the outgoing and incoming keys, so tokens signed with either continue to verify. 
     * Public JSON Web Key Set
     */
    async getJwks(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<JwkSet> {
        const response = await this.getJwksRaw(initOverrides);
        return await response.value();
    }

    /**
     * Drives the security settings screen: whether to offer enrolment, resume a half-finished one, or warn that recovery codes are running low. 
     * Whether a second factor is enrolled
     */
    async getMfaStatusRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MfaStatus>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/mfa`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MfaStatusFromJSON(jsonValue));
    }

    /**
     * Drives the security settings screen: whether to offer enrolment, resume a half-finished one, or warn that recovery codes are running low. 
     * Whether a second factor is enrolled
     */
    async getMfaStatus(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MfaStatus> {
        const response = await this.getMfaStatusRaw(initOverrides);
        return await response.value();
    }

    /**
     * Exchanges credentials for an access and refresh token pair, and registers the calling device in the same round trip so the client can offer biometric enrolment immediately.  Failures are deliberately uniform: a wrong password and an unknown username both return `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated failures lock the account per the tenant\'s password policy. 
     * Sign in with a password
     */
    async issueTokenRaw(requestParameters: IssueTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>> {
        if (requestParameters['xTenantCode'] == null) {
            throw new runtime.RequiredError(
                'xTenantCode',
                'Required parameter "xTenantCode" was null or undefined when calling issueToken().'
            );
        }

        if (requestParameters['passwordGrantRequest'] == null) {
            throw new runtime.RequiredError(
                'passwordGrantRequest',
                'Required parameter "passwordGrantRequest" was null or undefined when calling issueToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xTenantCode'] != null) {
            headerParameters['X-Tenant-Code'] = String(requestParameters['xTenantCode']);
        }

        const response = await this.request({
            path: `/v1/auth/token`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PasswordGrantRequestToJSON(requestParameters['passwordGrantRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokenResponseFromJSON(jsonValue));
    }

    /**
     * Exchanges credentials for an access and refresh token pair, and registers the calling device in the same round trip so the client can offer biometric enrolment immediately.  Failures are deliberately uniform: a wrong password and an unknown username both return `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated failures lock the account per the tenant\'s password policy. 
     * Sign in with a password
     */
    async issueToken(requestParameters: IssueTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse> {
        const response = await this.issueTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Powers the \"where am I signed in?\" screen. The device making the request is flagged with `current: true` so the client can avoid offering the user a way to revoke themselves without warning. 
     * List the current user\'s registered devices
     */
    async listDevicesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<Device>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/devices`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DeviceFromJSON));
    }

    /**
     * Powers the \"where am I signed in?\" screen. The device making the request is flagged with `current: true` so the client can avoid offering the user a way to revoke themselves without warning. 
     * List the current user\'s registered devices
     */
    async listDevices(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<Device>> {
        const response = await this.listDevicesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Revokes the token family the presented refresh token belongs to. Other devices stay signed in — signing a user out everywhere is a separate, deliberate action.  The body is optional. Without it the caller\'s entire set of sessions is revoked, which is the correct fallback when a client has lost track of its refresh token.  Never fails: sign-out is idempotent and a client must always be able to end its session. 
     * Sign out
     */
    async logoutRaw(requestParameters: LogoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/logout`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RefreshTokenRequestToJSON(requestParameters['refreshTokenRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Revokes the token family the presented refresh token belongs to. Other devices stay signed in — signing a user out everywhere is a separate, deliberate action.  The body is optional. Without it the caller\'s entire set of sessions is revoked, which is the correct fallback when a client has lost track of its refresh token.  Never fails: sign-out is idempotent and a client must always be able to end its session. 
     * Sign out
     */
    async logout(requestParameters: LogoutRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.logoutRaw(requestParameters, initOverrides);
    }

    /**
     * Refresh tokens are single-use and rotate on every exchange.  If a token that has already been used is presented again, the most likely explanation is theft, so the entire token family is revoked and every device sharing that login session must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.  Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security, so the tenant must be known before the token can be found. 
     * Exchange a refresh token for a new access token
     */
    async refreshTokenRaw(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>> {
        if (requestParameters['xTenantCode'] == null) {
            throw new runtime.RequiredError(
                'xTenantCode',
                'Required parameter "xTenantCode" was null or undefined when calling refreshToken().'
            );
        }

        if (requestParameters['refreshTokenRequest'] == null) {
            throw new runtime.RequiredError(
                'refreshTokenRequest',
                'Required parameter "refreshTokenRequest" was null or undefined when calling refreshToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xTenantCode'] != null) {
            headerParameters['X-Tenant-Code'] = String(requestParameters['xTenantCode']);
        }

        const response = await this.request({
            path: `/v1/auth/token/refresh`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RefreshTokenRequestToJSON(requestParameters['refreshTokenRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokenResponseFromJSON(jsonValue));
    }

    /**
     * Refresh tokens are single-use and rotate on every exchange.  If a token that has already been used is presented again, the most likely explanation is theft, so the entire token family is revoked and every device sharing that login session must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.  Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security, so the tenant must be known before the token can be found. 
     * Exchange a refresh token for a new access token
     */
    async refreshToken(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse> {
        const response = await this.refreshTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Invalidates every existing code. Requires a current code, for the same reason as disabling. 
     * Issue a fresh set of recovery codes
     */
    async regenerateRecoveryCodesRaw(requestParameters: RegenerateRecoveryCodesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RecoveryCodesResponse>> {
        if (requestParameters['mfaCodeRequest'] == null) {
            throw new runtime.RequiredError(
                'mfaCodeRequest',
                'Required parameter "mfaCodeRequest" was null or undefined when calling regenerateRecoveryCodes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/mfa/recovery-codes`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MfaCodeRequestToJSON(requestParameters['mfaCodeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RecoveryCodesResponseFromJSON(jsonValue));
    }

    /**
     * Invalidates every existing code. Requires a current code, for the same reason as disabling. 
     * Issue a fresh set of recovery codes
     */
    async regenerateRecoveryCodes(requestParameters: RegenerateRecoveryCodesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RecoveryCodesResponse> {
        const response = await this.regenerateRecoveryCodesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Registers or updates a device for the authenticated user, including its push token.  Called on sign-in and again whenever the push token rotates. Idempotent on `(user, deviceId)`: re-registering an existing device updates it rather than creating a duplicate. 
     * Register the current device
     */
    async registerDeviceRaw(requestParameters: RegisterDeviceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Device>> {
        if (requestParameters['registerDeviceRequest'] == null) {
            throw new runtime.RequiredError(
                'registerDeviceRequest',
                'Required parameter "registerDeviceRequest" was null or undefined when calling registerDevice().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/devices`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RegisterDeviceRequestToJSON(requestParameters['registerDeviceRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DeviceFromJSON(jsonValue));
    }

    /**
     * Registers or updates a device for the authenticated user, including its push token.  Called on sign-in and again whenever the push token rotates. Idempotent on `(user, deviceId)`: re-registering an existing device updates it rather than creating a duplicate. 
     * Register the current device
     */
    async registerDevice(requestParameters: RegisterDeviceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Device> {
        const response = await this.registerDeviceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The first step of sign-in. Deliberately does **not** reveal whether a user account exists — it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.  This replaces asking the user to type a \"service URL\", which is a common source of support load in comparable products. 
     * Resolve an organisation from a work email or org code
     */
    async resolveTenantRaw(requestParameters: ResolveTenantOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResolveTenantResponse>> {
        if (requestParameters['xTenantCode'] == null) {
            throw new runtime.RequiredError(
                'xTenantCode',
                'Required parameter "xTenantCode" was null or undefined when calling resolveTenant().'
            );
        }

        if (requestParameters['resolveTenantRequest'] == null) {
            throw new runtime.RequiredError(
                'resolveTenantRequest',
                'Required parameter "resolveTenantRequest" was null or undefined when calling resolveTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xTenantCode'] != null) {
            headerParameters['X-Tenant-Code'] = String(requestParameters['xTenantCode']);
        }

        const response = await this.request({
            path: `/v1/auth/resolve-tenant`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResolveTenantRequestToJSON(requestParameters['resolveTenantRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResolveTenantResponseFromJSON(jsonValue));
    }

    /**
     * The first step of sign-in. Deliberately does **not** reveal whether a user account exists — it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.  This replaces asking the user to type a \"service URL\", which is a common source of support load in comparable products. 
     * Resolve an organisation from a work email or org code
     */
    async resolveTenant(requestParameters: ResolveTenantOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResolveTenantResponse> {
        const response = await this.resolveTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Revokes the device and every refresh token bound to it. Takes effect immediately for refresh and biometric grants; an access token already issued remains valid until it expires, at most fifteen minutes.  Note the parameter is the device record\'s `id` (a UUID), not its `deviceId` (the client-generated string). The two are distinct and naming the path variable `deviceId` was actively misleading. 
     * Revoke a device
     */
    async revokeDeviceRaw(requestParameters: RevokeDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling revokeDevice().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/auth/devices/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Revokes the device and every refresh token bound to it. Takes effect immediately for refresh and biometric grants; an access token already issued remains valid until it expires, at most fifteen minutes.  Note the parameter is the device record\'s `id` (a UUID), not its `deviceId` (the client-generated string). The two are distinct and naming the path variable `deviceId` was actively misleading. 
     * Revoke a device
     */
    async revokeDevice(requestParameters: RevokeDeviceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokeDeviceRaw(requestParameters, initOverrides);
    }

    /**
     * The second half of a password sign-in. Called after `/v1/auth/token` answers `401 MFA_REQUIRED` with an `mfaToken` in `details`.  Accepts either a TOTP code or a recovery code — the user cannot always tell you which they are holding, and making them choose adds a decision at the worst possible moment. A recovery code is consumed on use.  The challenge token goes in the **body**, not the `Authorization` header: it is not a bearer token for this API, and putting it there invites proxies and client libraries to treat it as one. It carries no roles and no employee id, so even if something did accept it as a session it would grant nothing.  Every failure — wrong code, spent recovery code, account without a second factor — returns the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge which case they are in. 
     * Complete a sign-in with a second factor
     */
    async verifyMfaRaw(requestParameters: VerifyMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenResponse>> {
        if (requestParameters['xTenantCode'] == null) {
            throw new runtime.RequiredError(
                'xTenantCode',
                'Required parameter "xTenantCode" was null or undefined when calling verifyMfa().'
            );
        }

        if (requestParameters['mfaVerifyRequest'] == null) {
            throw new runtime.RequiredError(
                'mfaVerifyRequest',
                'Required parameter "mfaVerifyRequest" was null or undefined when calling verifyMfa().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xTenantCode'] != null) {
            headerParameters['X-Tenant-Code'] = String(requestParameters['xTenantCode']);
        }

        const response = await this.request({
            path: `/v1/auth/mfa/verify`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MfaVerifyRequestToJSON(requestParameters['mfaVerifyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokenResponseFromJSON(jsonValue));
    }

    /**
     * The second half of a password sign-in. Called after `/v1/auth/token` answers `401 MFA_REQUIRED` with an `mfaToken` in `details`.  Accepts either a TOTP code or a recovery code — the user cannot always tell you which they are holding, and making them choose adds a decision at the worst possible moment. A recovery code is consumed on use.  The challenge token goes in the **body**, not the `Authorization` header: it is not a bearer token for this API, and putting it there invites proxies and client libraries to treat it as one. It carries no roles and no employee id, so even if something did accept it as a session it would grant nothing.  Every failure — wrong code, spent recovery code, account without a second factor — returns the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge which case they are in. 
     * Complete a sign-in with a second factor
     */
    async verifyMfa(requestParameters: VerifyMfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenResponse> {
        const response = await this.verifyMfaRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
