/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  DirectoryEntry,
  DirectoryPage,
} from '../models/index';
import {
    DirectoryEntryFromJSON,
    DirectoryEntryToJSON,
    DirectoryPageFromJSON,
    DirectoryPageToJSON,
} from '../models/index';

export interface ListDirectReportsRequest {
    id: string;
}

export interface SearchDirectoryRequest {
    query?: string;
    departmentId?: string;
    locationId?: string;
    cursor?: string;
    limit?: number;
}

/**
 * DirectoryApi - interface
 * 
 * @export
 * @interface DirectoryApiInterface
 */
export interface DirectoryApiInterface {
    /**
     * One level only. The org chart expands a branch at a time rather than fetching an entire subtree, which keeps the payload small for a deep organisation and matches how the screen is actually used. 
     * @summary Direct reports of an employee
     * @param {string} id 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DirectoryApiInterface
     */
    listDirectReportsRaw(requestParameters: ListDirectReportsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DirectoryEntry>>>;

    /**
     * One level only. The org chart expands a branch at a time rather than fetching an entire subtree, which keeps the payload small for a deep organisation and matches how the screen is actually used. 
     * Direct reports of an employee
     */
    listDirectReports(requestParameters: ListDirectReportsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DirectoryEntry>>;

    /**
     * Available to every authenticated employee — finding a colleague\'s extension is not a privileged operation, and gating it would mean most of the workforce could not use the feature they open most often.  What makes that safe is what the response omits. Salary, bank details, identity documents, date of birth and home address are never selected by the query, so there is nothing to leak. Anything more sensitive goes through the profile endpoint, which applies , a data-scope check and field-level permissions.  Results are weighted so a name match outranks an employee-code match, which outranks an email match. Exited and not-yet-joined employees are excluded. 
     * @summary Search the employee directory
     * @param {string} [query] Free text. Tolerates whatever a person types — unbalanced quotes and stray operators do not produce an error.
     * @param {string} [departmentId] 
     * @param {string} [locationId] 
     * @param {string} [cursor] Opaque cursor from a previous response.
     * @param {number} [limit] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DirectoryApiInterface
     */
    searchDirectoryRaw(requestParameters: SearchDirectoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DirectoryPage>>;

    /**
     * Available to every authenticated employee — finding a colleague\'s extension is not a privileged operation, and gating it would mean most of the workforce could not use the feature they open most often.  What makes that safe is what the response omits. Salary, bank details, identity documents, date of birth and home address are never selected by the query, so there is nothing to leak. Anything more sensitive goes through the profile endpoint, which applies , a data-scope check and field-level permissions.  Results are weighted so a name match outranks an employee-code match, which outranks an email match. Exited and not-yet-joined employees are excluded. 
     * Search the employee directory
     */
    searchDirectory(requestParameters: SearchDirectoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DirectoryPage>;

}

/**
 * 
 */
export class DirectoryApi extends runtime.BaseAPI implements DirectoryApiInterface {

    /**
     * One level only. The org chart expands a branch at a time rather than fetching an entire subtree, which keeps the payload small for a deep organisation and matches how the screen is actually used. 
     * Direct reports of an employee
     */
    async listDirectReportsRaw(requestParameters: ListDirectReportsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DirectoryEntry>>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling listDirectReports().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/directory/employees/{id}/reports`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DirectoryEntryFromJSON));
    }

    /**
     * One level only. The org chart expands a branch at a time rather than fetching an entire subtree, which keeps the payload small for a deep organisation and matches how the screen is actually used. 
     * Direct reports of an employee
     */
    async listDirectReports(requestParameters: ListDirectReportsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DirectoryEntry>> {
        const response = await this.listDirectReportsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Available to every authenticated employee — finding a colleague\'s extension is not a privileged operation, and gating it would mean most of the workforce could not use the feature they open most often.  What makes that safe is what the response omits. Salary, bank details, identity documents, date of birth and home address are never selected by the query, so there is nothing to leak. Anything more sensitive goes through the profile endpoint, which applies , a data-scope check and field-level permissions.  Results are weighted so a name match outranks an employee-code match, which outranks an email match. Exited and not-yet-joined employees are excluded. 
     * Search the employee directory
     */
    async searchDirectoryRaw(requestParameters: SearchDirectoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DirectoryPage>> {
        const queryParameters: any = {};

        if (requestParameters['query'] != null) {
            queryParameters['query'] = requestParameters['query'];
        }

        if (requestParameters['departmentId'] != null) {
            queryParameters['departmentId'] = requestParameters['departmentId'];
        }

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/directory/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DirectoryPageFromJSON(jsonValue));
    }

    /**
     * Available to every authenticated employee — finding a colleague\'s extension is not a privileged operation, and gating it would mean most of the workforce could not use the feature they open most often.  What makes that safe is what the response omits. Salary, bank details, identity documents, date of birth and home address are never selected by the query, so there is nothing to leak. Anything more sensitive goes through the profile endpoint, which applies , a data-scope check and field-level permissions.  Results are weighted so a name match outranks an employee-code match, which outranks an email match. Exited and not-yet-joined employees are excluded. 
     * Search the employee directory
     */
    async searchDirectory(requestParameters: SearchDirectoryRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DirectoryPage> {
        const response = await this.searchDirectoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
