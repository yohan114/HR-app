/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiErrorResponse,
  FormSchema,
} from '../models/index';
import {
    ApiErrorResponseFromJSON,
    ApiErrorResponseToJSON,
    FormSchemaFromJSON,
    FormSchemaToJSON,
} from '../models/index';

export interface GetFormSchemaRequest {
    entityType: GetFormSchemaEntityTypeEnum;
    acceptLanguage?: string;
}

/**
 * FormsApi - interface
 * 
 * @export
 * @interface FormsApiInterface
 */
export interface FormsApiInterface {
    /**
     * Describes a form well enough for a client to render it without knowing what the fields are — so a tenant can add a field in the admin console and have it appear on Android and iOS on the next sync. No code change, no app release, no store review.  The schema covers the **whole** form: built-in and tenant-defined fields together, in one ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at the bottom — cannot interleave a tenant\'s field with the built-in one it relates to, and puts a customer\'s mandatory field below the save button.  **Permissions are already applied.** Fields the caller may not see are absent, not flagged; fields they may see but not change arrive with `editable: false`. The client does not filter, and cannot leak what it never received.  Clients cache by `version` and re-render only when it changes. The version is a hash of the definitions, not a timestamp, so two servers behind a load balancer agree. 
     * @summary The form schema for an entity type
     * @param {'employee' | 'company' | 'location' | 'department' | 'designation'} entityType 
     * @param {string} [acceptLanguage] First language tag wins. Labels fall back through region, then English, then the field key.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FormsApiInterface
     */
    getFormSchemaRaw(requestParameters: GetFormSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormSchema>>;

    /**
     * Describes a form well enough for a client to render it without knowing what the fields are — so a tenant can add a field in the admin console and have it appear on Android and iOS on the next sync. No code change, no app release, no store review.  The schema covers the **whole** form: built-in and tenant-defined fields together, in one ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at the bottom — cannot interleave a tenant\'s field with the built-in one it relates to, and puts a customer\'s mandatory field below the save button.  **Permissions are already applied.** Fields the caller may not see are absent, not flagged; fields they may see but not change arrive with `editable: false`. The client does not filter, and cannot leak what it never received.  Clients cache by `version` and re-render only when it changes. The version is a hash of the definitions, not a timestamp, so two servers behind a load balancer agree. 
     * The form schema for an entity type
     */
    getFormSchema(requestParameters: GetFormSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormSchema>;

}

/**
 * 
 */
export class FormsApi extends runtime.BaseAPI implements FormsApiInterface {

    /**
     * Describes a form well enough for a client to render it without knowing what the fields are — so a tenant can add a field in the admin console and have it appear on Android and iOS on the next sync. No code change, no app release, no store review.  The schema covers the **whole** form: built-in and tenant-defined fields together, in one ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at the bottom — cannot interleave a tenant\'s field with the built-in one it relates to, and puts a customer\'s mandatory field below the save button.  **Permissions are already applied.** Fields the caller may not see are absent, not flagged; fields they may see but not change arrive with `editable: false`. The client does not filter, and cannot leak what it never received.  Clients cache by `version` and re-render only when it changes. The version is a hash of the definitions, not a timestamp, so two servers behind a load balancer agree. 
     * The form schema for an entity type
     */
    async getFormSchemaRaw(requestParameters: GetFormSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FormSchema>> {
        if (requestParameters['entityType'] == null) {
            throw new runtime.RequiredError(
                'entityType',
                'Required parameter "entityType" was null or undefined when calling getFormSchema().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['acceptLanguage'] != null) {
            headerParameters['Accept-Language'] = String(requestParameters['acceptLanguage']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/forms/{entityType}`.replace(`{${"entityType"}}`, encodeURIComponent(String(requestParameters['entityType']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FormSchemaFromJSON(jsonValue));
    }

    /**
     * Describes a form well enough for a client to render it without knowing what the fields are — so a tenant can add a field in the admin console and have it appear on Android and iOS on the next sync. No code change, no app release, no store review.  The schema covers the **whole** form: built-in and tenant-defined fields together, in one ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at the bottom — cannot interleave a tenant\'s field with the built-in one it relates to, and puts a customer\'s mandatory field below the save button.  **Permissions are already applied.** Fields the caller may not see are absent, not flagged; fields they may see but not change arrive with `editable: false`. The client does not filter, and cannot leak what it never received.  Clients cache by `version` and re-render only when it changes. The version is a hash of the definitions, not a timestamp, so two servers behind a load balancer agree. 
     * The form schema for an entity type
     */
    async getFormSchema(requestParameters: GetFormSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FormSchema> {
        const response = await this.getFormSchemaRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const GetFormSchemaEntityTypeEnum = {
    Employee: 'employee',
    Company: 'company',
    Location: 'location',
    Department: 'department',
    Designation: 'designation'
} as const;
export type GetFormSchemaEntityTypeEnum = typeof GetFormSchemaEntityTypeEnum[keyof typeof GetFormSchemaEntityTypeEnum];
