/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { DirectoryEntry } from './DirectoryEntry';
import {
    DirectoryEntryFromJSON,
    DirectoryEntryFromJSONTyped,
    DirectoryEntryToJSON,
    DirectoryEntryToJSONTyped,
} from './DirectoryEntry';

/**
 * 
 * @export
 * @interface DirectoryPage
 */
export interface DirectoryPage {
    /**
     * 
     * @type {Array<DirectoryEntry>}
     * @memberof DirectoryPage
     */
    items: Array<DirectoryEntry>;
    /**
     * Pass as `cursor` on the next call. Absent when there are no more results.
     * @type {string}
     * @memberof DirectoryPage
     */
    nextCursor?: string;
    /**
     * 
     * @type {boolean}
     * @memberof DirectoryPage
     */
    hasMore: boolean;
}

/**
 * Check if a given object implements the DirectoryPage interface.
 */
export function instanceOfDirectoryPage(value: object): value is DirectoryPage {
    if (!('items' in value) || value['items'] === undefined) return false;
    if (!('hasMore' in value) || value['hasMore'] === undefined) return false;
    return true;
}

export function DirectoryPageFromJSON(json: any): DirectoryPage {
    return DirectoryPageFromJSONTyped(json, false);
}

export function DirectoryPageFromJSONTyped(json: any, ignoreDiscriminator: boolean): DirectoryPage {
    if (json == null) {
        return json;
    }
    return {
        
        'items': ((json['items'] as Array<any>).map(DirectoryEntryFromJSON)),
        'nextCursor': json['nextCursor'] == null ? undefined : json['nextCursor'],
        'hasMore': json['hasMore'],
    };
}

export function DirectoryPageToJSON(json: any): DirectoryPage {
    return DirectoryPageToJSONTyped(json, false);
}

export function DirectoryPageToJSONTyped(value?: DirectoryPage | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'items': ((value['items'] as Array<any>).map(DirectoryEntryToJSON)),
        'nextCursor': value['nextCursor'],
        'hasMore': value['hasMore'],
    };
}

