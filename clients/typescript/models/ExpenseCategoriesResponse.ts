/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ExpenseCategoryItem } from './ExpenseCategoryItem';
import {
    ExpenseCategoryItemFromJSON,
    ExpenseCategoryItemFromJSONTyped,
    ExpenseCategoryItemToJSON,
    ExpenseCategoryItemToJSONTyped,
} from './ExpenseCategoryItem';

/**
 * 
 * @export
 * @interface ExpenseCategoriesResponse
 */
export interface ExpenseCategoriesResponse {
    /**
     * 
     * @type {Array<ExpenseCategoryItem>}
     * @memberof ExpenseCategoriesResponse
     */
    categories: Array<ExpenseCategoryItem>;
}

/**
 * Check if a given object implements the ExpenseCategoriesResponse interface.
 */
export function instanceOfExpenseCategoriesResponse(value: object): value is ExpenseCategoriesResponse {
    if (!('categories' in value) || value['categories'] === undefined) return false;
    return true;
}

export function ExpenseCategoriesResponseFromJSON(json: any): ExpenseCategoriesResponse {
    return ExpenseCategoriesResponseFromJSONTyped(json, false);
}

export function ExpenseCategoriesResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): ExpenseCategoriesResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'categories': ((json['categories'] as Array<any>).map(ExpenseCategoryItemFromJSON)),
    };
}

export function ExpenseCategoriesResponseToJSON(json: any): ExpenseCategoriesResponse {
    return ExpenseCategoriesResponseToJSONTyped(json, false);
}

export function ExpenseCategoriesResponseToJSONTyped(value?: ExpenseCategoriesResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'categories': ((value['categories'] as Array<any>).map(ExpenseCategoryItemToJSON)),
    };
}

