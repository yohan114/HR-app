/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ExpenseClaimItem } from './ExpenseClaimItem';
import {
    ExpenseClaimItemFromJSON,
    ExpenseClaimItemFromJSONTyped,
    ExpenseClaimItemToJSON,
    ExpenseClaimItemToJSONTyped,
} from './ExpenseClaimItem';

/**
 * 
 * @export
 * @interface ExpenseClaimsResponse
 */
export interface ExpenseClaimsResponse {
    /**
     * 
     * @type {number}
     * @memberof ExpenseClaimsResponse
     */
    totalClaimedAmount: number;
    /**
     * 
     * @type {number}
     * @memberof ExpenseClaimsResponse
     */
    totalApprovedAmount: number;
    /**
     * 
     * @type {number}
     * @memberof ExpenseClaimsResponse
     */
    totalReimbursedAmount: number;
    /**
     * 
     * @type {number}
     * @memberof ExpenseClaimsResponse
     */
    pendingCount: number;
    /**
     * 
     * @type {Array<ExpenseClaimItem>}
     * @memberof ExpenseClaimsResponse
     */
    claims: Array<ExpenseClaimItem>;
}

/**
 * Check if a given object implements the ExpenseClaimsResponse interface.
 */
export function instanceOfExpenseClaimsResponse(value: object): value is ExpenseClaimsResponse {
    if (!('totalClaimedAmount' in value) || value['totalClaimedAmount'] === undefined) return false;
    if (!('totalApprovedAmount' in value) || value['totalApprovedAmount'] === undefined) return false;
    if (!('totalReimbursedAmount' in value) || value['totalReimbursedAmount'] === undefined) return false;
    if (!('pendingCount' in value) || value['pendingCount'] === undefined) return false;
    if (!('claims' in value) || value['claims'] === undefined) return false;
    return true;
}

export function ExpenseClaimsResponseFromJSON(json: any): ExpenseClaimsResponse {
    return ExpenseClaimsResponseFromJSONTyped(json, false);
}

export function ExpenseClaimsResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): ExpenseClaimsResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'totalClaimedAmount': json['totalClaimedAmount'],
        'totalApprovedAmount': json['totalApprovedAmount'],
        'totalReimbursedAmount': json['totalReimbursedAmount'],
        'pendingCount': json['pendingCount'],
        'claims': ((json['claims'] as Array<any>).map(ExpenseClaimItemFromJSON)),
    };
}

export function ExpenseClaimsResponseToJSON(json: any): ExpenseClaimsResponse {
    return ExpenseClaimsResponseToJSONTyped(json, false);
}

export function ExpenseClaimsResponseToJSONTyped(value?: ExpenseClaimsResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'totalClaimedAmount': value['totalClaimedAmount'],
        'totalApprovedAmount': value['totalApprovedAmount'],
        'totalReimbursedAmount': value['totalReimbursedAmount'],
        'pendingCount': value['pendingCount'],
        'claims': ((value['claims'] as Array<any>).map(ExpenseClaimItemToJSON)),
    };
}

