/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { FieldOption } from './FieldOption';
import {
    FieldOptionFromJSON,
    FieldOptionFromJSONTyped,
    FieldOptionToJSON,
    FieldOptionToJSONTyped,
} from './FieldOption';
import type { FieldValidation } from './FieldValidation';
import {
    FieldValidationFromJSON,
    FieldValidationFromJSONTyped,
    FieldValidationToJSON,
    FieldValidationToJSONTyped,
} from './FieldValidation';

/**
 * 
 * @export
 * @interface FormField
 */
export interface FormField {
    /**
     * 
     * @type {string}
     * @memberof FormField
     */
    key: string;
    /**
     * Already localised. Falls back through region, then English, then the field key — never blank, because a form with an unlabelled field is unusable.
     * @type {string}
     * @memberof FormField
     */
    label: string;
    /**
     * 
     * @type {string}
     * @memberof FormField
     */
    type: FormFieldTypeEnum;
    /**
     * 
     * @type {boolean}
     * @memberof FormField
     */
    required?: boolean;
    /**
     * False when the caller may see the value but not change it. Distinct from omitting the
     * field — a read-only salary grade still belongs on the profile, it simply is not the
     * employee's to edit.
     * 
     * @type {boolean}
     * @memberof FormField
     */
    editable?: boolean;
    /**
     * 
     * @type {string}
     * @memberof FormField
     */
    helpText?: string;
    /**
     * 
     * @type {FieldValidation}
     * @memberof FormField
     */
    validation?: FieldValidation;
    /**
     * For DROPDOWN, MULTI_SELECT and RADIO.
     * @type {Array<FieldOption>}
     * @memberof FormField
     */
    options?: Array<FieldOption>;
    /**
     * For REFERENCE fields: which taxonomy supplies the options, e.g. `nationality`. The
     * client already caches these, so the schema names the taxonomy rather than inlining
     * several hundred options.
     * 
     * @type {string}
     * @memberof FormField
     */
    referenceTable?: string;
    /**
     * True for tenant-defined fields. Clients need not care; support does.
     * @type {boolean}
     * @memberof FormField
     */
    custom?: boolean;
}


/**
 * @export
 */
export const FormFieldTypeEnum = {
    Text: 'TEXT',
    MultilineText: 'MULTILINE_TEXT',
    Number: 'NUMBER',
    Date: 'DATE',
    Dropdown: 'DROPDOWN',
    MultiSelect: 'MULTI_SELECT',
    Radio: 'RADIO',
    Checkbox: 'CHECKBOX',
    Attachment: 'ATTACHMENT',
    Employee: 'EMPLOYEE',
    Reference: 'REFERENCE',
    Email: 'EMAIL',
    Phone: 'PHONE'
} as const;
export type FormFieldTypeEnum = typeof FormFieldTypeEnum[keyof typeof FormFieldTypeEnum];


/**
 * Check if a given object implements the FormField interface.
 */
export function instanceOfFormField(value: object): value is FormField {
    if (!('key' in value) || value['key'] === undefined) return false;
    if (!('label' in value) || value['label'] === undefined) return false;
    if (!('type' in value) || value['type'] === undefined) return false;
    return true;
}

export function FormFieldFromJSON(json: any): FormField {
    return FormFieldFromJSONTyped(json, false);
}

export function FormFieldFromJSONTyped(json: any, ignoreDiscriminator: boolean): FormField {
    if (json == null) {
        return json;
    }
    return {
        
        'key': json['key'],
        'label': json['label'],
        'type': json['type'],
        'required': json['required'] == null ? undefined : json['required'],
        'editable': json['editable'] == null ? undefined : json['editable'],
        'helpText': json['helpText'] == null ? undefined : json['helpText'],
        'validation': json['validation'] == null ? undefined : FieldValidationFromJSON(json['validation']),
        'options': json['options'] == null ? undefined : ((json['options'] as Array<any>).map(FieldOptionFromJSON)),
        'referenceTable': json['referenceTable'] == null ? undefined : json['referenceTable'],
        'custom': json['custom'] == null ? undefined : json['custom'],
    };
}

export function FormFieldToJSON(json: any): FormField {
    return FormFieldToJSONTyped(json, false);
}

export function FormFieldToJSONTyped(value?: FormField | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'key': value['key'],
        'label': value['label'],
        'type': value['type'],
        'required': value['required'],
        'editable': value['editable'],
        'helpText': value['helpText'],
        'validation': FieldValidationToJSON(value['validation']),
        'options': value['options'] == null ? undefined : ((value['options'] as Array<any>).map(FieldOptionToJSON)),
        'referenceTable': value['referenceTable'],
        'custom': value['custom'],
    };
}

