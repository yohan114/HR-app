/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { FormField } from './FormField';
import {
    FormFieldFromJSON,
    FormFieldFromJSONTyped,
    FormFieldToJSON,
    FormFieldToJSONTyped,
} from './FormField';

/**
 * 
 * @export
 * @interface FormSection
 */
export interface FormSection {
    /**
     * 
     * @type {string}
     * @memberof FormSection
     */
    key: string;
    /**
     * 
     * @type {string}
     * @memberof FormSection
     */
    label: string;
    /**
     * 
     * @type {Array<FormField>}
     * @memberof FormSection
     */
    fields: Array<FormField>;
}

/**
 * Check if a given object implements the FormSection interface.
 */
export function instanceOfFormSection(value: object): value is FormSection {
    if (!('key' in value) || value['key'] === undefined) return false;
    if (!('label' in value) || value['label'] === undefined) return false;
    if (!('fields' in value) || value['fields'] === undefined) return false;
    return true;
}

export function FormSectionFromJSON(json: any): FormSection {
    return FormSectionFromJSONTyped(json, false);
}

export function FormSectionFromJSONTyped(json: any, ignoreDiscriminator: boolean): FormSection {
    if (json == null) {
        return json;
    }
    return {
        
        'key': json['key'],
        'label': json['label'],
        'fields': ((json['fields'] as Array<any>).map(FormFieldFromJSON)),
    };
}

export function FormSectionToJSON(json: any): FormSection {
    return FormSectionToJSONTyped(json, false);
}

export function FormSectionToJSONTyped(value?: FormSection | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'key': value['key'],
        'label': value['label'],
        'fields': ((value['fields'] as Array<any>).map(FormFieldToJSON)),
    };
}

