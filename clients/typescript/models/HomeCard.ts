/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface HomeCard
 */
export interface HomeCard {
    /**
     * 
     * @type {string}
     * @memberof HomeCard
     */
    id: string;
    /**
     * Card kind identifier. Open string rather than closed enum so new cards do not crash older clients.
     * Known values: PENDING_APPROVALS, MILESTONES, ANNOUNCEMENTS, QUICK_ACTIONS, EXPIRING_DOCUMENTS.
     * 
     * @type {string}
     * @memberof HomeCard
     */
    type: string;
    /**
     * 
     * @type {string}
     * @memberof HomeCard
     */
    title: string;
    /**
     * Lower number displays first.
     * @type {number}
     * @memberof HomeCard
     */
    priority: number;
    /**
     * Card-specific data structure.
     * @type {{ [key: string]: any; }}
     * @memberof HomeCard
     */
    payload: { [key: string]: any; };
}

/**
 * Check if a given object implements the HomeCard interface.
 */
export function instanceOfHomeCard(value: object): value is HomeCard {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('type' in value) || value['type'] === undefined) return false;
    if (!('title' in value) || value['title'] === undefined) return false;
    if (!('priority' in value) || value['priority'] === undefined) return false;
    if (!('payload' in value) || value['payload'] === undefined) return false;
    return true;
}

export function HomeCardFromJSON(json: any): HomeCard {
    return HomeCardFromJSONTyped(json, false);
}

export function HomeCardFromJSONTyped(json: any, ignoreDiscriminator: boolean): HomeCard {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'type': json['type'],
        'title': json['title'],
        'priority': json['priority'],
        'payload': json['payload'],
    };
}

export function HomeCardToJSON(json: any): HomeCard {
    return HomeCardToJSONTyped(json, false);
}

export function HomeCardToJSONTyped(value?: HomeCard | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'type': value['type'],
        'title': value['title'],
        'priority': value['priority'],
        'payload': value['payload'],
    };
}

