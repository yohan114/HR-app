/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface MfaEnrolment
 */
export interface MfaEnrolment {
    /**
     * Base32, 160 bits. Show it as text beneath the QR code — a user whose camera will not
     * focus needs to be able to type it in.
     * 
     * @type {string}
     * @memberof MfaEnrolment
     */
    secret: string;
    /**
     * `otpauth://totp/...` — render as a QR code. Carries the issuer twice, as a label prefix
     * and as a parameter, because authenticator apps disagree about which they read and an
     * unlabelled entry in a list of thirty accounts is unidentifiable.
     * 
     * @type {string}
     * @memberof MfaEnrolment
     */
    provisioningUri: string;
}

/**
 * Check if a given object implements the MfaEnrolment interface.
 */
export function instanceOfMfaEnrolment(value: object): value is MfaEnrolment {
    if (!('secret' in value) || value['secret'] === undefined) return false;
    if (!('provisioningUri' in value) || value['provisioningUri'] === undefined) return false;
    return true;
}

export function MfaEnrolmentFromJSON(json: any): MfaEnrolment {
    return MfaEnrolmentFromJSONTyped(json, false);
}

export function MfaEnrolmentFromJSONTyped(json: any, ignoreDiscriminator: boolean): MfaEnrolment {
    if (json == null) {
        return json;
    }
    return {
        
        'secret': json['secret'],
        'provisioningUri': json['provisioningUri'],
    };
}

export function MfaEnrolmentToJSON(json: any): MfaEnrolment {
    return MfaEnrolmentToJSONTyped(json, false);
}

export function MfaEnrolmentToJSONTyped(value?: MfaEnrolment | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'secret': value['secret'],
        'provisioningUri': value['provisioningUri'],
    };
}

