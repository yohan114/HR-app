/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface QuietHoursSettings
 */
export interface QuietHoursSettings {
    /**
     * Local wall-clock time. A `startAt` after `endAt` is a window crossing midnight.
     * @type {string}
     * @memberof QuietHoursSettings
     */
    startAt: string;
    /**
     * 
     * @type {string}
     * @memberof QuietHoursSettings
     */
    endAt: string;
    /**
     * IANA zone name, not an offset. An offset is wrong twice a year wherever daylight saving
     * applies, and the symptom is notifications arriving an hour into the night for months.
     * Rejected with `VALIDATION_FAILED` if the server cannot resolve it.
     * 
     * @type {string}
     * @memberof QuietHoursSettings
     */
    timezone: string;
    /**
     * Weekdays the window applies to, 1 = Monday. Empty means every day. For a window
     * crossing midnight the day is the one the window *opened* on, so `[5]` with 22:00–07:00
     * covers Friday night into Saturday morning.
     * 
     * @type {Array<number>}
     * @memberof QuietHoursSettings
     */
    days?: Array<number>;
    /**
     * 
     * @type {boolean}
     * @memberof QuietHoursSettings
     */
    enabled?: boolean;
}

/**
 * Check if a given object implements the QuietHoursSettings interface.
 */
export function instanceOfQuietHoursSettings(value: object): value is QuietHoursSettings {
    if (!('startAt' in value) || value['startAt'] === undefined) return false;
    if (!('endAt' in value) || value['endAt'] === undefined) return false;
    if (!('timezone' in value) || value['timezone'] === undefined) return false;
    return true;
}

export function QuietHoursSettingsFromJSON(json: any): QuietHoursSettings {
    return QuietHoursSettingsFromJSONTyped(json, false);
}

export function QuietHoursSettingsFromJSONTyped(json: any, ignoreDiscriminator: boolean): QuietHoursSettings {
    if (json == null) {
        return json;
    }
    return {
        
        'startAt': json['startAt'],
        'endAt': json['endAt'],
        'timezone': json['timezone'],
        'days': json['days'] == null ? undefined : json['days'],
        'enabled': json['enabled'] == null ? undefined : json['enabled'],
    };
}

export function QuietHoursSettingsToJSON(json: any): QuietHoursSettings {
    return QuietHoursSettingsToJSONTyped(json, false);
}

export function QuietHoursSettingsToJSONTyped(value?: QuietHoursSettings | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'startAt': value['startAt'],
        'endAt': value['endAt'],
        'timezone': value['timezone'],
        'days': value['days'],
        'enabled': value['enabled'],
    };
}

