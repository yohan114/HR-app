/* tslint:disable */
/* eslint-disable */
/**
 * HR Platform API
 * Multi-tenant HR platform API.  **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript clients are all generated from it, and a breaking change to a published version fails CI. See `docs/03-architecture.md` §9.  ## Tenancy  Every request is resolved to exactly one tenant. Precedence, highest first:  1. The `tenant_id` claim in the bearer token — authoritative once authenticated. 2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints. 3. The request subdomain.  Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.  ## Errors  Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`, never from `message` — the server cannot reliably know the caller\'s locale, and we ship in six languages.  ## Pagination  Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it re-scans skipped rows and silently drops and duplicates records on concurrently-written tables such as attendance and audit.  ## Idempotency  Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints the mobile outbox can replay. A repeat of a key already processed returns the original response rather than applying the operation twice. 
 *
 * The version of the OpenAPI document: 1.0.0-alpha.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface SalaryHistoryItem
 */
export interface SalaryHistoryItem {
    /**
     * 
     * @type {string}
     * @memberof SalaryHistoryItem
     */
    id: string;
    /**
     * 
     * @type {string}
     * @memberof SalaryHistoryItem
     */
    employeeId: string;
    /**
     * 
     * @type {string}
     * @memberof SalaryHistoryItem
     */
    careerMovementId?: string;
    /**
     * 
     * @type {Date}
     * @memberof SalaryHistoryItem
     */
    effectiveFrom: Date;
    /**
     * 
     * @type {Date}
     * @memberof SalaryHistoryItem
     */
    effectiveTo?: Date;
    /**
     * 
     * @type {number}
     * @memberof SalaryHistoryItem
     */
    baseSalary: number;
    /**
     * 
     * @type {string}
     * @memberof SalaryHistoryItem
     */
    currency: string;
    /**
     * 
     * @type {number}
     * @memberof SalaryHistoryItem
     */
    changePercentage?: number;
    /**
     * 
     * @type {string}
     * @memberof SalaryHistoryItem
     */
    revisionReason: string;
    /**
     * 
     * @type {boolean}
     * @memberof SalaryHistoryItem
     */
    isCurrent: boolean;
}

/**
 * Check if a given object implements the SalaryHistoryItem interface.
 */
export function instanceOfSalaryHistoryItem(value: object): value is SalaryHistoryItem {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('employeeId' in value) || value['employeeId'] === undefined) return false;
    if (!('effectiveFrom' in value) || value['effectiveFrom'] === undefined) return false;
    if (!('baseSalary' in value) || value['baseSalary'] === undefined) return false;
    if (!('currency' in value) || value['currency'] === undefined) return false;
    if (!('revisionReason' in value) || value['revisionReason'] === undefined) return false;
    if (!('isCurrent' in value) || value['isCurrent'] === undefined) return false;
    return true;
}

export function SalaryHistoryItemFromJSON(json: any): SalaryHistoryItem {
    return SalaryHistoryItemFromJSONTyped(json, false);
}

export function SalaryHistoryItemFromJSONTyped(json: any, ignoreDiscriminator: boolean): SalaryHistoryItem {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'employeeId': json['employeeId'],
        'careerMovementId': json['careerMovementId'] == null ? undefined : json['careerMovementId'],
        'effectiveFrom': (new Date(json['effectiveFrom'])),
        'effectiveTo': json['effectiveTo'] == null ? undefined : (new Date(json['effectiveTo'])),
        'baseSalary': json['baseSalary'],
        'currency': json['currency'],
        'changePercentage': json['changePercentage'] == null ? undefined : json['changePercentage'],
        'revisionReason': json['revisionReason'],
        'isCurrent': json['isCurrent'],
    };
}

export function SalaryHistoryItemToJSON(json: any): SalaryHistoryItem {
    return SalaryHistoryItemToJSONTyped(json, false);
}

export function SalaryHistoryItemToJSONTyped(value?: SalaryHistoryItem | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'employeeId': value['employeeId'],
        'careerMovementId': value['careerMovementId'],
        'effectiveFrom': ((value['effectiveFrom']).toISOString().substring(0,10)),
        'effectiveTo': value['effectiveTo'] == null ? undefined : ((value['effectiveTo']).toISOString().substring(0,10)),
        'baseSalary': value['baseSalary'],
        'currency': value['currency'],
        'changePercentage': value['changePercentage'],
        'revisionReason': value['revisionReason'],
        'isCurrent': value['isCurrent'],
    };
}

