openapi: 3.1.0

info:
  title: HR Platform API
  version: 1.0.0-alpha.1
  description: |
    Multi-tenant HR platform API.

    **This specification is the contract.** Server interfaces and the Kotlin, Swift and TypeScript
    clients are all generated from it, and a breaking change to a published version fails CI.
    See `docs/03-architecture.md` §9.

    ## Tenancy

    Every request is resolved to exactly one tenant. Precedence, highest first:

    1. The `tenant_id` claim in the bearer token — authoritative once authenticated.
    2. The `X-Tenant-Code` header — for the unauthenticated login and org-resolve endpoints.
    3. The request subdomain.

    Supplying a token and a conflicting `X-Tenant-Code` is rejected with `TENANT_MISMATCH`.

    ## Errors

    Every non-2xx response uses one envelope. Clients localise from the machine-readable `code`,
    never from `message` — the server cannot reliably know the caller's locale, and we ship in
    six languages.

    ## Pagination

    Cursor-based everywhere. Offset pagination is not offered on tenant-scoped collections: it
    re-scans skipped rows and silently drops and duplicates records on concurrently-written
    tables such as attendance and audit.

    ## Idempotency

    Every unsafe method accepts an `Idempotency-Key` header and it is **required** for endpoints
    the mobile outbox can replay. A repeat of a key already processed returns the original
    response rather than applying the operation twice.
  contact:
    name: HR Platform Engineering
  license:
    name: Proprietary
    # OpenAPI 3.1 expects an SPDX expression here. There is no SPDX code for "all rights
    # reserved", so the spec's own LicenseRef- escape hatch is the correct form.
    identifier: LicenseRef-Proprietary

servers:
  - url: https://api.hrapp.io
    description: Production
  - url: https://staging-api.hrapp.io
    description: Staging
  - url: http://localhost:8080
    description: Local

tags:
  - name: Authentication
    description: Tenant resolution, sign-in, token lifecycle, device registration
  - name: Tenancy
    description: Customer organisations, isolation tiers, and module enablement
  - name: Roles
    description: Role-based access control and system permissions catalogue
  - name: Users
    description: User account administration, lifecycle, password reset, and device management
  # The Sync tag and GET /v1/sync are deliberately absent until the endpoint exists (P0-BE-33).
  # A documented-but-unimplemented operation ships a generated client method that 404s, which is
  # exactly the drift ApiContractTest prevents. It returns here with its implementation.
  - name: Directory
    description: Employee directory and reporting hierarchy
  - name: Employees
    description: Employee profiles, with per-field permissions applied server-side
  - name: Reference
    description: Reference taxonomies — configurable lookup lists
  - name: Forms
    description: Server-rendered form schemas — how a tenant-defined field reaches mobile without an app release
  - name: Me
    description: The authenticated user's own context
  - name: Mobile
    description: Mobile home composite and client-optimised aggregation endpoints
  - name: Attendance
    description: Attendance check-in/out, geofence audit, and shift status
  - name: Approvals
    description: Manager unified approval inbox for leave, attendance regularisation, and workflows
  - name: Expenses
    description: Multi-line expense claims, receipt OCR extraction, and policy compliance
  - name: Benefits
    description: Flexible benefits, health insurance coverage, policy catalogues, and claim reimbursements
  - name: Onboarding
    description: Employee onboarding tracks, milestones, and multi-role action checklists
  - name: Offboarding
    description: Resignation notices, cross-departmental clearance workflows, and exit interviews
  - name: Documents
    description: Enterprise document vault, company policies, multi-versioning, and template letter requests
  - name: Signatures
    description: Legally compliant digital e-signatures, signer progression, and SHA-256 audit trails
  - name: Training
    description: Enterprise learning management, training catalogue, batch schedules, enrollments, attendance, and digital certificates
  - name: Timesheets
    description: Enterprise project timesheets, activity billing, weekly matrix logging, and attendance reconciliation
  - name: Loans
    description: Employee loan types, eligibility simulation, loan applications, and settlement calculations
  - name: Lifecycle
    description: Career movements, promotions, transfers, salary increments, and timeline audit
  - name: Grievance
    description: Formal employee grievances, submission channels, dispute grounds, and appeals
  - name: Disciplinary
    description: Disciplinary incident tracking, warnings, corrective actions, hearings, and appeals
  - name: Performance
    description: Objectives and key results (OKRs), competency evaluations, multi-rater appraisals, and feedback
  - name: Recruitment
    description: Requisitions, vacancy listings, applicant tracking, interview scorecards, and job offers
  - name: Payroll
    description: Payroll cycles, pay groups, gross-to-net computation, run variance, and bank advice export
  - name: Leave
    description: Statutory and company leave policies, balance tracking, applications, and ledger movements
  - name: Dashboard
    description: Role-adaptive composite dashboard with widgets and quick actions




security:
  - bearerAuth: []

paths:
  /v1/auth/resolve-tenant:
    post:
      tags: [Authentication]
      summary: Resolve an organisation from a work email or org code
      description: |
        The first step of sign-in. Deliberately does **not** reveal whether a user account exists —
        it resolves the organisation only, so this endpoint cannot be used to enumerate accounts.

        This replaces asking the user to type a "service URL", which is a common source of
        support load in comparable products.
      operationId: resolveTenant
      security: []
      parameters:
        # Required: the tenant is resolved by the server's request filter from this header (or
        # from the subdomain). Send the same value as `orgCode` in the body.
        - $ref: '#/components/parameters/TenantCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveTenantRequest'
      responses:
        '200':
          description: Organisation resolved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResolveTenantResponse'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/token:
    post:
      tags: [Authentication]
      summary: Sign in with a password
      description: |
        Exchanges credentials for an access and refresh token pair, and registers the calling
        device in the same round trip so the client can offer biometric enrolment immediately.

        Failures are deliberately uniform: a wrong password and an unknown username both return
        `INVALID_CREDENTIALS`, so this endpoint cannot be used to enumerate accounts. Repeated
        failures lock the account per the tenant's password policy.
      operationId: issueToken
      security: []
      parameters:
        - $ref: '#/components/parameters/TenantCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordGrantRequest'
      responses:
        '200':
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: |
            Invalid credentials, or MFA is required. `MFA_REQUIRED` carries an `mfaToken` in
            `details` which must be presented to `/v1/auth/mfa/verify`.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/token/refresh:
    post:
      tags: [Authentication]
      summary: Exchange a refresh token for a new access token
      description: |
        Refresh tokens are single-use and rotate on every exchange.

        If a token that has already been used is presented again, the most likely explanation is
        theft, so the entire token family is revoked and every device sharing that login session
        must re-authenticate. The response is `TOKEN_REUSE_DETECTED`.

        Requires `X-Tenant-Code`: the token lookup is itself tenant-scoped by row-level security,
        so the tenant must be known before the token can be found.
      operationId: refreshToken
      security: []
      parameters:
        - $ref: '#/components/parameters/TenantCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '200':
          description: Rotated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/auth/token/biometric:
    post:
      tags: [Authentication]
      summary: Exchange a device-sealed refresh token after a biometric assertion
      description: |
        The core of the "no password after enrolment" experience.

        The refresh token is sealed at enrolment in the device's Android Keystore or iOS Secure
        Enclave under a key that requires user authentication. The OS releases it only after a
        successful fingerprint or face match, and invalidates the key entirely if the device's
        enrolled biometrics change.

        The server therefore trusts the *presentation* of the sealed token as evidence that the
        biometric check passed — it never receives or stores biometric data itself.

        Two conditions beyond a normal refresh: the token must have been issued to the device
        presenting it, and that device must have completed biometric enrolment. Together these
        stop a token exfiltrated from one device being replayed from another.
      operationId: biometricToken
      security: []
      parameters:
        - $ref: '#/components/parameters/TenantCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BiometricGrantRequest'
      responses:
        '200':
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Device revoked, or re-authentication with a password is required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'

  /v1/auth/devices:
    get:
      tags: [Authentication]
      summary: List the current user's registered devices
      description: |
        Powers the "where am I signed in?" screen. The device making the request is flagged with
        `current: true` so the client can avoid offering the user a way to revoke themselves
        without warning.
      operationId: listDevices
      responses:
        '200':
          description: Devices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Device'
    post:
      tags: [Authentication]
      summary: Register the current device
      description: |
        Registers or updates a device for the authenticated user, including its push token.

        Called on sign-in and again whenever the push token rotates. Idempotent on
        `(user, deviceId)`: re-registering an existing device updates it rather than creating a
        duplicate.
      operationId: registerDevice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDeviceRequest'
      responses:
        '201':
          description: Registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'

  /v1/auth/devices/{id}:
    delete:
      tags: [Authentication]
      summary: Revoke a device
      description: |
        Revokes the device and every refresh token bound to it. Takes effect immediately for
        refresh and biometric grants; an access token already issued remains valid until it
        expires, at most fifteen minutes.

        Note the parameter is the device record's `id` (a UUID), not its `deviceId` (the
        client-generated string). The two are distinct and naming the path variable `deviceId`
        was actively misleading.
      operationId: revokeDevice
      parameters:
        - name: id
          in: path
          required: true
          description: The device record's UUID, as returned by `GET /v1/auth/devices`.
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Revoked
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/auth/logout:
    post:
      tags: [Authentication]
      summary: Sign out
      description: |
        Revokes the token family the presented refresh token belongs to. Other devices stay signed
        in — signing a user out everywhere is a separate, deliberate action.

        The body is optional. Without it the caller's entire set of sessions is revoked, which is
        the correct fallback when a client has lost track of its refresh token.

        Never fails: sign-out is idempotent and a client must always be able to end its session.
      operationId: logout
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
      responses:
        '204':
          description: Signed out

  /v1/auth/mfa/verify:
    post:
      tags: [Authentication]
      summary: Complete a sign-in with a second factor
      description: |
        The second half of a password sign-in. Called after `/v1/auth/token` answers
        `401 MFA_REQUIRED` with an `mfaToken` in `details`.

        Accepts either a TOTP code or a recovery code — the user cannot always tell you which they
        are holding, and making them choose adds a decision at the worst possible moment. A
        recovery code is consumed on use.

        The challenge token goes in the **body**, not the `Authorization` header: it is not a
        bearer token for this API, and putting it there invites proxies and client libraries to
        treat it as one. It carries no roles and no employee id, so even if something did accept it
        as a session it would grant nothing.

        Every failure — wrong code, spent recovery code, account without a second factor — returns
        the same `MFA_INVALID_CODE`. Distinguishing them would tell whoever holds the challenge
        which case they are in.
      operationId: verifyMfa
      security: []
      parameters:
        - $ref: '#/components/parameters/TenantCode'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaVerifyRequest'
      responses:
        '200':
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/mfa:
    get:
      tags: [Authentication]
      summary: Whether a second factor is enrolled
      description: |
        Drives the security settings screen: whether to offer enrolment, resume a half-finished
        one, or warn that recovery codes are running low.
      operationId: getMfaStatus
      responses:
        '200':
          description: Current state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MfaStatus'

  /v1/auth/mfa/enrol:
    post:
      tags: [Authentication]
      summary: Begin enrolment — returns a secret and a QR payload
      description: |
        Issues a TOTP secret and the `otpauth://` URI an authenticator app reads from a QR code.
        The secret is stored immediately but **MFA is not switched on** until
        `/v1/auth/mfa/enrol/confirm` succeeds.

        Two steps rather than one deliberately: a user who mis-scans, or scans into an app they
        then delete, would otherwise lock themselves out of their own account with no route back
        except an administrator — which is a support call and a social-engineering path straight
        past the factor. Calling this again replaces the pending secret, which is what someone does
        after scanning into the wrong app.
      operationId: beginMfaEnrolment
      responses:
        '200':
          description: Enrolment started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MfaEnrolment'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /v1/auth/mfa/enrol/confirm:
    post:
      tags: [Authentication]
      summary: Confirm enrolment with a code from the authenticator
      description: |
        Proves the app and the server agree before anything depends on it, then switches MFA on.

        **Returns the recovery codes, and this is the only time they exist in plaintext.** They are
        stored as hashes, so neither the server nor a database backup can produce them again. A
        client that does not show them here has lost them for that user.
      operationId: confirmMfaEnrolment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaCodeRequest'
      responses:
        '200':
          description: Enrolled — show these codes once
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecoveryCodesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/mfa/disable:
    post:
      tags: [Authentication]
      summary: Turn the second factor off
      description: |
        Requires a current code — possession, not just a live session. Without that, anyone who
        borrowed an unlocked laptop could switch the factor off from a settings screen, which makes
        having it decorative.
      operationId: disableMfa
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaCodeRequest'
      responses:
        '204':
          description: Disabled
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/mfa/recovery-codes:
    post:
      tags: [Authentication]
      summary: Issue a fresh set of recovery codes
      description: |
        Invalidates every existing code. Requires a current code, for the same reason as disabling.
      operationId: regenerateRecoveryCodes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaCodeRequest'
      responses:
        '200':
          description: New codes — show these once
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecoveryCodesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/.well-known/jwks.json:
    get:
      tags: [Authentication]
      summary: Public JSON Web Key Set
      description: |
        The public half of the key used to sign access tokens, so that other services, an API
        gateway or a third-party integration can verify our tokens without holding any credential
        of their own.

        The application itself does not call this endpoint — it verifies with the in-process
        public key rather than issuing an HTTP request to itself.

        During a key rotation window this returns both the outgoing and incoming keys, so tokens
        signed with either continue to verify.
      operationId: getJwks
      security: []
      responses:
        '200':
          description: JWK set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JwkSet'

  /v1/directory/search:
    get:
      tags: [Directory]
      summary: Search the employee directory
      description: |
        Available to every authenticated employee — finding a colleague's extension is not a
        privileged operation, and gating it would mean most of the workforce could not use the
        feature they open most often.

        What makes that safe is what the response omits. Salary, bank details, identity documents,
        date of birth and home address are never selected by the query, so there is nothing to
        leak. Anything more sensitive goes through the profile endpoint, which applies
        , a data-scope check and field-level permissions.

        Results are weighted so a name match outranks an employee-code match, which outranks an
        email match. Exited and not-yet-joined employees are excluded.
      operationId: searchDirectory
      parameters:
        - name: query
          in: query
          description: Free text. Tolerates whatever a person types — unbalanced quotes and stray operators do not produce an error.
          schema: { type: string }
        - name: departmentId
          in: query
          schema: { type: string, format: uuid }
        - name: locationId
          in: query
          schema: { type: string, format: uuid }
        - name: cursor
          in: query
          description: Opaque cursor from a previous response.
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      responses:
        '200':
          description: Matching colleagues
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DirectoryPage'

  /v1/directory/employees/{id}/reports:
    get:
      tags: [Directory]
      summary: Direct reports of an employee
      description: |
        One level only. The org chart expands a branch at a time rather than fetching an entire
        subtree, which keeps the payload small for a deep organisation and matches how the screen
        is actually used.
      operationId: listDirectReports
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Direct reports
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DirectoryEntry'

  /v1/reference:
    get:
      tags: [Reference]
      summary: Several reference taxonomies in one call
      description: |
        The mobile clients sync these as a unit and cache them indefinitely — they change perhaps
        twice a year. Serving them individually would mean four round trips to render one form,
        which is the chattiness that makes an app feel slow regardless of how fast each call is.
      operationId: listReferenceData
      parameters:
        - name: tables
          in: query
          description: Kebab-case taxonomy names, e.g. `blood-group`. Omit to fetch all of them.
          schema:
            type: array
            items: { type: string }
          style: form
          explode: false
      responses:
        '200':
          description: Taxonomy name to its items
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: array
                  items:
                    $ref: '#/components/schemas/ReferenceItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/reference/{table}:
    get:
      tags: [Reference]
      summary: One reference taxonomy
      description: Kebab-case taxonomy name, e.g. `employee-category`.
      operationId: listReferenceTable
      parameters:
        - name: table
          in: path
          required: true
          schema: { type: string }
        - name: includeInactive
          in: query
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ReferenceItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/forms/{entityType}:
    get:
      tags: [Forms]
      summary: The form schema for an entity type
      description: |
        Describes a form well enough for a client to render it without knowing what the fields
        are — so a tenant can add a field in the admin console and have it appear on Android and
        iOS on the next sync. No code change, no app release, no store review.

        The schema covers the **whole** form: built-in and tenant-defined fields together, in one
        ordered list of sections. The alternative — a hardcoded form with a custom-fields lump at
        the bottom — cannot interleave a tenant's field with the built-in one it relates to, and
        puts a customer's mandatory field below the save button.

        **Permissions are already applied.** Fields the caller may not see are absent, not
        flagged; fields they may see but not change arrive with `editable: false`. The client does
        not filter, and cannot leak what it never received.

        Clients cache by `version` and re-render only when it changes. The version is a hash of
        the definitions, not a timestamp, so two servers behind a load balancer agree.
      operationId: getFormSchema
      parameters:
        - name: entityType
          in: path
          required: true
          schema:
            type: string
            enum: [employee, company, location, department, designation]
        - name: Accept-Language
          in: header
          required: false
          description: First language tag wins. Labels fall back through region, then English, then the field key.
          schema: { type: string }
      responses:
        '200':
          description: Form schema
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FormSchema'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/employees/me:
    get:
      tags: [Employees]
      summary: The caller's own employee profile
      description: |
        A separate path from `/v1/employees/{id}` rather than making the client substitute its own
        id: the app opens this on launch, before it necessarily knows the employee id.

        Returns `404 NO_EMPLOYEE_RECORD` for a user account that is not linked to an employee —
        a platform operator or an integration credential.
      operationId: getOwnEmployeeProfile
      responses:
        '200':
          description: Profile, filtered to the fields the caller may see
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeProfile'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/employees/{id}:
    get:
      tags: [Employees]
      summary: An employee profile
      description: |
        Two independent checks decide what comes back.

        **Whether you see the record at all.** Your own record always; anyone's with
        `employee.view.all` or `employee.manage`; your reporting line — at any depth — with
        `employee.view`. Otherwise `404`, deliberately not `403`: a `403` confirms the record
        exists, which turns this endpoint into an oracle for enumerating employee ids.

        **Which of its fields.** Applied server-side. A field you may not see is **absent from the
        payload**, not null — a null would tell you the field exists and you are not allowed it,
        and would have clients rendering disabled inputs for values they should not know about.
        A masked field arrives redacted, and the true value never leaves the server.

        Sensitive fields — date of birth, home address, personal email, salary grade — are hidden
        by default and require an explicit grant, **including** for callers who hold
        `employee.manage`. Maintaining records and handling identity documents are separable
        duties. Your own sensitive fields are always readable to you.
      operationId: getEmployeeProfile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Profile, filtered to the fields the caller may see
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeProfile'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags: [Employees]
      summary: Update an employee profile
      description: |
        PATCH rather than PUT because the caller may not be able to read every field. A PUT means
        "here is the whole record", which someone who cannot see the sensitive half is not in a
        position to send — they would have to echo back fields they never received, and the ones
        they omitted would be cleared.

        A field the caller may not write is rejected with `403 FIELD_NOT_WRITABLE`, never silently
        dropped: a save that appears to succeed and quietly discards a change is worse than a
        refusal. Nothing is applied unless everything validates.

        Writing is never the default. Without `employee.manage` you may change only your own
        contact details, preferred name and photo — not your name, date of birth or join date,
        which appear on statutory filings and change by request with evidence.

        Send `If-Match` with the `version` you last read to avoid overwriting a concurrent edit.
      operationId: updateEmployeeProfile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: If-Match
          in: header
          required: false
          description: |
            The `version` from the profile you loaded, as a weak ETag. Omitted, the update is
            last-write-wins — two HR officers editing the same profile is common enough that
            omitting it loses one of them silently.
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmployeeUpdate'
      responses:
        '200':
          description: The updated profile, filtered as on read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /v1/employees/{id}/form:
    get:
      tags: [Employees]
      summary: The edit form for one employee, with permissions applied
      description: |
        The same schema as `/v1/forms/employee`, narrowed to this caller and this record: fields
        they may not see are absent, and fields they may see but not change arrive with
        `editable: false`.

        Built from the same permission context as the payload and the update path, so what the
        client is offered and what the server will accept cannot drift apart. Deriving the form
        separately is how you end up with an input that saves nothing.
      operationId: getEmployeeEditForm
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: Accept-Language
          in: header
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Form schema for this caller and record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FormSchema'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/me:
    get:
      tags: [Me]
      summary: The authenticated user's identity, permissions and enabled modules
      description: |
        Called on every cold start. Drives the navigation shell, so it returns enabled modules and
        effective permissions together — the client should never have to assemble that from
        several calls before it can render.
      operationId: getMe
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'

  /v1/me/notification-settings:
    get:
      tags: [Me]
      summary: The authenticated user's notification preferences and quiet hours
      description: |
        Only settings that differ from the default are stored, so an empty `preferences` list means
        the user has expressed no opinion rather than that everything is off. Clients render the
        defaults for anything absent: in-app, push and email on, SMS off.
      operationId: getNotificationSettings
      responses:
        '200':
          description: Current settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSettings'
    put:
      tags: [Me]
      summary: Replace the authenticated user's notification settings
      description: |
        A full replace, not a patch. The settings screen holds the whole set on screen, so sending
        only what changed means a switch flicked twice has to be tracked as unchanged — and a
        client that gets that wrong silently drops an edit.

        The entire request is validated before anything is written, so a bad entry in a long list
        cannot leave half of it saved.
      operationId: replaceNotificationSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationSettings'
      responses:
        '200':
          description: The settings as stored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSettings'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/dashboard:
    get:
      tags: [Dashboard]
      summary: Role-adaptive composite dashboard
      description: |
        Returns aggregated KPIs, widgets, and quick actions dynamically tailored to the authenticated
        user's roles and permissions. Cached with short TTL for maximum responsiveness.
      operationId: getDashboard
      responses:
        '200':
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/mobile/home:
    get:
      tags: [Mobile]
      summary: Mobile home screen composite
      description: |
        Single-trip composite endpoint delivering role-based dashboard cards, pending approval counts
        with counted entity identities for offline outbox reconciliation, company announcements,
        and milestone reminders.
      operationId: getMobileHome
      responses:
        '200':
          description: Home composite payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MobileHomeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/me/today:
    get:
      tags: [Attendance]
      summary: Today's attendance status and shift rules for authenticated employee
      description: |
        Returns the caller's active shift schedule for today, morning check-in grace limits,
        current punch status, punch timeline, and branch geofence boundary coordinates.
      operationId: getMyTodayAttendance
      responses:
        '200':
          description: Today's attendance status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TodayAttendanceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/punches:
    post:
      tags: [Attendance]
      summary: Ingest a clock punch from mobile or biometric source
      description: |
        Records a clock-in, clock-out, or break punch. Accepts single-use Idempotency-Key header
        for reliable offline outbox retry. Evaluates branch geofence radius and flags mock GPS providers.
      operationId: ingestAttendancePunch
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttendancePunchRequest'
      responses:
        '201':
          description: Punch recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendancePunchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
    get:
      tags: [Attendance]
      summary: List raw clock punches with filters
      description: Retrieves raw punch logs across tenant filtered by date, employee, source, or anomaly flag.
      operationId: listPunches
      parameters:
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: flag
          in: query
          required: false
          schema: { type: string }
        - name: source
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Raw punch list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RawPunchListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/me/roster:
    get:
      tags: [Attendance]
      summary: Shift roster schedule for authenticated employee
      description: |
        Returns the caller's rostered work shifts, rest days, and holidays across a date window.
      operationId: getMyShiftRoster
      parameters:
        - name: startDate
          in: query
          required: false
          schema: { type: string, format: date }
        - name: endDate
          in: query
          required: false
          schema: { type: string, format: date }
      responses:
        '200':
          description: Shift roster schedule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShiftRosterResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/team/coverage:
    get:
      tags: [Attendance]
      summary: Team shift coverage for a selected work date
      description: |
        Returns scheduled and active team members on a specified date with shift codes, roles, and duty status.
      operationId: getTeamShiftCoverage
      parameters:
        - name: date
          in: query
          required: true
          schema: { type: string, format: date }
      responses:
        '200':
          description: Team coverage roster
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamCoverageResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/roster/swap-request:
    post:
      tags: [Attendance]
      summary: Submit a shift swap request
      description: |
        Initiates a shift swap request with another team member. Supports Idempotency-Key for offline outbox sync.
      operationId: requestShiftSwap
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShiftSwapRequest'
      responses:
        '200':
          description: Shift swap request submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShiftSwapResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/me/regularise:
    post:
      tags: [Attendance]
      summary: Request attendance punch regularisation
      description: |
        Submits an attendance adjustment/regularisation request for missed punches to manager approval inbox.
      operationId: requestAttendanceRegularisation
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttendanceRegulariseRequest'
      responses:
        '200':
          description: Regularisation request submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendanceRegulariseResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/shifts:
    get:
      tags: [Attendance]
      summary: List configured work shifts
      description: Retrieves active work shift templates with timing and grace rules.
      operationId: listShifts
      responses:
        '200':
          description: Shift template list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShiftListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/roster:
    get:
      tags: [Attendance]
      summary: Retrieve workforce shift roster matrix
      description: Retrieves roster schedules and employee assignments across date range and departments.
      operationId: getRoster
      parameters:
        - name: startDate
          in: query
          required: false
          schema: { type: string, format: date }
        - name: endDate
          in: query
          required: false
          schema: { type: string, format: date }
        - name: department
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Roster schedule matrix
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RosterResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/shifts/assign:
    post:
      tags: [Attendance]
      summary: Assign or swap an employee shift
      description: Assigns a specific shift or marks a rest day for an employee on a work date.
      operationId: assignShift
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignShiftPayload'
      responses:
        '200':
          description: Shift assignment saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShiftScheduleItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/punches/ingest:
    post:
      tags: [Attendance]
      summary: Ingest punch via web admin
      description: Ingests a clock punch from administrator console with employee assignment.
      operationId: ingestPunchAdmin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IngestPunchAdminPayload'
      responses:
        '201':
          description: Punch ingested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RawPunchAdmin'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/daily:
    get:
      tags: [Attendance]
      summary: List calculated daily attendance rollups
      description: Retrieves daily attendance calculations with worked hours, OT, and calculation traces.
      operationId: listDailyAttendance
      parameters:
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: department
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: anomaly
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Daily attendance records and summary metrics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyAttendanceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/daily/{id}:
    get:
      tags: [Attendance]
      summary: Get daily attendance record details with formula trace
      description: Retrieves a single daily attendance record including detailed calculation audit trace.
      operationId: getDailyAttendanceDetails
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Daily attendance detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DailyAttendanceAdmin'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/attendance/recompute:
    post:
      tags: [Attendance]
      summary: Recompute attendance calculations
      description: Triggers attendance pairing, OT tiering, and lateness penalties calculation for date or employee.
      operationId: recomputeAttendance
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecomputeAttendancePayload'
      responses:
        '200':
          description: Recomputation completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecomputeAttendanceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/payroll-variable-summary:
    get:
      tags: [Attendance]
      summary: Retrieve variable payroll inputs summary
      description: Aggregates overtime hours, late deductions, and unpaid leave days for payroll period.
      operationId: getPayrollVariableSummary
      parameters:
        - name: payPeriodId
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Variable payroll summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttendancePayrollSummaryResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/devices:
    get:
      tags: [Attendance]
      summary: List registered biometric hardware terminals
      description: Returns all biometric devices (ZKTeco, Hikvision, Suprema, Anviz) registered for this tenant.
      operationId: listBiometricDevices
      responses:
        '200':
          description: Biometric devices list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricDeviceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Attendance]
      summary: Register a new biometric attendance terminal
      description: Adds a physical time-clock device configuration to the tenant.
      operationId: createBiometricDevice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBiometricDevicePayload'
      responses:
        '201':
          description: Device registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricDeviceItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/devices/{id}:
    get:
      tags: [Attendance]
      summary: Get biometric terminal details
      description: Returns details of a registered biometric hardware device.
      operationId: getBiometricDevice
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Biometric device details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricDeviceItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Attendance]
      summary: Update biometric terminal settings
      description: Modifies network, location, or direction settings for a biometric device.
      operationId: updateBiometricDevice
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBiometricDevicePayload'
      responses:
        '200':
          description: Device updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricDeviceItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Attendance]
      summary: Delete biometric terminal
      description: De-registers a biometric hardware terminal from the tenant.
      operationId: deleteBiometricDevice
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Device deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/attendance/devices/{id}/ping:
    post:
      tags: [Attendance]
      summary: Ping biometric terminal
      description: Tests connectivity and updates heartbeat for a registered biometric device.
      operationId: pingBiometricDevice
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Device ping result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricDeviceItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/attendance/devices/sync:
    post:
      tags: [Attendance]
      summary: Batch ingest hardware biometric punches
      description: Ingests batches of punch transactions pushed from edge IoT controllers or biometric middleware.
      operationId: syncBatchBiometricPunches
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HardwarePunchBatchPayload'
      responses:
        '200':
          description: Batch ingestion results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HardwarePunchResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/devices/simulate-punch:
    post:
      tags: [Attendance]
      summary: Simulate a hardware biometric terminal punch
      description: Simulates an incoming biometric clock transaction for testing and manual validation.
      operationId: simulateBiometricHardwarePunch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HardwarePunchItem'
      responses:
        '200':
          description: Simulation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HardwarePunchResult'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/attendance/devices/live-stream:
    get:
      tags: [Attendance]
      summary: Live feed of biometric terminal punches
      description: Returns real-time stream of punches ingested from biometric devices with employee and location metadata.
      operationId: getBiometricLiveStream
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 20 }
      responses:
        '200':
          description: Live stream of punches
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BiometricLiveStreamResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /iclock/cdata:
    get:
      tags: [Attendance]
      summary: ZKTeco ADMS terminal handshake
      description: Handles initial handshake and option negotiation for ZKTeco ADMS push protocol.
      operationId: zkTecoHandshake
      parameters:
        - name: SN
          in: query
          required: false
          schema: { type: string }
        - name: options
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: ZKTeco server configuration options
          content:
            text/plain:
              schema: { type: string }
    post:
      tags: [Attendance]
      summary: ZKTeco ADMS attendance log push
      description: Ingests raw attendance log transactions pushed by ZKTeco biometric terminals.
      operationId: zkTecoAttendanceLogPush
      parameters:
        - name: SN
          in: query
          required: false
          schema: { type: string }
        - name: table
          in: query
          required: false
          schema: { type: string }
      requestBody:
        required: false
        content:
          text/plain:
            schema: { type: string }
      responses:
        '200':
          description: Acknowledgment response (OK or OK count)
          content:
            text/plain:
              schema: { type: string }

  /iclock/getrequest:
    get:
      tags: [Attendance]
      summary: ZKTeco ADMS terminal heartbeat polling
      description: Periodic polling from ZKTeco terminal to check-in and retrieve pending commands.
      operationId: zkTecoGetRequest
      parameters:
        - name: SN
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Command delivery or OK
          content:
            text/plain:
              schema: { type: string }

  /iclock/devicecmd:
    post:
      tags: [Attendance]
      summary: ZKTeco ADMS device command acknowledgment
      description: Terminal feedback reporting execution status of previously dispatched commands.
      operationId: zkTecoDeviceCommandAck
      parameters:
        - name: SN
          in: query
          required: false
          schema: { type: string }
      requestBody:
        required: false
        content:
          text/plain:
            schema: { type: string }
      responses:
        '200':
          description: Acknowledgment response (OK)
          content:
            text/plain:
              schema: { type: string }

  /v1/approvals/pending:
    get:
      tags: [Approvals]
      summary: Pending approval inbox for the authenticated manager
      description: |
        Returns unified pending approvals across all modules (leave applications, attendance regularizations),
        including requester context, remaining leave balances, roster conflict warnings, and urgency indicators.
      operationId: listPendingApprovals
      responses:
        '200':
          description: Pending approvals list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PendingApprovalsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/approvals/{id}/decision:
    post:
      tags: [Approvals]
      summary: Record manager approval or rejection decision
      description: |
        Submits an approval or rejection decision on an item. Supports single-use Idempotency-Key
        for safe offline outbox retry. Returns 409 ALREADY_DECIDED if another approver acted first.
      operationId: decideApproval
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApprovalDecisionRequest'
      responses:
        '200':
          description: Decision recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalDecisionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/employees/me/documents:
    get:
      tags: [Employees]
      summary: Retrieve statutory and compliance documents for current employee
      description: |
        Returns active identity, statutory, and compliance documents belonging to the authenticated
        employee, including days remaining until expiry and compliance statuses.
      operationId: getOwnDocuments
      responses:
        '200':
          description: Employee documents retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EmployeeDocumentItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/employees/me/documents/renew:
    post:
      tags: [Employees]
      summary: Submit renewal or replacement for a compliance document
      description: |
        Submits updated document particulars (new expiry date, document number, and optional attachment)
        and archives the previous document record as REPLACED.
      operationId: renewOwnDocument
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RenewDocumentRequest'
      responses:
        '200':
          description: Document renewed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RenewDocumentResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
  /v1/leave/balances:
    get:
      tags: [Leave]
      summary: Retrieve current employee leave balances
      description: Returns active leave balances with entitled, accrued, taken, pending, and available days.
      operationId: getMyLeaveBalances
      responses:
        '200':
          description: Leave balances retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveBalancesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/leave/applications:
    get:
      tags: [Leave]
      summary: Retrieve employee leave applications
      description: Returns list of submitted leave requests for the authenticated employee.
      operationId: getMyLeaveApplications
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Leave applications retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveApplicationsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags: [Leave]
      summary: Submit a leave application
      description: Submits a new leave request and triggers the approval workflow.
      operationId: submitLeaveApplication
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveApplicationRequest'
      responses:
        '201':
          description: Leave application created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveApplicationItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/leave/eligibility:
    post:
      tags: [Leave]
      summary: Check leave application eligibility
      description: Pre-validates a requested date range, expands working days skipping weekends and holidays, and returns balance projection.
      operationId: checkLeaveEligibility
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaveEligibilityRequest'
      responses:
        '200':
          description: Eligibility evaluated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveEligibilityResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/leave/applications/{id}/cancel:
    post:
      tags: [Leave]
      summary: Cancel a leave application
      description: Cancels a pending or approved leave application and restores ledger debits.
      operationId: cancelLeaveApplication
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelLeaveRequest'
      responses:
        '200':
          description: Application cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveApplicationItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/leave/ledger:
    get:
      tags: [Leave]
      summary: Retrieve leave ledger statement
      description: Retrieves the immutable audit ledger entries explaining balance movements.
      operationId: getMyLeaveLedger
      parameters:
        - name: leaveTypeId
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Leave ledger statement entries retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveLedgerResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/leave/team-calendar:
    get:
      tags: [Leave]
      summary: Retrieve team leave calendar
      description: Retrieves daily absences and public holidays for a given month and year.
      operationId: getTeamCalendar
      parameters:
        - name: year
          in: query
          required: true
          schema: { type: integer }
        - name: month
          in: query
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: Team leave calendar retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamCalendarResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/payroll/me/payslips:
    get:
      tags: [Payroll]
      summary: Retrieve employee payslip history
      description: Retrieves finalized payslips for the authenticated employee.
      operationId: getMyPayslips
      responses:
        '200':
          description: List of employee payslips retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayslipsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/payroll/me/payslips/{id}:
    get:
      tags: [Payroll]
      summary: Retrieve detailed payslip with calculation traces
      description: Retrieves the detailed payslip breakdown, bank info, and formula traces.
      operationId: getPayslipDetails
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Detailed payslip retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayslipDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/me/payslips/{id}/comparison:
    get:
      tags: [Payroll]
      summary: Retrieve Month-over-Month payslip comparison
      description: Compares the payslip with the immediate prior period.
      operationId: getPayslipComparison
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Payslip comparison retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayslipComparisonResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/pay-groups:
    get:
      tags: [Payroll]
      summary: List pay groups
      description: Retrieves configured payroll calculation groups with frequency and currency rules.
      operationId: listPayGroups
      responses:
        '200':
          description: List of pay groups
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayGroupsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/payroll/pay-periods:
    get:
      tags: [Payroll]
      summary: List pay periods
      description: Retrieves pay periods optionally filtered by pay group identifier.
      operationId: listPayPeriods
      parameters:
        - name: payGroupId
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of pay periods
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayPeriodsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/payroll/runs:
    get:
      tags: [Payroll]
      summary: List payroll runs
      description: Retrieves payroll calculation runs filtered optionally by pay period identifier.
      operationId: listPayrollRuns
      parameters:
        - name: payPeriodId
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of payroll runs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/payroll/runs/{id}:
    get:
      tags: [Payroll]
      summary: Get payroll run by ID
      description: Retrieves status and summary totals for a specific payroll calculation run.
      operationId: getPayrollRunById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payroll run details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/calculate:
    post:
      tags: [Payroll]
      summary: Calculate payroll run
      description: Initiates a gross-to-net calculation batch for a pay group and period.
      operationId: calculatePayrollRun
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalculatePayrollRequest'
      responses:
        '200':
          description: Calculation initiated or completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/payroll/runs/{id}/approve:
    post:
      tags: [Payroll]
      summary: Approve payroll run
      description: Approves a calculated payroll run for commitment and bank file generation.
      operationId: approvePayrollRun
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payroll run approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/reject:
    post:
      tags: [Payroll]
      summary: Reject payroll run
      description: Rejects a payroll run back to draft status for recalculation.
      operationId: rejectPayrollRun
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payroll run rejected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/commit:
    post:
      tags: [Payroll]
      summary: Commit payroll run
      description: Finalizes and locks a payroll run, generating immutable payslip snapshots.
      operationId: commitPayrollRun
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payroll run committed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollRunItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/results:
    get:
      tags: [Payroll]
      summary: List payroll run results
      description: Retrieves employee-level gross, tax, statutory, and net salary results for a run.
      operationId: listPayrollResults
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of payroll results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollResultsResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/results/{resultId}:
    get:
      tags: [Payroll]
      summary: Get employee payroll result details
      description: Retrieves itemized earnings, deductions, and calculation traces for an employee result.
      operationId: getPayrollResultDetails
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: resultId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detailed payroll result with item lines
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollResultDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/variance:
    get:
      tags: [Payroll]
      summary: Get payroll run variance
      description: Computes period-over-period variance anomalies and headcount differences.
      operationId: getPayrollVariance
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Payroll variance breakdown and flagged anomalies
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PayrollVarianceResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/payroll/runs/{id}/bank-advice:
    post:
      tags: [Payroll]
      summary: Generate bank advice file
      description: Exports direct deposit batch instructions in standard CSV or NACHA bank formats.
      operationId: generateBankAdvice
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BankAdviceRequest'
      responses:
        '200':
          description: Generated bank advice payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAdviceResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/loans/types:
    get:
      tags: [Loans]
      summary: Retrieve company loan types and entitlement policies
      description: Returns active loan programs (e.g. Festival Advance, Distress Loan, Vehicle Loan) with tenure, interest rates, and eligibility rules.
      operationId: getLoanTypes
      responses:
        '200':
          description: Available loan types retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoanTypesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/loans/me:
    get:
      tags: [Loans]
      summary: Retrieve employee active and historical loans
      description: Returns loans and salary advances for the authenticated employee with remaining balances and repayment progress.
      operationId: getMyLoans
      responses:
        '200':
          description: Employee loans retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeLoansResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/loans/me/{id}:
    get:
      tags: [Loans]
      summary: Retrieve detailed loan particulars and amortization repayment schedule
      description: Returns loan details with installment-by-installment schedule, principal/interest breakdown, and deduction statuses.
      operationId: getMyLoanDetails
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Loan details with amortization schedule retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeLoanDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/loans/eligibility:
    post:
      tags: [Loans]
      summary: Pre-check loan eligibility and calculate projected installments
      description: Evaluates requested principal and tenure against employee tenure, basic salary multiples, and debt limits, returning instant pass/fail with EMI preview.
      operationId: checkLoanEligibility
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoanEligibilityRequest'
      responses:
        '200':
          description: Eligibility evaluated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoanEligibilityResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/loans/applications:
    post:
      tags: [Loans]
      summary: Submit a loan or salary advance application
      description: Submits loan request, generates amortization schedule, and initiates approval workflow.
      operationId: submitLoanApplication
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoanApplicationRequest'
      responses:
        '201':
          description: Loan application submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmployeeLoanItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/loans/me/{id}/settle:
    post:
      tags: [Loans]
      summary: Request early settlement of an active loan
      description: Calculates early payoff amount, waives future unaccrued interest if applicable, and marks remaining installments settled.
      operationId: requestLoanSettlement
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoanSettlementRequest'
      responses:
        '200':
          description: Early settlement processed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoanSettlementResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/expenses/categories:
    get:
      tags: [Expenses]
      summary: Retrieve company expense categories and policy limits
      description: Returns all active expense categories with per-claim spending limits, receipt requirements, and per-unit rates.
      operationId: getExpenseCategories
      responses:
        '200':
          description: Expense categories retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpenseCategoriesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/expenses/claims:
    get:
      tags: [Expenses]
      summary: Retrieve employee expense claims
      description: Returns expense reimbursement claims submitted by the current authenticated employee with summary metrics.
      operationId: getMyExpenseClaims
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Expense claims retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpenseClaimsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Expenses]
      summary: Submit a new multi-line expense claim
      description: Submits an expense claim with itemized receipts and lines for manager/finance approval.
      operationId: submitExpenseClaim
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExpenseClaimSubmitRequest'
      responses:
        '201':
          description: Expense claim submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpenseClaimDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/expenses/claims/{id}:
    get:
      tags: [Expenses]
      summary: Retrieve expense claim details with itemized lines
      description: Returns full particulars, itemized claim lines, receipt attachments, and approval metadata.
      operationId: getMyExpenseClaimDetails
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Expense claim details retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpenseClaimDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/expenses/claims/{id}/cancel:
    post:
      tags: [Expenses]
      summary: Cancel or withdraw a pending expense claim
      description: Withdraws a submitted or draft expense claim that has not yet been approved or reimbursed.
      operationId: cancelExpenseClaim
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelExpenseClaimRequest'
      responses:
        '200':
          description: Expense claim cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExpenseClaimItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/expenses/ocr:
    post:
      tags: [Expenses]
      summary: Parse receipt image or text using OCR
      description: Extracts merchant name, transaction date, total amount, and suggested category from a receipt payload.
      operationId: scanReceiptOcr
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReceiptOcrRequest'
      responses:
        '200':
          description: Receipt fields successfully extracted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReceiptOcrResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/benefits/catalogue:
    get:
      tags: [Benefits]
      summary: Retrieve company benefit catalogue and eligibility
      description: Returns available benefit categories and policies, evaluating employee eligibility and returning reasons if ineligible.
      operationId: getBenefitCatalogue
      responses:
        '200':
          description: Available benefit catalogue retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BenefitCatalogueResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/benefits/my-benefits:
    get:
      tags: [Benefits]
      summary: Retrieve active benefit enrollments and remaining balances
      description: Returns current employee benefit enrollments with annual entitlement, used amounts, pending claims, and registered dependents.
      operationId: getMyBenefits
      responses:
        '200':
          description: Employee benefit enrollments retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyBenefitsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/benefits/claims:
    get:
      tags: [Benefits]
      summary: Retrieve employee benefit claims history
      description: Returns all submitted, approved, and reimbursed benefit claims for the authenticated employee.
      operationId: getMyBenefitClaims
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Benefit claims retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BenefitClaimsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Benefits]
      summary: Submit a medical or flexible benefit reimbursement claim
      description: Submits a benefit reimbursement claim against an active enrollment policy with receipt attachment.
      operationId: submitBenefitClaim
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BenefitClaimSubmitRequest'
      responses:
        '201':
          description: Benefit claim submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BenefitClaimItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/benefits/claims/{id}/cancel:
    post:
      tags: [Benefits]
      summary: Cancel or withdraw a pending benefit claim
      description: Cancels a submitted benefit claim before approval or payment.
      operationId: cancelBenefitClaim
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelBenefitClaimRequest'
      responses:
        '200':
          description: Benefit claim cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BenefitClaimItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/lifecycle/movements:
    get:
      tags: [Lifecycle]
      summary: List employee career movements
      description: Returns filterable career movements including promotions, lateral transfers, and probation confirmations.
      operationId: getCareerMovements
      parameters:
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [DRAFT, SUBMITTED, APPROVED, SCHEDULED, APPLIED, REJECTED, CANCELLED, REVERTED] }
        - name: type
          in: query
          required: false
          schema: { type: string, enum: [PROMOTION, LATERAL_TRANSFER, DEMOTION, CONFIRMATION, SALARY_REVISION, DESIGNATION_CHANGE, SECONDMENT] }
      responses:
        '200':
          description: List of career movements
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerMovementListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Lifecycle]
      summary: Propose a career movement
      description: Initiates a new promotion, lateral transfer, confirmation, or salary revision request.
      operationId: proposeCareerMovement
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CareerMovementProposalRequest'
      responses:
        '201':
          description: Career movement proposed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerMovementItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/lifecycle/movements/timeline:
    get:
      tags: [Lifecycle]
      summary: Get interactive career timeline
      description: Returns chronological career milestones including hire, probation confirmation, promotions, and transfers for the employee.
      operationId: getCareerTimeline
      parameters:
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Career journey timeline milestones
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerTimelineResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/lifecycle/movements/{id}:
    get:
      tags: [Lifecycle]
      summary: Get career movement details
      description: Returns full particulars, previous vs proposed snapshot, and cascade audit summary.
      operationId: getCareerMovementById
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Movement details retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerMovementItem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/lifecycle/movements/{id}/approve:
    post:
      tags: [Lifecycle]
      summary: Approve career movement and trigger cascade
      description: Approves the career movement and executes the transactional cascade (or schedules it for effective date).
      operationId: approveCareerMovement
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Career movement approved and cascade executed or scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerMovementItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/lifecycle/movements/{id}/revert:
    post:
      tags: [Lifecycle]
      summary: Revert an approved or applied career movement
      description: Restores prior employee master fields and rolls back salary records.
      operationId: revertCareerMovement
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CareerMovementRevertRequest'
      responses:
        '200':
          description: Career movement reverted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CareerMovementItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/grievance/grounds:
    get:
      tags: [Grievance]
      summary: List grievance ground groups and grounds
      description: Returns all configured grievance categories, grounds with SLA resolution days, severity, and default handler roles.
      operationId: getGrievanceGrounds
      responses:
        '200':
          description: Grievance grounds catalogue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceGroundsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/grievance/channels:
    get:
      tags: [Grievance]
      summary: List grievance submission channels
      description: Returns available channels (Direct Portal, Anonymous Hotline, Ombudsman, Union).
      operationId: getGrievanceChannels
      responses:
        '200':
          description: Grievance channels list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceChannelsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/grievance/my-grievances:
    get:
      tags: [Grievance]
      summary: List grievances for authenticated employee
      description: Returns active and past grievances filed by or involving the employee.
      operationId: getMyGrievances
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Employee grievances
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/grievance/{id}:
    get:
      tags: [Grievance]
      summary: Get grievance particulars and resolution
      description: Returns detailed grievance status, SLA tracking, investigation findings, and appeal.
      operationId: getGrievanceById
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Grievance details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/grievance:
    post:
      tags: [Grievance]
      summary: Submit a new grievance or whistleblowing report
      description: Submits a grievance with confidential handling and optional anonymous whistleblowing flag.
      operationId: submitGrievance
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrievanceSubmitRequest'
      responses:
        '201':
          description: Grievance submitted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/grievance/{id}/appeal:
    post:
      tags: [Grievance]
      summary: Appeal a resolved grievance
      description: Lodges a formal appeal against a grievance resolution.
      operationId: appealGrievance
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GrievanceAppealRequest'
      responses:
        '200':
          description: Appeal submitted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrievanceDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/disciplinary/types:
    get:
      tags: [Disciplinary]
      summary: List disciplinary incident types and subtypes
      description: Returns company-defined incident classifications and subcategories.
      operationId: getIncidentTypes
      responses:
        '200':
          description: Incident types and subtypes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncidentTypesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/disciplinary/incidents:
    get:
      tags: [Disciplinary]
      summary: List disciplinary incidents
      description: Returns disciplinary incidents filtered by employee, severity, or status.
      operationId: getDisciplinaryIncidents
      parameters:
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Incidents list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DisciplinaryIncidentListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Disciplinary]
      summary: Report a disciplinary incident
      description: Creates an incident report with witness accounts and initial severity rating.
      operationId: reportDisciplinaryIncident
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DisciplinaryIncidentReportRequest'
      responses:
        '201':
          description: Incident reported successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DisciplinaryIncidentItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/disciplinary/incidents/{id}:
    get:
      tags: [Disciplinary]
      summary: Get disciplinary incident details
      description: Returns incident case particulars, investigation journal entries, corrective actions, and appeals.
      operationId: getDisciplinaryIncidentById
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Incident details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DisciplinaryIncidentDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/disciplinary/incidents/{id}/actions:
    post:
      tags: [Disciplinary]
      summary: Issue progressive corrective action
      description: Issues a corrective action (Oral Warning, Written Warning, Show Cause Notice, Suspension, etc.) for an incident.
      operationId: issueCorrectiveAction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueCorrectiveActionRequest'
      responses:
        '201':
          description: Action issued successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CorrectiveActionItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/disciplinary/incidents/{id}/journal:
    post:
      tags: [Disciplinary]
      summary: Add investigation journal case note
      description: Appends a chronological entry to the incident investigation journal.
      operationId: addIncidentJournalEntry
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddJournalEntryRequest'
      responses:
        '201':
          description: Journal entry added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IncidentJournalEntryItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/disciplinary/actions/{id}/respond:
    post:
      tags: [Disciplinary]
      summary: Submit employee response to corrective action
      description: Allows employee to submit a formal written response or explanation to a show cause notice or warning.
      operationId: respondToCorrectiveAction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CorrectiveActionResponseRequest'
      responses:
        '200':
          description: Response recorded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CorrectiveActionItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/disciplinary/actions/{id}/appeal:
    post:
      tags: [Disciplinary]
      summary: File appeal against corrective action
      description: Lodges a formal appeal against an issued disciplinary penalty or action.
      operationId: appealCorrectiveAction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DisciplinaryAppealRequest'
      responses:
        '201':
          description: Appeal lodged successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DisciplinaryAppealItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/goals:
    get:
      tags: [Performance]
      summary: List performance goals and OKRs
      description: Returns goals and OKR objectives filtered by cycle, employee, or status.
      operationId: getGoals
      parameters:
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: cycleId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Goals list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Performance]
      summary: Create a performance goal or OKR objective
      description: Creates a new goal or key result with target metric and weighting.
      operationId: createGoal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoalCreateRequest'
      responses:
        '201':
          description: Goal created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/performance/goals/{id}/check-in:
    post:
      tags: [Performance]
      summary: Record goal progress check-in
      description: Updates goal current metric value, recalculates progress percentage, and appends a check-in note.
      operationId: recordGoalCheckIn
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoalCheckInRequest'
      responses:
        '200':
          description: Check-in recorded and goal updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/competencies:
    get:
      tags: [Performance]
      summary: List competency framework catalogue
      description: Returns company-defined competency clusters and behavioral competencies.
      operationId: getCompetencies
      responses:
        '200':
          description: Competency framework
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompetencyFrameworkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/performance/appraisals/my:
    get:
      tags: [Performance]
      summary: List authenticated employee appraisals
      description: Returns active and historical appraisals for the authenticated employee.
      operationId: getMyAppraisals
      parameters:
        - name: cycleId
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: My appraisals
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/performance/appraisals/team:
    get:
      tags: [Performance]
      summary: List team appraisals for manager
      description: Returns appraisals for direct reports managed by the caller.
      operationId: getTeamAppraisals
      parameters:
        - name: cycleId
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Team appraisals
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/performance/appraisals/{id}:
    get:
      tags: [Performance]
      summary: Get appraisal details by ID
      description: Returns complete appraisal record, goal ratings, competency ratings, and 360 feedback summaries.
      operationId: getAppraisalById
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Appraisal detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/appraisals/{id}/self-review:
    post:
      tags: [Performance]
      summary: Submit employee self-assessment
      description: Submits self-ratings and comments for goals and competencies.
      operationId: submitSelfReview
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppraisalSelfReviewRequest'
      responses:
        '200':
          description: Self-review submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/appraisals/{id}/manager-review:
    post:
      tags: [Performance]
      summary: Submit manager appraisal
      description: Submits manager evaluation, goal ratings, competency proficiencies, and computed weighted score.
      operationId: submitManagerReview
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppraisalManagerReviewRequest'
      responses:
        '200':
          description: Manager appraisal submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/appraisals/{id}/acknowledge:
    post:
      tags: [Performance]
      summary: Acknowledge performance appraisal
      description: Employee signs and acknowledges final appraisal outcome and calibration notes.
      operationId: acknowledgeAppraisal
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Appraisal acknowledged
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppraisalDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/performance/feedback:
    get:
      tags: [Performance]
      summary: List continuous feedback notes
      description: Returns praise, 1-on-1 coaching notes, and check-in logs for an employee.
      operationId: getContinuousFeedback
      parameters:
        - name: employeeId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: type
          in: query
          required: false
          schema: { type: string }
      responses:
        '200':
          description: Feedback notes list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuousFeedbackListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Performance]
      summary: Send continuous feedback or 1-on-1 note
      description: Creates a praise note, coaching observation, or check-in entry.
      operationId: sendContinuousFeedback
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendFeedbackRequest'
      responses:
        '201':
          description: Feedback sent successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuousFeedbackItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/recruitment/vacancies:
    get:
      tags: [Recruitment]
      summary: List job vacancies
      description: Returns job vacancies filtered by status and department.
      operationId: getVacancies
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [DRAFT, OPEN, ON_HOLD, CLOSED, FILLED]
        - name: departmentId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of vacancies
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VacancyListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Recruitment]
      summary: Create new job vacancy
      description: Creates a new open requisition vacancy posting.
      operationId: createVacancy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VacancyCreateRequest'
      responses:
        '201':
          description: Vacancy created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VacancyItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/recruitment/vacancies/{id}:
    get:
      tags: [Recruitment]
      summary: Get vacancy details
      description: Retrieves a specific vacancy by its unique identifier.
      operationId: getVacancyById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Vacancy item details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VacancyItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/recruitment/candidates:
    get:
      tags: [Recruitment]
      summary: List candidate pool
      description: Searches candidates across talent database with optional search query.
      operationId: getCandidates
      parameters:
        - name: query
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Candidate pool list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CandidateListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Recruitment]
      summary: Register candidate
      description: Adds a new candidate to the ATS talent pool.
      operationId: createCandidate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CandidateCreateRequest'
      responses:
        '201':
          description: Candidate created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CandidateItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/recruitment/applications:
    get:
      tags: [Recruitment]
      summary: List candidate applications
      description: Returns pipeline applications filtered by vacancy, stage, and active status.
      operationId: getApplications
      parameters:
        - name: vacancyId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: stage
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Candidate application pipeline list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Recruitment]
      summary: Submit candidate application
      description: Submits an application for a candidate to a specific job vacancy.
      operationId: createApplication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplicationCreateRequest'
      responses:
        '201':
          description: Application submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/recruitment/applications/{id}/stage:
    post:
      tags: [Recruitment]
      summary: Update application stage
      description: Transitions candidate application to next hiring stage.
      operationId: updateApplicationStage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplicationStageUpdateRequest'
      responses:
        '200':
          description: Stage updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/recruitment/interviews:
    get:
      tags: [Recruitment]
      summary: List interviews
      description: Returns scheduled or completed interviews filtered by application or interviewer.
      operationId: getInterviews
      parameters:
        - name: applicationId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: interviewerEmployeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of interviews
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InterviewListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Recruitment]
      summary: Schedule interview round
      description: Schedules an interview session with candidate and panel members.
      operationId: scheduleInterview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InterviewScheduleRequest'
      responses:
        '201':
          description: Interview scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InterviewItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/recruitment/interviews/{id}/scorecard:
    post:
      tags: [Recruitment]
      summary: Submit interview evaluation scorecard
      description: Submits ratings, strengths, weaknesses, and hiring recommendation.
      operationId: submitInterviewScorecard
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScorecardSubmitRequest'
      responses:
        '201':
          description: Scorecard recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InterviewScorecardItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/recruitment/applications/{id}/offer:
    get:
      tags: [Recruitment]
      summary: Get employment offer details
      description: Retrieves employment offer terms for an application.
      operationId: getApplicationOffer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Offer detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OfferDetail'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [Recruitment]
      summary: Create or extend employment offer
      description: Creates a formal offer letter with compensation packages and joining terms.
      operationId: createApplicationOffer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OfferCreateRequest'
      responses:
        '201':
          description: Offer created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OfferDetail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/onboarding/profiles:
    get:
      tags: [Onboarding]
      summary: List onboarding profiles
      description: Returns all onboarding tracks configured for departments and roles.
      operationId: getOnboardingProfiles
      responses:
        '200':
          description: Onboarding profiles list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnboardingProfileListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/onboarding/instances:
    get:
      tags: [Onboarding]
      summary: List onboarding instances
      description: Returns active or completed employee onboarding instances.
      operationId: getOnboardingInstances
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [NOT_STARTED, IN_PROGRESS, COMPLETED, CANCELLED]
      responses:
        '200':
          description: List of onboarding instances
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnboardingInstanceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Onboarding]
      summary: Initialize employee onboarding
      description: Launches onboarding checklist instance for an employee from a profile blueprint.
      operationId: createOnboardingInstance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OnboardingInstanceCreateRequest'
      responses:
        '201':
          description: Onboarding instance initialized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnboardingInstanceDetail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/onboarding/instances/{id}:
    get:
      tags: [Onboarding]
      summary: Get onboarding instance details
      description: Retrieves complete onboarding instance including stage checklist tasks.
      operationId: getOnboardingInstanceById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Onboarding instance details with task checklist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnboardingInstanceDetail'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/onboarding/tasks/{id}/complete:
    post:
      tags: [Onboarding]
      summary: Complete onboarding task
      description: Marks an onboarding checklist task as completed and recalculates overall instance progress.
      operationId: completeOnboardingTask
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OnboardingTaskCompleteRequest'
      responses:
        '200':
          description: Task marked completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OnboardingTaskItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/offboarding/exit-types:
    get:
      tags: [Offboarding]
      summary: List exit types and reasons
      description: Returns exit departure types and configured exit reasons catalog.
      operationId: getExitTypes
      responses:
        '200':
          description: Exit types catalog
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitTypeListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/offboarding/exit-notices:
    get:
      tags: [Offboarding]
      summary: List exit notices
      description: Returns employee resignation and termination notices filtered by employee or status.
      operationId: getExitNotices
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [DRAFT, SUBMITTED, APPROVED, REJECTED, WITHDRAWN, REVERSED]
      responses:
        '200':
          description: Exit notice list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitNoticeListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Offboarding]
      summary: Submit resignation exit notice
      description: Submits a formal resignation notice with requested last working date and reason.
      operationId: createExitNotice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExitNoticeCreateRequest'
      responses:
        '201':
          description: Resignation notice submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitNoticeItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/offboarding/exit-notices/{id}/approve:
    post:
      tags: [Offboarding]
      summary: Approve exit notice
      description: Approves resignation notice, finalizes last working date, and initializes departmental clearance tasks.
      operationId: approveExitNotice
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExitNoticeApproveRequest'
      responses:
        '200':
          description: Exit notice approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitNoticeItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/offboarding/exit-notices/{id}/reject:
    post:
      tags: [Offboarding]
      summary: Reject exit notice
      description: Rejects or rescinds an exit notice with reasons or remarks.
      operationId: rejectExitNotice
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                remarks:
                  type: string
      responses:
        '200':
          description: Exit notice rejected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitNoticeItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/offboarding/exit-notices/{id}/clearance:
    get:
      tags: [Offboarding]
      summary: Get clearance checklist
      description: Retrieves cross-departmental clearance items and financial dues recovery summary.
      operationId: getExitNoticeClearance
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Clearance checklist detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClearanceDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/offboarding/clearance-tasks/{id}/status:
    post:
      tags: [Offboarding]
      summary: Update clearance task status
      description: Departmental officer clears, waives, or rejects a clearance item with notes and recoverable charges.
      operationId: updateClearanceTaskStatus
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClearanceTaskStatusUpdateRequest'
      responses:
        '200':
          description: Clearance task updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClearanceTaskItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/offboarding/exit-notices/{id}/interview:
    get:
      tags: [Offboarding]
      summary: Get exit interview
      description: Retrieves exit interview responses and ratings for an exit notice.
      operationId: getExitInterview
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Exit interview detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitInterviewItem'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [Offboarding]
      summary: Submit exit interview
      description: Submits feedback, ratings, and sentiment responses from employee exit interview.
      operationId: submitExitInterview
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExitInterviewSubmitRequest'
      responses:
        '201':
          description: Exit interview recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExitInterviewItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/documents/folders:
    get:
      tags: [Documents]
      summary: List document folders
      description: Retrieves hierarchical folders for company documents with document count and access level.
      operationId: listDocumentFolders
      responses:
        '200':
          description: List of document folders
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentFolderListResponse'
    post:
      tags: [Documents]
      summary: Create document folder
      description: Creates a new folder for categorizing company documents.
      operationId: createDocumentFolder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentFolderCreateRequest'
      responses:
        '201':
          description: Document folder created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentFolderItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/documents:
    get:
      tags: [Documents]
      summary: List company documents
      description: Retrieves documents filtered by folder, category, or search term with latest version metadata.
      operationId: listDocuments
      parameters:
        - name: folderId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: category
          in: query
          required: false
          schema:
            type: string
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of documents
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentListResponse'
    post:
      tags: [Documents]
      summary: Upload or register document
      description: Creates a new company document record and initial version.
      operationId: createDocument
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentCreateRequest'
      responses:
        '201':
          description: Document created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompanyDocumentItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/documents/{id}:
    get:
      tags: [Documents]
      summary: Get document details and version history
      description: Retrieves document metadata and complete version audit history.
      operationId: getDocumentDetails
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Document details with version history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/documents/{id}/versions:
    post:
      tags: [Documents]
      summary: Upload new version of document
      description: Increments version number and updates current version reference with changelog.
      operationId: createDocumentVersion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentVersionCreateRequest'
      responses:
        '201':
          description: New version created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentVersionItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/documents/templates:
    get:
      tags: [Documents]
      summary: List document templates
      description: Retrieves letter and agreement templates available for generation.
      operationId: listDocumentTemplates
      responses:
        '200':
          description: List of document templates
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentTemplateListResponse'

  /v1/documents/templates/{id}:
    get:
      tags: [Documents]
      summary: Get document template
      description: Retrieves template content and required substitution variables.
      operationId: getDocumentTemplate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Template details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentTemplateItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/documents/letter-requests:
    get:
      tags: [Documents]
      summary: List letter requests
      description: Retrieves employee letter requests with status and approval info.
      operationId: listLetterRequests
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of letter requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetterRequestListResponse'
    post:
      tags: [Documents]
      summary: Submit letter request
      description: Requests generation of an official HR letter (service, visa, salary verification).
      operationId: createLetterRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LetterRequestCreateRequest'
      responses:
        '201':
          description: Letter request submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetterRequestItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/documents/letter-requests/{id}/approve:
    post:
      tags: [Documents]
      summary: Approve or reject letter request
      description: Approves request and triggers automatic variable substitution, or rejects with reason.
      operationId: approveLetterRequest
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LetterRequestApproveRequest'
      responses:
        '200':
          description: Letter request updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetterRequestItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/documents/letter-requests/{id}/reject:
    post:
      tags: [Documents]
      summary: Reject letter request
      description: Rejects a requested official company letter with optional remarks.
      operationId: rejectLetterRequest
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                remarks:
                  type: string
      responses:
        '200':
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetterRequestItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/signatures/requests:
    get:
      tags: [Signatures]
      summary: List digital signature requests
      description: Retrieves signature requests filtered by status or participant employee.
      operationId: listSignatureRequests
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: signerEmployeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of signature requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignatureRequestListResponse'
    post:
      tags: [Signatures]
      summary: Create signature request
      description: Dispatches document for sequential/multi-signer digital signing.
      operationId: createSignatureRequest
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignatureRequestCreateRequest'
      responses:
        '201':
          description: Signature request created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignatureRequestItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/signatures/requests/{id}:
    get:
      tags: [Signatures]
      summary: Get signature request details
      description: Retrieves signature request with signers status and immutable audit logs.
      operationId: getSignatureRequestDetails
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Signature request details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignatureRequestDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/signatures/requests/{id}/sign:
    post:
      tags: [Signatures]
      summary: Sign document
      description: Submits canvas signature drawing or typed signature with legal consent acknowledgment.
      operationId: signDocument
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignatureSignRequest'
      responses:
        '200':
          description: Document signed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignatureRequestDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/training/courses:
    get:
      tags: [Training]
      summary: List training courses
      description: Retrieves courses filtered by category, competency, or search query.
      operationId: listTrainingCourses
      parameters:
        - name: category
          in: query
          required: false
          schema:
            type: string
        - name: competencyId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of training courses
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseListResponse'
    post:
      tags: [Training]
      summary: Create training course
      description: Registers a new course in the training catalogue.
      operationId: createTrainingCourse
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourseCreateRequest'
      responses:
        '201':
          description: Training course created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/training/courses/{id}:
    get:
      tags: [Training]
      summary: Get training course details
      description: Retrieves detailed course information, syllabus, provider, and upcoming schedules.
      operationId: getTrainingCourseDetails
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Course details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourseDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/training/schedules:
    get:
      tags: [Training]
      summary: List training schedules
      description: Retrieves scheduled training sessions and batches.
      operationId: listTrainingSchedules
      parameters:
        - name: courseId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of training schedules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingScheduleListResponse'
    post:
      tags: [Training]
      summary: Create training schedule
      description: Schedules a new training batch or session offering.
      operationId: createTrainingSchedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrainingScheduleCreateRequest'
      responses:
        '201':
          description: Training schedule created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingScheduleItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/training/enrollments:
    get:
      tags: [Training]
      summary: List training enrollments
      description: Retrieves employee training enrollments filtered by employee, schedule, or status.
      operationId: listTrainingEnrollments
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: scheduleId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of training enrollments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingEnrollmentListResponse'
    post:
      tags: [Training]
      summary: Enrol or nominate employee for training
      description: Enrols an employee into a training schedule (self-enrolled or manager-nominated).
      operationId: createTrainingEnrollment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrainingEnrollmentCreateRequest'
      responses:
        '201':
          description: Training enrollment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingEnrollmentItem'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/training/enrollments/{id}/approve:
    post:
      tags: [Training]
      summary: Approve or reject training enrollment
      description: Approves or rejects a pending employee training nomination.
      operationId: approveTrainingEnrollment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrainingEnrollmentApproveRequest'
      responses:
        '200':
          description: Enrollment decision recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingEnrollmentItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/training/enrollments/{id}/attendance:
    post:
      tags: [Training]
      summary: Check in attendance for training session
      description: Ingests an employee attendance check-in for a scheduled training session.
      operationId: checkInTrainingAttendance
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrainingAttendanceCheckInRequest'
      responses:
        '200':
          description: Attendance checked in successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingAttendanceItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/training/enrollments/{id}/evaluate:
    post:
      tags: [Training]
      summary: Submit trainee feedback and evaluation
      description: Submits participant course feedback ratings, reviews, and automatically issues certificate if eligible.
      operationId: submitTraineeEvaluation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TraineeEvaluationSubmitRequest'
      responses:
        '200':
          description: Evaluation submitted and processed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TraineeEvaluationItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/training/certificates:
    get:
      tags: [Training]
      summary: List training certificates
      description: Retrieves verified digital certificates issued to an employee.
      operationId: listTrainingCertificates
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of certificates
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingCertificateListResponse'

  /v1/training/needs:
    get:
      tags: [Training]
      summary: Get recommended courses based on competency gaps
      description: Analyzes performance appraisal ratings and recommends courses closing competency gaps.
      operationId: getTrainingNeeds
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Personalized course recommendations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrainingNeedsResponse'

  /v1/timesheets/clients:
    get:
      tags: [Timesheets]
      summary: List timesheet clients
      description: Retrieves active client organisations for project and activity billing.
      operationId: listTimesheetClients
      responses:
        '200':
          description: List of clients
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetClientListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/timesheets/projects:
    get:
      tags: [Timesheets]
      summary: List billable and internal projects
      description: Retrieves projects filtered optionally by client identifier.
      operationId: listTimesheetProjects
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of projects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetProjectListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Timesheets]
      summary: Create timesheet project
      description: Registers a new project under a client organisation.
      operationId: createTimesheetProject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimesheetProjectCreateRequest'
      responses:
        '201':
          description: Project created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetProjectItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/timesheets/projects/{id}/activities:
    get:
      tags: [Timesheets]
      summary: List activities for project
      description: Retrieves task types and activities configured for a project.
      operationId: listTimesheetActivities
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of activities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetActivityListResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/timesheets/activities:
    get:
      tags: [Timesheets]
      summary: List global timesheet activities
      description: Retrieves task types and activities configured across the organisation or for a project.
      operationId: listGlobalTimesheetActivities
      parameters:
        - name: projectId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of activities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetActivityListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/timesheets/my:
    get:
      tags: [Timesheets]
      summary: List employee timesheets
      description: Retrieves weekly timesheet summaries for an employee.
      operationId: listMyTimesheets
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of timesheets
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/timesheets/{id}:
    get:
      tags: [Timesheets]
      summary: Get timesheet details
      description: Retrieves a weekly timesheet including all daily project/activity entries.
      operationId: getTimesheetById
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Timesheet detail with entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/timesheets:
    get:
      tags: [Timesheets]
      summary: List all timesheets
      description: Retrieves submitted and draft timesheets across the organisation for review.
      operationId: listTimesheets
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of timesheets
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Timesheets]
      summary: Save timesheet draft
      description: Creates or updates a weekly timesheet draft with daily time entries.
      operationId: saveTimesheetDraft
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimesheetSaveRequest'
      responses:
        '200':
          description: Saved timesheet draft
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/timesheets/{id}/submit:
    post:
      tags: [Timesheets]
      summary: Submit timesheet for approval
      description: Transitions timesheet from DRAFT to SUBMITTED status for manager review.
      operationId: submitTimesheet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Submitted timesheet
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/timesheets/{id}/approve:
    post:
      tags: [Timesheets]
      summary: Approve submitted timesheet
      description: Approves a submitted timesheet and locks entries for billing.
      operationId: approveTimesheet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimesheetApproveRequest'
      responses:
        '200':
          description: Approved timesheet
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/timesheets/{id}/reject:
    post:
      tags: [Timesheets]
      summary: Reject submitted timesheet
      description: Rejects a submitted timesheet with an explanation for corrections.
      operationId: rejectTimesheet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimesheetRejectRequest'
      responses:
        '200':
          description: Rejected timesheet
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/timesheets/reconciliation:
    get:
      tags: [Timesheets]
      summary: Reconcile timesheet with attendance
      description: Compares logged project timesheet hours with clocked attendance hours for a given week.
      operationId: getTimesheetReconciliation
      parameters:
        - name: employeeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: weekStart
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Weekly attendance reconciliation summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetReconciliationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/timesheets/copy-previous:
    post:
      tags: [Timesheets]
      summary: Copy previous week timesheet rows
      description: Pre-populates the current week draft with projects and activities from the prior week.
      operationId: copyPreviousWeekTimesheet
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimesheetCopyPreviousRequest'
      responses:
        '200':
          description: Pre-populated timesheet draft
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimesheetDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /v1/tenants:
    get:
      tags: [Tenancy]
      summary: List customer organisations
      description: Returns a list of customer organisations with isolation tier and enabled module status.
      operationId: listTenants
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: plan
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of tenants
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Tenancy]
      summary: Create customer organisation
      description: Provisions a new tenant, seeds default system roles, and configures initial modules.
      operationId: createTenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTenantPayload'
      responses:
        '201':
          description: Tenant provisioned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantDetail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/tenants/{id}:
    get:
      tags: [Tenancy]
      summary: Get tenant detail
      description: Retrieves complete configuration, isolation tier, and module details for a tenant.
      operationId: getTenant
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Tenant detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Tenancy]
      summary: Update tenant detail
      description: Updates organisation attributes, branding, timezone, currency, or subscription tier.
      operationId: updateTenant
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTenantPayload'
      responses:
        '200':
          description: Updated tenant detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantDetail'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/tenants/{id}/archive:
    post:
      tags: [Tenancy]
      summary: Archive tenant
      description: Transitions organisation status to ARCHIVED.
      operationId: archiveTenant
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Archived tenant detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/tenants/{id}/modules:
    put:
      tags: [Tenancy]
      summary: Update tenant modules
      description: Enables or disables platform modules for a customer organisation.
      operationId: updateTenantModules
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTenantModulesPayload'
      responses:
        '200':
          description: Updated module statuses
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantModulesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/roles:
    get:
      tags: [Roles]
      summary: List roles
      description: Lists system and custom roles defined in the current organisation.
      operationId: listRoles
      responses:
        '200':
          description: List of roles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Roles]
      summary: Create custom role
      description: Creates a new custom role with assigned permission keys.
      operationId: createRole
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRolePayload'
      responses:
        '201':
          description: Created role summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/roles/{id}:
    get:
      tags: [Roles]
      summary: Get role detail
      description: Retrieves role details and assigned permissions.
      operationId: getRole
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Role summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Roles]
      summary: Update role
      description: Updates role name, description, and permission assignments.
      operationId: updateRole
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRolePayload'
      responses:
        '200':
          description: Updated role summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Roles]
      summary: Delete custom role
      description: Deletes custom role. System roles cannot be deleted.
      operationId: deleteRole
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Role deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleActionSuccess'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/permissions:
    get:
      tags: [Roles]
      summary: List system permissions
      description: Returns the global catalogue of permissions across modules.
      operationId: listPermissions
      responses:
        '200':
          description: List of permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PermissionListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /v1/users:
    get:
      tags: [Users]
      summary: List organisation users
      description: Lists user accounts in current tenant with status and role filtering.
      operationId: listUsers
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: role
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Users]
      summary: Create user account
      description: Creates a user account, hashes credentials, and assigns role.
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserPayload'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/users/{id}:
    get:
      tags: [Users]
      summary: Get user detail
      description: Retrieves single user account details and roles.
      operationId: getUser
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Users]
      summary: Update user account
      description: Updates user status (active, locked, disabled) or role assignment.
      operationId: updateUser
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserPayload'
      responses:
        '200':
          description: Updated user summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/users/{id}/reset-password:
    post:
      tags: [Users]
      summary: Reset user password
      description: Generates or sets temporary password and revokes existing sessions.
      operationId: resetUserPassword
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordPayload'
      responses:
        '200':
          description: Password reset confirmation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResetPasswordResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/users/{userId}/devices:
    get:
      tags: [Users]
      summary: List user devices
      description: Lists active registered devices and biometric tokens for a user.
      operationId: listUserDevices
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of user devices
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserDeviceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /v1/users/{userId}/devices/{deviceId}:
    delete:
      tags: [Users]
      summary: Revoke user device
      description: Revokes device registration and associated refresh tokens.
      operationId: revokeUserDevice
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: deviceId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Device revoked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RevokeDeviceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/users/{userId}/devices/revoke-all:
    post:
      tags: [Users]
      summary: Revoke all user devices
      description: Bulk-revokes all active registered devices and refresh tokens for user.
      operationId: revokeAllUserDevices
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Revoked count
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RevokeAllDevicesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Access token from `/v1/auth/token`. Lifetime 15 minutes.

  parameters:
    TenantCode:
      name: X-Tenant-Code
      in: header
      required: true
      description: Organisation code. Required on unauthenticated endpoints, where no token exists yet.
      schema:
        type: string
        pattern: '^[a-z0-9][a-z0-9-]{1,62}[a-z0-9]$'

    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: |
        Client-generated key (UUIDv7) making the request safe to retry. Required on endpoints the
        offline outbox replays. Reusing a key with a different payload is rejected rather than
        silently returning the earlier result.
      schema:
        type: string
        maxLength: 128

  responses:
    Unauthorized:
      description: Authentication required or token invalid
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    Forbidden:
      description: Authenticated but not permitted
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    BadRequest:
      description: Malformed request, or a field value that could not be interpreted
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    Conflict:
      description: |
        The record changed since the caller loaded it. `details` carries `expected` and `actual`
        versions so the client can offer a meaningful choice rather than "please try again".
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    UnprocessableEntity:
      description: |
        Syntactically valid but rejected by a domain rule. `details.violations` lists **every**
        problem, not just the first — reporting one at a time turns filling a form into a guessing
        game.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
    RateLimited:
      description: Too many requests
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds to wait before retrying
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'

  schemas:
    ApiErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          $ref: '#/components/schemas/ApiError'

    ApiError:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          description: Stable machine-readable identifier. Clients localise from this.
          examples: [LEAVE_BALANCE_INSUFFICIENT]
        message:
          type: string
          description: Developer-facing English description. Not for display to end users.
        field:
          type: string
          description: Input field the error is attributable to.
        details:
          type: object
          additionalProperties: true
          description: Structured context for building a localised message.
          examples: [{ available: 2.5, requested: 5 }]
        violations:
          type: array
          items:
            $ref: '#/components/schemas/FieldViolation'
        requestId:
          type: string
          description: Correlates with server logs. Quote this to support.

    FieldViolation:
      type: object
      required: [field, code, message]
      properties:
        field: { type: string }
        code: { type: string }
        message: { type: string }
        rejectedValue: {}

    ResolveTenantRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        orgCode:
          type: string
      description: Supply exactly one of `email` or `orgCode`.

    ResolveTenantResponse:
      type: object
      required: [code, name, authMethods]
      properties:
        code: { type: string }
        name: { type: string }
        logoUrl: { type: string, format: uri }
        brandColor: { type: string, pattern: '^#[0-9A-Fa-f]{6}$' }
        locale: { type: string }
        authMethods:
          type: array
          description: Which sign-in methods this organisation offers, so the client renders the right form.
          items:
            type: string
            enum: [PASSWORD, SSO_SAML, SSO_OIDC]
        ssoRedirectUrl: { type: string, format: uri }

    PasswordGrantRequest:
      type: object
      required: [username, password, device]
      properties:
        username: { type: string }
        password: { type: string, format: password }
        device:
          $ref: '#/components/schemas/DeviceInfo'

    RefreshTokenRequest:
      type: object
      required: [refreshToken]
      properties:
        refreshToken: { type: string }

    BiometricGrantRequest:
      type: object
      required: [sealedRefreshToken, deviceId]
      properties:
        sealedRefreshToken:
          type: string
          description: Refresh token released from device secure hardware after a successful biometric assertion.
        deviceId:
          type: string
          description: Must match the device the token was bound to at enrolment.

    TokenResponse:
      type: object
      required: [accessToken, refreshToken, tokenType, expiresIn]
      properties:
        accessToken: { type: string }
        refreshToken: { type: string }
        tokenType: { type: string, enum: [Bearer] }
        expiresIn: { type: integer, description: Access token lifetime in seconds. }
        refreshExpiresIn: { type: integer }
        biometricEnrolmentOffered:
          type: boolean
          description: |
            True when this device has not yet enrolled biometrics. The client should offer
            enrolment immediately after first sign-in — deferring it is why users end up typing
            passwords forever.

    DeviceInfo:
      type: object
      required: [deviceId, platform]
      properties:
        deviceId: { type: string, maxLength: 128 }
        platform: { type: string, enum: [ANDROID, IOS, WEB, KIOSK] }
        model: { type: string }
        osVersion: { type: string }
        appVersion: { type: string }
        pushToken: { type: string }

    RegisterDeviceRequest:
      allOf:
        - $ref: '#/components/schemas/DeviceInfo'
        - type: object
          properties:
            attestation:
              type: string
              description: Play Integrity or App Attest token, verified server-side.

    Device:
      type: object
      required: [id, deviceId, platform, trusted]
      properties:
        id: { type: string, format: uuid }
        deviceId: { type: string }
        platform: { type: string, enum: [ANDROID, IOS, WEB, KIOSK] }
        model: { type: string }
        osVersion: { type: string }
        appVersion: { type: string }
        biometricEnrolled: { type: boolean }
        attestationVerified: { type: boolean }
        trusted: { type: boolean }
        lastSeenAt: { type: string, format: date-time }
        current: { type: boolean, description: True for the device making this request. }

    JwkSet:
      type: object
      required: [keys]
      description: A JSON Web Key Set as defined by RFC 7517 §5.
      properties:
        keys:
          type: array
          items:
            $ref: '#/components/schemas/Jwk'

    Jwk:
      type: object
      required: [kty, kid]
      description: |
        A public JSON Web Key (RFC 7517 §4). Only the fields relevant to RSA signature
        verification are described; additional members may be present and must be ignored rather
        than rejected, per the RFC.
      properties:
        kty: { type: string, description: Key type. Always `RSA`. }
        kid: { type: string, description: Key identifier, matching the `kid` header of issued tokens. }
        use: { type: string, description: Public key use. Always `sig`. }
        alg: { type: string, description: Signing algorithm. Always `RS256`. }
        n: { type: string, description: RSA modulus, base64url-encoded. }
        e: { type: string, description: RSA exponent, base64url-encoded. }

    FormSchema:
      type: object
      required: [entityType, version, sections]
      properties:
        entityType: { type: string }
        version:
          type: string
          description: |
            A hash of the tenant's field definitions. Cache against it and skip re-rendering when
            unchanged. Deliberately not a timestamp — two servers must agree for the same
            configuration, or a client behind a load balancer sees the schema change on every
            other request.
        sections:
          type: array
          items:
            $ref: '#/components/schemas/FormSection'

    FormSection:
      type: object
      required: [key, label, fields]
      properties:
        key: { type: string }
        label: { type: string }
        fields:
          type: array
          items:
            $ref: '#/components/schemas/FormField'

    FormField:
      type: object
      required: [key, label, type]
      properties:
        key: { type: string }
        label:
          type: string
          description: Already localised. Falls back through region, then English, then the field key — never blank, because a form with an unlabelled field is unusable.
        type:
          type: string
          enum: [TEXT, MULTILINE_TEXT, NUMBER, DATE, DROPDOWN, MULTI_SELECT, RADIO, CHECKBOX, ATTACHMENT, EMPLOYEE, REFERENCE, EMAIL, PHONE]
        required: { type: boolean, default: false }
        editable:
          type: boolean
          default: true
          description: |
            False when the caller may see the value but not change it. Distinct from omitting the
            field — a read-only salary grade still belongs on the profile, it simply is not the
            employee's to edit.
        helpText: { type: string }
        validation:
          $ref: '#/components/schemas/FieldValidation'
        options:
          type: array
          description: For DROPDOWN, MULTI_SELECT and RADIO.
          items:
            $ref: '#/components/schemas/FieldOption'
        referenceTable:
          type: string
          description: |
            For REFERENCE fields: which taxonomy supplies the options, e.g. `nationality`. The
            client already caches these, so the schema names the taxonomy rather than inlining
            several hundred options.
        custom:
          type: boolean
          default: false
          description: True for tenant-defined fields. Clients need not care; support does.

    FieldValidation:
      type: object
      description: |
        Client-side rules, for immediate feedback only. The server re-validates everything —
        anything arriving at the API may have come from a modified app, a replayed request or a
        bulk import.
      properties:
        minLength: { type: integer }
        maxLength: { type: integer }
        min: { type: number }
        max: { type: number }
        pattern:
          type: string
          description: Applied in the client's own regex engine. A malformed pattern is ignored rather than rejecting the user's input — they cannot fix a pattern they cannot see.
        patternMessage: { type: string }

    FieldOption:
      type: object
      required: [value, label]
      properties:
        value: { type: string }
        label: { type: string }

    DirectoryPage:
      type: object
      required: [items, hasMore]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/DirectoryEntry'
        nextCursor:
          type: string
          description: Pass as `cursor` on the next call. Absent when there are no more results.
        hasMore:
          type: boolean

    DirectoryEntry:
      type: object
      required: [id, employeeCode, displayName]
      description: |
        A colleague, as visible to any authenticated employee.

        Deliberately narrow. Salary, bank details, identity documents, date of birth and home
        address are absent — and absent from the *query*, not filtered afterwards. A serialisation
        filter someone forgets to apply leaks; a column that was never selected cannot.
      properties:
        id: { type: string, format: uuid }
        employeeCode: { type: string }
        displayName:
          type: string
          description: |
            Stored rather than derived from first and last name. Naming order differs by market,
            and several target countries use a mononym or patronymic that does not reconstruct
            from a first/last pair.
        preferredName: { type: string }
        designation: { type: string }
        department: { type: string }
        location: { type: string }
        workEmail: { type: string, format: email }
        mobile: { type: string }
        workPhone: { type: string }
        photoKey:
          type: string
          description: Object storage key. Exchange for a short-lived signed URL to display.
        supervisorId:
          type: string
          format: uuid
          description: The solid reporting line. Approval routing follows this and only this.

    MfaVerifyRequest:
      type: object
      required: [mfaToken, code, device]
      properties:
        mfaToken:
          type: string
          description: From `details.mfaToken` on the `MFA_REQUIRED` response. Valid for five minutes.
        code:
          type: string
          description: |
            A six-digit code from the authenticator app, or a recovery code. Separators, spaces and
            case are ignored — the user is retyping from paper or from a small screen.
        device:
          $ref: '#/components/schemas/DeviceInfo'

    MfaCodeRequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          description: A current TOTP code, or a recovery code.

    MfaEnrolment:
      type: object
      required: [secret, provisioningUri]
      properties:
        secret:
          type: string
          description: |
            Base32, 160 bits. Show it as text beneath the QR code — a user whose camera will not
            focus needs to be able to type it in.
        provisioningUri:
          type: string
          description: |
            `otpauth://totp/...` — render as a QR code. Carries the issuer twice, as a label prefix
            and as a parameter, because authenticator apps disagree about which they read and an
            unlabelled entry in a list of thirty accounts is unidentifiable.

    MfaStatus:
      type: object
      required: [enabled, enrolmentPending, recoveryCodesRemaining]
      properties:
        enabled: { type: boolean }
        enrolmentPending:
          type: boolean
          description: A secret has been issued but not yet confirmed.
        recoveryCodesRemaining:
          type: integer
          description: Prompt the user to regenerate as this approaches zero.

    RecoveryCodesResponse:
      type: object
      required: [recoveryCodes]
      properties:
        recoveryCodes:
          type: array
          description: |
            Shown **once**. Stored only as hashes, so they cannot be retrieved again — a client
            that does not display them here has lost them for that user.
          items: { type: string }

    EmployeeProfile:
      type: object
      required: [id, version]
      description: |
        A full employee record, **filtered to what the caller may see**.

        Only `id` and `version` are guaranteed present. Every other property is absent when the
        caller lacks access to it — absent, not null, because a null would confirm the field
        exists and they are not allowed it, and would have clients rendering disabled inputs for
        values they should not know about. Clients must therefore treat every field as optional
        and render from what arrived, never from a fixed list.

        A masked field is present as a redacted string (`••••4567`). The true value never leaves
        the server, so it is not in the HTTP cache, the client database, or any log capturing
        payloads.

        `customFields` holds tenant-defined values, permission-checked individually under their
        own keys and described by `/v1/employees/{id}/form`. The container is omitted entirely
        when nothing in it is visible.
      properties:
        id: { type: string, format: uuid }
        version:
          type: integer
          format: int64
          description: Send back as `If-Match` on update to detect a concurrent edit.
        employeeCode: { type: string }
        status:
          type: string
          enum: [PENDING_JOIN, PROBATION, ACTIVE, SUSPENDED, ON_NOTICE, EXITED]
          description: Read-only here. Changing it is the joiner/leaver process, which has its own approvals and side effects.
        displayName: { type: string }
        firstName: { type: string }
        middleName: { type: string }
        lastName: { type: string }
        preferredName: { type: string }
        photoKey: { type: string }
        titleId: { type: string, format: uuid }
        dateOfBirth: { type: string, format: date }
        genderTypeId: { type: string, format: uuid }
        maritalStatusId: { type: string, format: uuid }
        bloodGroupId: { type: string, format: uuid }
        nationalityId: { type: string, format: uuid }
        religionId: { type: string, format: uuid }
        raceId: { type: string, format: uuid }
        workEmail: { type: string }
        personalEmail: { type: string }
        mobile: { type: string }
        workPhone: { type: string }
        permanentAddress: { $ref: '#/components/schemas/Address' }
        currentAddress: { $ref: '#/components/schemas/Address' }
        companyId: { type: string, format: uuid }
        joinDate: { type: string, format: date }
        confirmationDate: { type: string, format: date }
        probationEndDate: { type: string, format: date }
        resignDate: { type: string, format: date }
        lastWorkingDate: { type: string, format: date }
        employmentTypeId: { type: string, format: uuid }
        employeeCategoryId: { type: string, format: uuid }
        employeeGroupId: { type: string, format: uuid }
        statutoryClassificationId: { type: string, format: uuid }
        departmentId: { type: string, format: uuid }
        designationId: { type: string, format: uuid }
        salaryGradeId: { type: string, format: uuid }
        corporateTitleId: { type: string, format: uuid }
        locationId: { type: string, format: uuid }
        costCentreId: { type: string, format: uuid }
        functionId: { type: string, format: uuid }
        supervisorId:
          type: string
          format: uuid
          description: The solid reporting line. Approval routing follows this and only this.
        dottedLineSupervisorId:
          type: string
          format: uuid
          description: Matrix reporting. Informational — never used for approval routing.
        yearsOfService:
          type: integer
          description: |
            Completed years as at today, stopping at `lastWorkingDate` for a leaver. Drives
            service-based leave slabs and gratuity, both of which are money — hence a server-side
            value rather than something each client computes from `joinDate`.
        customFields:
          type: object
          additionalProperties: true
          description: Tenant-defined values, keyed by field key. Omitted when none are visible.

    EmployeeUpdate:
      type: object
      description: |
        A partial update. Only the properties present are changed; absent ones are untouched.

        To clear a field, name it in `clearFields`. Sending an explicit `null` also works and
        means the same thing, but generated typed clients cannot express it: kotlinx and Swift
        both encode "unset" and "deliberately null" identically, so the encoder must omit nulls
        or every partial update would carry one for each field the caller did not touch.
        `clearFields` is the spelling that survives code generation.

        An empty string also clears a text field — storing `""` would make "not set" and "set to
        nothing" two distinct states that every query downstream then has to handle.

        `status`, `resignDate` and `lastWorkingDate` are absent by design. They are outcomes of
        the joiner/leaver processes, which carry approvals and side effects — payroll cut-off,
        access revocation, final settlement. A profile update that could set `status: EXITED`
        would skip all of it and leave someone paid but locked out, or the reverse.
      properties:
        clearFields:
          type: array
          description: |
            Field keys to set to null. Subject to the same write permissions as setting a value —
            clearing someone's department is a change like any other.

            A key that also appears with a value in the same request is rejected rather than
            resolved by precedence: the two instructions contradict each other, and guessing which
            the caller meant is how a save silently does the opposite of what was intended.
          items: { type: string }
        employeeCode: { type: string, maxLength: 64 }
        firstName: { type: string, maxLength: 128 }
        middleName: { type: [string, "null"], maxLength: 128 }
        lastName: { type: string, maxLength: 128 }
        displayName: { type: string, maxLength: 255 }
        preferredName: { type: [string, "null"], maxLength: 128 }
        photoKey: { type: [string, "null"], maxLength: 512 }
        titleId: { type: [string, "null"], format: uuid }
        dateOfBirth: { type: [string, "null"], format: date }
        genderTypeId: { type: [string, "null"], format: uuid }
        maritalStatusId: { type: [string, "null"], format: uuid }
        bloodGroupId: { type: [string, "null"], format: uuid }
        nationalityId: { type: [string, "null"], format: uuid }
        religionId: { type: [string, "null"], format: uuid }
        raceId: { type: [string, "null"], format: uuid }
        workEmail: { type: [string, "null"], maxLength: 320 }
        personalEmail: { type: [string, "null"], maxLength: 320 }
        mobile: { type: [string, "null"], maxLength: 32 }
        workPhone: { type: [string, "null"], maxLength: 32 }
        permanentAddress: { $ref: '#/components/schemas/Address' }
        currentAddress: { $ref: '#/components/schemas/Address' }
        companyId: { type: string, format: uuid }
        joinDate: { type: string, format: date }
        confirmationDate: { type: [string, "null"], format: date }
        probationEndDate: { type: [string, "null"], format: date }
        employmentTypeId: { type: [string, "null"], format: uuid }
        employeeCategoryId: { type: [string, "null"], format: uuid }
        employeeGroupId: { type: [string, "null"], format: uuid }
        statutoryClassificationId: { type: [string, "null"], format: uuid }
        departmentId: { type: [string, "null"], format: uuid }
        designationId: { type: [string, "null"], format: uuid }
        salaryGradeId: { type: [string, "null"], format: uuid }
        corporateTitleId: { type: [string, "null"], format: uuid }
        locationId: { type: [string, "null"], format: uuid }
        costCentreId: { type: [string, "null"], format: uuid }
        functionId: { type: [string, "null"], format: uuid }
        supervisorId: { type: [string, "null"], format: uuid }
        dottedLineSupervisorId: { type: [string, "null"], format: uuid }
        customFields:
          type: object
          additionalProperties: true
          description: |
            Tenant-defined values. A `null` removes the value. Every key is checked against the
            active definitions — an unknown key is rejected rather than ignored, because silently
            dropping something the user typed is worse than refusing it.

    Address:
      type: object
      description: |
        Free-form because address structure is not universal. A schema with `street`, `city`,
        `state`, `zip` is a US postal address wearing a generic name, and it does not survive
        contact with a Sri Lankan or Japanese address. The keys a tenant uses come from their
        configured address format.
      additionalProperties: true

    ReferenceItem:
      type: object
      required: [id, code, name, sequence, active]
      description: A row in a configurable lookup list — blood group, marital status, and so on.
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        sequence:
          type: integer
          description: Display order. Lower first.
        active:
          type: boolean
          description: Inactive items are retained so historical records still resolve, but are not offered for new entry.

    NotificationSettings:
      type: object
      required: [preferences]
      properties:
        preferences:
          type: array
          items:
            $ref: '#/components/schemas/ChannelPreference'
          description: |
            Only entries differing from the default are stored and returned. An absent
            (event, channel) pair is at its default: in-app, push and email on, SMS off.
        quietHours:
          $ref: '#/components/schemas/QuietHoursSettings'

    ChannelPreference:
      type: object
      required: [eventKey, channel, enabled]
      properties:
        eventKey:
          type: string
          maxLength: 128
          description: The event this applies to, e.g. `leave.approved`.
        channel:
          $ref: '#/components/schemas/NotificationChannel'
        enabled: { type: boolean }
        digestMode:
          type: string
          enum: [IMMEDIATE, DIGEST]
          default: IMMEDIATE
          description: |
            DIGEST batches the event into one daily summary. If any channel for an event is set to
            digest, the whole event is digested — a summary cannot deliver one channel sooner than
            another, so the stricter reading wins and nothing arrives more often than asked.

    NotificationChannel:
      type: string
      enum: [PUSH, EMAIL, IN_APP, SMS]

    QuietHoursSettings:
      type: object
      required: [startAt, endAt, timezone]
      properties:
        startAt:
          type: string
          format: time
          example: '22:00:00'
          description: Local wall-clock time. A `startAt` after `endAt` is a window crossing midnight.
        endAt:
          type: string
          format: time
          example: '07:00:00'
        timezone:
          type: string
          example: Asia/Colombo
          description: |
            IANA zone name, not an offset. An offset is wrong twice a year wherever daylight saving
            applies, and the symptom is notifications arriving an hour into the night for months.
            Rejected with `VALIDATION_FAILED` if the server cannot resolve it.
        days:
          type: array
          items: { type: integer, minimum: 1, maximum: 7 }
          description: |
            Weekdays the window applies to, 1 = Monday. Empty means every day. For a window
            crossing midnight the day is the one the window *opened* on, so `[5]` with 22:00–07:00
            covers Friday night into Saturday morning.
        enabled: { type: boolean, default: true }

    MeResponse:
      type: object
      required: [userId, username, tenant, roles, permissions, enabledModules]
      properties:
        userId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        username: { type: string }
        displayName: { type: string }
        email: { type: string, format: email }
        photoUrl: { type: string, format: uri }
        locale: { type: string }
        timezone: { type: string }
        tenant:
          $ref: '#/components/schemas/TenantSummary'
        roles:
          type: array
          items: { type: string }
          description: Role keys held by the user, e.g. `ADMIN`, `HR_ADMIN`.
        permissions:
          type: array
          items: { type: string }
          description: Effective permission keys. The UI hides what the caller cannot do; the API enforces it regardless.
        enabledModules:
          type: array
          items: { type: string }
        mustChangePassword: { type: boolean }
        mfaEnabled: { type: boolean }

    TenantSummary:
      type: object
      required: [id, code, name]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        logoUrl: { type: string, format: uri }
        brandColor: { type: string }
        defaultCurrency: { type: string }
        timezone: { type: string }

    DashboardResponse:
      type: object
      required: [asOf, greeting, widgets, quickActions]
      properties:
        asOf:
          type: string
          format: date-time
        greeting:
          type: string
          description: Contextual greeting for the user (e.g. Good morning, Alex)
        widgets:
          type: array
          items:
            $ref: '#/components/schemas/DashboardWidget'
        quickActions:
          type: array
          items:
            $ref: '#/components/schemas/QuickActionItem'

    DashboardWidget:
      type: object
      required: [key, title, category, value, subtext, deepLink]
      properties:
        key:
          type: string
        title:
          type: string
        category:
          type: string
          enum: [METRIC, APPROVALS, ACTION, ALERT]
        value:
          type: string
        subtext:
          type: string
        trend:
          type: string
          nullable: true
        status:
          type: string
          enum: [NORMAL, WARNING, CRITICAL, SUCCESS]
        deepLink:
          type: string
        permission:
          type: string
          nullable: true

    MobileHomeResponse:
      type: object
      required: [cards]
      properties:
        syncCursor:
          type: string
          nullable: true
          description: Opaque sync cursor for the home scope.
        cards:
          type: array
          items:
            $ref: '#/components/schemas/HomeCard'

    HomeCard:
      type: object
      required: [id, type, title, priority, payload]
      properties:
        id:
          type: string
        type:
          type: string
          description: |
            Card kind identifier. Open string rather than closed enum so new cards do not crash older clients.
            Known values: PENDING_APPROVALS, MILESTONES, ANNOUNCEMENTS, QUICK_ACTIONS, EXPIRING_DOCUMENTS.
        title:
          type: string
        priority:
          type: integer
          description: Lower number displays first.
        payload:
          type: object
          additionalProperties: true
          description: Card-specific data structure.

    CountedEntity:
      type: object
      required: [entityType, entityId]
      properties:
        entityType:
          type: string
        entityId:
          type: string

    PendingApprovalsPayload:
      type: object
      required: [totalCount, countIsApproximate, countedEntities, leaveCount, attendanceCount, claimsCount, actionUri]
      properties:
        totalCount: { type: integer }
        countIsApproximate: { type: boolean }
        countedEntities:
          type: array
          items:
            $ref: '#/components/schemas/CountedEntity'
        leaveCount: { type: integer }
        attendanceCount: { type: integer }
        claimsCount: { type: integer }
        actionUri: { type: string }

    MilestoneItem:
      type: object
      required: [employeeId, displayName, type, eventDate]
      properties:
        employeeId: { type: string, format: uuid }
        displayName: { type: string }
        designation: { type: string, nullable: true }
        photoKey: { type: string, nullable: true }
        type:
          type: string
          description: BIRTHDAY or WORK_ANNIVERSARY
        eventDate: { type: string, format: date }
        yearsCount: { type: integer, nullable: true }
        actionUri: { type: string, nullable: true }

    AnnouncementItem:
      type: object
      required: [id, title, summary, publishedAt, priority, authorName]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        summary: { type: string }
        publishedAt: { type: string, format: date-time }
        priority: { type: string }
        authorName: { type: string }
        actionUri: { type: string, nullable: true }

    QuickActionItem:
      type: object
      required: [key, label, icon, actionUri]
      properties:
        key: { type: string }
        label: { type: string }
        icon: { type: string }
        actionUri: { type: string }

    ExpiringDocumentItem:
      type: object
      required: [id, docType, expiryDate, daysRemaining, status]
      properties:
        id: { type: string, format: uuid }
        docType: { type: string }
        expiryDate: { type: string, format: date }
        daysRemaining: { type: integer }
        status: { type: string }
        actionUri: { type: string, nullable: true }

    TodayAttendanceResponse:
      type: object
      required: [employeeId, workDate, dayStatus, currentStatus, workedMinutes, punches, geofence]
      properties:
        employeeId: { type: string, format: uuid }
        workDate: { type: string, format: date }
        dayStatus:
          type: string
          description: NOT_CHECKED_IN, PRESENT, HALF_DAY, ON_LEAVE, REST_DAY, HOLIDAY
        currentStatus:
          type: string
          description: NOT_CHECKED_IN, CHECKED_IN, ON_BREAK, CHECKED_OUT
        shift:
          $ref: '#/components/schemas/AttendanceShiftInfo'
        firstInAt: { type: string, format: date-time, nullable: true }
        lastOutAt: { type: string, format: date-time, nullable: true }
        workedMinutes: { type: integer }
        punches:
          type: array
          items:
            $ref: '#/components/schemas/AttendancePunchItem'
        geofence:
          $ref: '#/components/schemas/BranchGeofenceInfo'

    AttendanceShiftInfo:
      type: object
      required: [id, code, name, startTime, endTime, graceInMinutes, breakMinutes]
      properties:
        id: { type: string }
        code: { type: string }
        name: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        graceInMinutes: { type: integer }
        breakMinutes: { type: integer }

    BranchGeofenceInfo:
      type: object
      required: [branchName, latitude, longitude, radiusMeters]
      properties:
        branchName: { type: string }
        latitude: { type: number }
        longitude: { type: number }
        radiusMeters: { type: number }

    AttendancePunchItem:
      type: object
      required: [id, punchType, punchedAt, source, geofenceStatus, isMockLocation]
      properties:
        id: { type: string }
        punchType:
          type: string
          enum: [IN, OUT, BREAK_IN, BREAK_OUT]
        punchedAt: { type: string, format: date-time }
        source: { type: string }
        geofenceStatus:
          type: string
          enum: [INSIDE, OUTSIDE, UNKNOWN, NOT_APPLICABLE]
        isMockLocation: { type: boolean }
        locationName: { type: string, nullable: true }

    AttendancePunchRequest:
      type: object
      required: [punchType, punchedAt, source]
      properties:
        punchType:
          type: string
          enum: [IN, OUT, BREAK_IN, BREAK_OUT]
        punchedAt: { type: string, format: date-time }
        source:
          type: string
          enum: [MOBILE_APP, BIOMETRIC_DEVICE, WEB_PORTAL, KIOSK]
        geoLat: { type: number, nullable: true }
        geoLng: { type: number, nullable: true }
        geoAccuracyM: { type: number, nullable: true }
        isMockLocation: { type: boolean, default: false }
        deviceId: { type: string, nullable: true }
        locationName: { type: string, nullable: true }

    AttendancePunchResponse:
      type: object
      required: [id, punchType, punchedAt, geofenceStatus, isMockLocation, message]
      properties:
        id: { type: string }
        punchType: { type: string }
        punchedAt: { type: string, format: date-time }
        geofenceStatus: { type: string }
        isMockLocation: { type: boolean }
        message: { type: string }

    PendingApprovalsResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalItem'
        totalCount:
          type: integer

    ApprovalItem:
      type: object
      required: [id, type, requesterId, requesterName, submittedAt, title, summary, status, urgency]
      properties:
        id: { type: string }
        type:
          type: string
          description: LEAVE_APPLICATION or ATTENDANCE_REGULARISATION
        requesterId: { type: string, format: uuid }
        requesterName: { type: string }
        requesterDesignation: { type: string, nullable: true }
        requesterPhotoKey: { type: string, nullable: true }
        departmentName: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time }
        title: { type: string }
        summary: { type: string }
        status: { type: string }
        urgency:
          type: string
          enum: [NORMAL, URGENT, OVERDUE]
        leaveDetails:
          $ref: '#/components/schemas/LeaveApprovalDetails'
        attendanceDetails:
          $ref: '#/components/schemas/AttendanceApprovalDetails'

    LeaveApprovalDetails:
      type: object
      required: [leaveTypeName, startDate, endDate, workingDays, reason, employeeBalanceDays]
      properties:
        leaveTypeName: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        workingDays: { type: number }
        reason: { type: string }
        employeeBalanceDays: { type: number }
        teamCoverageWarning: { type: string, nullable: true }

    AttendanceApprovalDetails:
      type: object
      required: [workDate, requestedPunchType, requestedTime, reason]
      properties:
        workDate: { type: string, format: date }
        requestedPunchType: { type: string }
        requestedTime: { type: string }
        shiftName: { type: string, nullable: true }
        reason: { type: string }

    ApprovalDecisionRequest:
      type: object
      required: [decision]
      properties:
        decision:
          type: string
          enum: [APPROVE, REJECT]
        remarks:
          type: string
          nullable: true

    ApprovalDecisionResponse:
      type: object
      required: [id, status, decidedAt, message]
      properties:
        id: { type: string }
        status: { type: string }
        decidedAt: { type: string, format: date-time }
        message: { type: string }

    EmployeeDocumentItem:
      type: object
      required: [id, docType, docNumberMasked, status, hasAttachment]
      properties:
        id: { type: string, format: uuid }
        docType:
          type: string
          description: PASSPORT, VISA, WORK_PERMIT, LABOUR_CARD, DRIVING_LICENCE, NATIONAL_ID, OTHER
        docNumberMasked: { type: string }
        issueDate: { type: string, format: date, nullable: true }
        expiryDate: { type: string, format: date, nullable: true }
        issuingCountry: { type: string, nullable: true }
        daysRemaining: { type: integer, nullable: true }
        status:
          type: string
          description: VALID, EXPIRING, EXPIRED, REPLACED
        hasAttachment: { type: boolean }
        attachmentKey: { type: string, nullable: true }

    RenewDocumentRequest:
      type: object
      required: [docType, docNumber, expiryDate]
      properties:
        docType: { type: string }
        docNumber: { type: string }
        issueDate: { type: string, format: date, nullable: true }
        expiryDate: { type: string, format: date }
        issuingCountry: { type: string, nullable: true }
        attachmentKey: { type: string, nullable: true }
        previousDocumentId: { type: string, format: uuid, nullable: true }

    RenewDocumentResponse:
      type: object
      required: [document, message]
      properties:
        document:
          $ref: '#/components/schemas/EmployeeDocumentItem'
        message: { type: string }

    ShiftRosterDayItem:
      type: object
      required: [date, dayOfWeek, shiftCode, shiftName, startTime, endTime, isRestDay, isHoliday, dayStatus, teamMembersOnDuty]
      properties:
        date: { type: string, format: date }
        dayOfWeek: { type: string }
        shiftCode: { type: string }
        shiftName: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        isRestDay: { type: boolean }
        isHoliday: { type: boolean }
        holidayName: { type: string, nullable: true }
        dayStatus:
          type: string
          description: SCHEDULED, ON_DUTY, COMPLETED, REST_DAY, HOLIDAY, ON_LEAVE
        teamMembersOnDuty: { type: integer }

    ShiftRosterResponse:
      type: object
      required: [startDate, endDate, days]
      properties:
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        days:
          type: array
          items:
            $ref: '#/components/schemas/ShiftRosterDayItem'

    TeamCoverageMemberItem:
      type: object
      required: [employeeId, employeeName, jobTitle, shiftCode, shiftName, status]
      properties:
        employeeId: { type: string, format: uuid }
        employeeCode: { type: string }
        employeeName: { type: string }
        jobTitle: { type: string }
        departmentName: { type: string, nullable: true }
        shiftCode: { type: string }
        shiftName: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        status:
          type: string
          description: ON_DUTY, SCHEDULED, REST_DAY, ON_LEAVE, COMPLETED

    TeamCoverageResponse:
      type: object
      required: [date, totalScheduled, totalOnDuty, members]
      properties:
        date: { type: string, format: date }
        totalScheduled: { type: integer }
        totalOnDuty: { type: integer }
        members:
          type: array
          items:
            $ref: '#/components/schemas/TeamCoverageMemberItem'

    ShiftSwapRequest:
      type: object
      required: [workDate, targetEmployeeId, targetWorkDate, reason]
      properties:
        workDate: { type: string, format: date }
        targetEmployeeId: { type: string, format: uuid }
        targetWorkDate: { type: string, format: date }
        reason: { type: string }

    ShiftSwapResponse:
      type: object
      required: [requestId, status, message]
      properties:
        requestId: { type: string }
        status: { type: string }
        message: { type: string }

    AttendanceRegulariseRequest:
      type: object
      required: [workDate, reason]
      properties:
        workDate: { type: string, format: date }
        requestedInTime: { type: string, nullable: true }
        requestedOutTime: { type: string, nullable: true }
        reason: { type: string }

    AttendanceRegulariseResponse:
      type: object
      required: [requestId, status, message]
      properties:
        requestId: { type: string }
        status: { type: string }
        message: { type: string }

    LeaveBalanceItem:
      type: object
      required: [leaveTypeId, leaveTypeCode, leaveTypeName, color, entitledDays, accruedDays, takenDays, pendingDays, availableDays]
      properties:
        leaveTypeId: { type: string }
        leaveTypeCode: { type: string }
        leaveTypeName: { type: string }
        color: { type: string }
        entitledDays: { type: number }
        accruedDays: { type: number }
        takenDays: { type: number }
        pendingDays: { type: number }
        availableDays: { type: number }

    LeaveBalancesResponse:
      type: object
      required: [leaveYear, balances]
      properties:
        leaveYear: { type: string }
        balances:
          type: array
          items:
            $ref: '#/components/schemas/LeaveBalanceItem'

    LeaveApplicationDayItem:
      type: object
      required: [date, dayOfWeek, isWorkingDay, isPublicHoliday, portion, hours]
      properties:
        date: { type: string, format: date }
        dayOfWeek: { type: string }
        isWorkingDay: { type: boolean }
        isPublicHoliday: { type: boolean }
        holidayName: { type: string, nullable: true }
        portion: { type: string, enum: [FULL_DAY, FIRST_HALF, SECOND_HALF] }
        hours: { type: number }

    LeaveApplicationItem:
      type: object
      required: [id, employeeId, employeeName, leaveTypeId, leaveTypeCode, leaveTypeName, startDate, endDate, dayPortion, totalDays, reason, status, submittedAt]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        leaveTypeId: { type: string }
        leaveTypeCode: { type: string }
        leaveTypeName: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        dayPortion: { type: string, enum: [FULL_DAY, FIRST_HALF, SECOND_HALF] }
        totalDays: { type: number }
        reason: { type: string }
        status: { type: string, enum: [SUBMITTED, APPROVED, REJECTED, CANCELLED, WITHDRAWN] }
        submittedAt: { type: string, format: date-time }
        approvedAt: { type: string, format: date-time, nullable: true }
        days:
          type: array
          items:
            $ref: '#/components/schemas/LeaveApplicationDayItem'

    LeaveApplicationsResponse:
      type: object
      required: [applications]
      properties:
        applications:
          type: array
          items:
            $ref: '#/components/schemas/LeaveApplicationItem'

    LeaveEligibilityRequest:
      type: object
      required: [leaveTypeId, startDate, endDate]
      properties:
        leaveTypeId: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        dayPortion: { type: string, enum: [FULL_DAY, FIRST_HALF, SECOND_HALF] }

    LeaveEligibilityResponse:
      type: object
      required: [eligible, workingDaysRequested, balanceAvailable, remainingAfter, reasons]
      properties:
        eligible: { type: boolean }
        workingDaysRequested: { type: number }
        balanceAvailable: { type: number }
        remainingAfter: { type: number }
        reasons:
          type: array
          items:
            type: string
        days:
          type: array
          items:
            $ref: '#/components/schemas/LeaveApplicationDayItem'

    LeaveApplicationRequest:
      type: object
      required: [leaveTypeId, startDate, endDate, reason]
      properties:
        leaveTypeId: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        dayPortion: { type: string, enum: [FULL_DAY, FIRST_HALF, SECOND_HALF] }
        reason: { type: string }

    CancelLeaveRequest:
      type: object
      properties:
        reason: { type: string }

    LeaveLedgerEntryItem:
      type: object
      required: [id, date, leaveTypeId, leaveTypeCode, leaveTypeName, eventType, daysCredited, daysDebited, balanceAfter]
      properties:
        id: { type: string }
        date: { type: string, format: date }
        leaveTypeId: { type: string }
        leaveTypeCode: { type: string }
        leaveTypeName: { type: string }
        eventType: { type: string, enum: [OPENING, ACCRUAL, TAKEN, ADJUSTMENT, CANCELLATION, FORFEITURE] }
        daysCredited: { type: number }
        daysDebited: { type: number }
        balanceAfter: { type: number }
        referenceId: { type: string, nullable: true }
        notes: { type: string, nullable: true }

    LeaveLedgerResponse:
      type: object
      required: [ledger]
      properties:
        ledger:
          type: array
          items:
            $ref: '#/components/schemas/LeaveLedgerEntryItem'

    PayslipSummaryItem:
      type: object
      required: [id, payPeriodId, periodCode, periodName, startDate, endDate, paymentDate, currency, basicSalary, grossPay, totalDeductions, netPay, paymentStatus]
      properties:
        id: { type: string, format: uuid }
        payPeriodId: { type: string, format: uuid }
        periodCode: { type: string }
        periodName: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        paymentDate: { type: string, format: date }
        currency: { type: string }
        basicSalary: { type: number }
        grossPay: { type: number }
        totalDeductions: { type: number }
        netPay: { type: number }
        paymentStatus: { type: string, enum: [PAID, PENDING, PROCESSING] }

    PayslipsResponse:
      type: object
      required: [payslips]
      properties:
        payslips:
          type: array
          items:
            $ref: '#/components/schemas/PayslipSummaryItem'

    PayslipLineItem:
      type: object
      required: [id, category, itemCode, itemName, amount, isStatutory]
      properties:
        id: { type: string }
        category: { type: string, enum: [EARNING, STATUTORY_DEDUCTION, VOLUNTARY_DEDUCTION, TAX, EMPLOYER_CONTRIBUTION] }
        itemCode: { type: string }
        itemName: { type: string }
        amount: { type: number }
        rate: { type: number, nullable: true }
        hours: { type: number, nullable: true }
        isStatutory: { type: boolean }
        calculationTrace: { type: string, nullable: true }

    PayslipDetailResponse:
      type: object
      required: [id, employeeId, employeeCode, employeeName, department, designation, payPeriodCode, payPeriodName, startDate, endDate, paymentDate, currency, basicSalary, grossPay, totalStatutoryEmployee, totalStatutoryEmployer, taxWithheld, totalVoluntaryDeductions, totalDeductions, netPay, paymentStatus, earnings, deductions, taxes, employerContributions]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeCode: { type: string }
        employeeName: { type: string }
        department: { type: string }
        designation: { type: string }
        payPeriodCode: { type: string }
        payPeriodName: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        paymentDate: { type: string, format: date }
        currency: { type: string }
        bankName: { type: string, nullable: true }
        bankAccountNumberMasked: { type: string, nullable: true }
        basicSalary: { type: number }
        grossPay: { type: number }
        totalStatutoryEmployee: { type: number }
        totalStatutoryEmployer: { type: number }
        taxWithheld: { type: number }
        totalVoluntaryDeductions: { type: number }
        totalDeductions: { type: number }
        netPay: { type: number }
        paymentStatus: { type: string }
        earnings:
          type: array
          items:
            $ref: '#/components/schemas/PayslipLineItem'
        deductions:
          type: array
          items:
            $ref: '#/components/schemas/PayslipLineItem'
        taxes:
          type: array
          items:
            $ref: '#/components/schemas/PayslipLineItem'
        employerContributions:
          type: array
          items:
            $ref: '#/components/schemas/PayslipLineItem'

    PayslipComparisonLine:
      type: object
      required: [itemCode, itemName, category, currentAmount, priorAmount, varianceAmount, variancePercent]
      properties:
        itemCode: { type: string }
        itemName: { type: string }
        category: { type: string }
        currentAmount: { type: number }
        priorAmount: { type: number }
        varianceAmount: { type: number }
        variancePercent: { type: number }

    PayslipComparisonResponse:
      type: object
      required: [currentPeriodCode, priorPeriodCode, currentGross, priorGross, grossVariance, grossVariancePercent, currentNet, priorNet, netVariance, netVariancePercent, lines]
      properties:
        currentPeriodCode: { type: string }
        priorPeriodCode: { type: string }
        currentGross: { type: number }
        priorGross: { type: number }
        grossVariance: { type: number }
        grossVariancePercent: { type: number }
        currentNet: { type: number }
        priorNet: { type: number }
        netVariance: { type: number }
        netVariancePercent: { type: number }
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PayslipComparisonLine'

    LoanTypeItem:
      type: object
      required: [id, code, name, interestMethod, annualInterestRate, minTenureMonths, maxTenureMonths, minPrincipal, maxPrincipal, salaryMultipleLimit, minServiceMonths, maxActiveLoans]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        interestMethod: { type: string, enum: [ZERO_INTEREST, REDUCING_BALANCE, FLAT_RATE] }
        annualInterestRate: { type: number }
        minTenureMonths: { type: integer }
        maxTenureMonths: { type: integer }
        minPrincipal: { type: number }
        maxPrincipal: { type: number }
        salaryMultipleLimit: { type: number }
        minServiceMonths: { type: integer }
        maxActiveLoans: { type: integer }

    LoanTypesResponse:
      type: object
      required: [types]
      properties:
        types:
          type: array
          items:
            $ref: '#/components/schemas/LoanTypeItem'

    LoanRepaymentScheduleItem:
      type: object
      required: [installmentNumber, dueDate, principalAmount, interestAmount, totalInstallment, status]
      properties:
        installmentNumber: { type: integer }
        dueDate: { type: string, format: date }
        principalAmount: { type: number }
        interestAmount: { type: number }
        totalInstallment: { type: number }
        status: { type: string, enum: [PENDING, DEDUCTED, WAIVED] }
        deductedDate: { type: string, format: date, nullable: true }

    EmployeeLoanItem:
      type: object
      required: [id, loanCode, loanTypeId, loanTypeCode, loanTypeName, principalAmount, interestMethod, annualInterestRate, tenureMonths, monthlyInstallment, totalInterest, totalRepayable, totalRepaid, remainingBalance, reason, status]
      properties:
        id: { type: string, format: uuid }
        loanCode: { type: string }
        loanTypeId: { type: string, format: uuid }
        loanTypeCode: { type: string }
        loanTypeName: { type: string }
        principalAmount: { type: number }
        interestMethod: { type: string, enum: [ZERO_INTEREST, REDUCING_BALANCE, FLAT_RATE] }
        annualInterestRate: { type: number }
        tenureMonths: { type: integer }
        monthlyInstallment: { type: number }
        totalInterest: { type: number }
        totalRepayable: { type: number }
        totalRepaid: { type: number }
        remainingBalance: { type: number }
        reason: { type: string }
        status: { type: string, enum: [SUBMITTED, APPROVED, ACTIVE, SETTLED, REJECTED, CANCELLED] }
        disbursedDate: { type: string, format: date, nullable: true }
        settledDate: { type: string, format: date, nullable: true }

    EmployeeLoansResponse:
      type: object
      required: [totalOutstandingBalance, activeLoansCount, loans]
      properties:
        totalOutstandingBalance: { type: number }
        activeLoansCount: { type: integer }
        nextRepaymentDate: { type: string, format: date, nullable: true }
        nextRepaymentAmount: { type: number, nullable: true }
        loans:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeLoanItem'

    EmployeeLoanDetailResponse:
      type: object
      required: [loan, schedule]
      properties:
        loan:
          $ref: '#/components/schemas/EmployeeLoanItem'
        schedule:
          type: array
          items:
            $ref: '#/components/schemas/LoanRepaymentScheduleItem'

    LoanEligibilityRequest:
      type: object
      required: [loanTypeId, principalAmount, tenureMonths]
      properties:
        loanTypeId: { type: string, format: uuid }
        principalAmount: { type: number }
        tenureMonths: { type: integer }

    LoanEligibilityResponse:
      type: object
      required: [eligible, maxAllowedPrincipal, projectedMonthlyInstallment, projectedTotalInterest, projectedTotalRepayable, reasons]
      properties:
        eligible: { type: boolean }
        maxAllowedPrincipal: { type: number }
        projectedMonthlyInstallment: { type: number }
        projectedTotalInterest: { type: number }
        projectedTotalRepayable: { type: number }
        reasons:
          type: array
          items: { type: string }

    LoanApplicationRequest:
      type: object
      required: [loanTypeId, principalAmount, tenureMonths, reason]
      properties:
        loanTypeId: { type: string, format: uuid }
        principalAmount: { type: number }
        tenureMonths: { type: integer }
        reason: { type: string }

    LoanSettlementRequest:
      type: object
      properties:
        settlementNotes: { type: string, nullable: true }

    LoanSettlementResponse:
      type: object
      required: [loanId, settledDate, settlementAmountPaid, remainingBalance, status]
      properties:
        loanId: { type: string, format: uuid }
        settledDate: { type: string, format: date }
        settlementAmountPaid: { type: number }
        remainingBalance: { type: number }
        status: { type: string }

    ExpenseCategoryItem:
      type: object
      required: [id, code, name, requiresReceipt, isActive]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        glCode: { type: string, nullable: true }
        requiresReceipt: { type: boolean }
        maxAmountPerClaim: { type: number, nullable: true }
        ratePerUnit: { type: number, nullable: true }
        unitName: { type: string, nullable: true }
        isActive: { type: boolean }

    ExpenseCategoriesResponse:
      type: object
      required: [categories]
      properties:
        categories:
          type: array
          items:
            $ref: '#/components/schemas/ExpenseCategoryItem'

    ExpenseClaimLineItem:
      type: object
      required: [id, categoryId, categoryCode, categoryName, expenseDate, description, amount, status]
      properties:
        id: { type: string, format: uuid }
        categoryId: { type: string, format: uuid }
        categoryCode: { type: string }
        categoryName: { type: string }
        expenseDate: { type: string, format: date }
        description: { type: string }
        merchantName: { type: string, nullable: true }
        unitQuantity: { type: number, nullable: true }
        amount: { type: number }
        receiptKey: { type: string, nullable: true }
        receiptUrl: { type: string, nullable: true }
        status: { type: string, enum: [PENDING, APPROVED, REJECTED] }
        approvedAmount: { type: number, nullable: true }
        rejectionReason: { type: string, nullable: true }
        ocrExtractedSummary: { type: string, nullable: true }

    ExpenseClaimLineInput:
      type: object
      required: [categoryId, expenseDate, description, amount]
      properties:
        categoryId: { type: string, format: uuid }
        expenseDate: { type: string, format: date }
        description: { type: string }
        merchantName: { type: string, nullable: true }
        unitQuantity: { type: number, nullable: true }
        amount: { type: number }
        receiptKey: { type: string, nullable: true }
        receiptUrl: { type: string, nullable: true }

    ExpenseClaimItem:
      type: object
      required: [id, claimNumber, title, claimDate, currency, totalAmount, status, lineCount]
      properties:
        id: { type: string, format: uuid }
        claimNumber: { type: string }
        title: { type: string }
        claimDate: { type: string, format: date }
        currency: { type: string }
        totalAmount: { type: number }
        approvedAmount: { type: number, nullable: true }
        status: { type: string, enum: [DRAFT, SUBMITTED, UNDER_REVIEW, APPROVED, REJECTED, REIMBURSED, CANCELLED] }
        lineCount: { type: integer }
        rejectionReason: { type: string, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        reimbursedAt: { type: string, format: date-time, nullable: true }
        remarks: { type: string, nullable: true }

    ExpenseClaimsResponse:
      type: object
      required: [totalClaimedAmount, totalApprovedAmount, totalReimbursedAmount, pendingCount, claims]
      properties:
        totalClaimedAmount: { type: number }
        totalApprovedAmount: { type: number }
        totalReimbursedAmount: { type: number }
        pendingCount: { type: integer }
        claims:
          type: array
          items:
            $ref: '#/components/schemas/ExpenseClaimItem'

    ExpenseClaimDetailResponse:
      type: object
      required: [claim, lines]
      properties:
        claim:
          $ref: '#/components/schemas/ExpenseClaimItem'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ExpenseClaimLineItem'

    ExpenseClaimSubmitRequest:
      type: object
      required: [title, claimDate, lines]
      properties:
        title: { type: string }
        claimDate: { type: string, format: date }
        currency: { type: string, default: 'LKR' }
        remarks: { type: string, nullable: true }
        lines:
          type: array
          items:
            $ref: '#/components/schemas/ExpenseClaimLineInput'

    CancelExpenseClaimRequest:
      type: object
      properties:
        cancellationReason: { type: string, nullable: true }

    ReceiptOcrRequest:
      type: object
      properties:
        receiptImageBase64: { type: string, nullable: true }
        receiptText: { type: string, nullable: true }
        fileName: { type: string, nullable: true }

    ReceiptOcrResponse:
      type: object
      required: [confidence]
      properties:
        merchantName: { type: string, nullable: true }
        expenseDate: { type: string, format: date, nullable: true }
        amount: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        suggestedCategoryCode: { type: string, nullable: true }
        confidence: { type: number }
        rawExtractedText: { type: string, nullable: true }

    BenefitCategoryItem:
      type: object
      required: [id, code, name, benefitKind, isActive]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        benefitKind: { type: string, enum: [CASH, NON_CASH, REIMBURSEMENT] }
        isActive: { type: boolean }

    BenefitPolicyItem:
      type: object
      required: [id, categoryId, categoryCode, categoryName, code, name, coverageTier, annualLimit, currency, coPayPercentage, deductibleAmount, minServiceMonths, eligibleGrades, requiresReceipt, isActive, isEligible]
      properties:
        id: { type: string, format: uuid }
        categoryId: { type: string, format: uuid }
        categoryCode: { type: string }
        categoryName: { type: string }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        coverageTier: { type: string, enum: [INDIVIDUAL, EMPLOYEE_AND_SPOUSE, FAMILY] }
        annualLimit: { type: number }
        currency: { type: string }
        coPayPercentage: { type: number }
        deductibleAmount: { type: number }
        minServiceMonths: { type: integer }
        eligibleGrades: { type: string }
        requiresReceipt: { type: boolean }
        isActive: { type: boolean }
        isEligible: { type: boolean }
        ineligibilityReason: { type: string, nullable: true }

    BenefitCatalogueResponse:
      type: object
      required: [categories, policies]
      properties:
        categories:
          type: array
          items:
            $ref: '#/components/schemas/BenefitCategoryItem'
        policies:
          type: array
          items:
            $ref: '#/components/schemas/BenefitPolicyItem'

    BenefitDependentItem:
      type: object
      required: [id, fullName, relationship, dateOfBirth, isCovered]
      properties:
        id: { type: string, format: uuid }
        fullName: { type: string }
        relationship: { type: string, enum: [SPOUSE, CHILD, PARENT] }
        dateOfBirth: { type: string, format: date }
        nationalIdOrPassport: { type: string, nullable: true }
        isCovered: { type: boolean }

    EmployeeBenefitEnrollmentItem:
      type: object
      required: [id, policyId, policyCode, policyName, categoryName, coverageTier, policyNumber, enrollmentYear, startDate, endDate, annualEntitlement, usedAmount, pendingAmount, remainingBalance, currency, status, dependents]
      properties:
        id: { type: string, format: uuid }
        policyId: { type: string, format: uuid }
        policyCode: { type: string }
        policyName: { type: string }
        categoryName: { type: string }
        coverageTier: { type: string, enum: [INDIVIDUAL, EMPLOYEE_AND_SPOUSE, FAMILY] }
        policyNumber: { type: string }
        enrollmentYear: { type: integer }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        annualEntitlement: { type: number }
        usedAmount: { type: number }
        pendingAmount: { type: number }
        remainingBalance: { type: number }
        currency: { type: string }
        status: { type: string, enum: [ACTIVE, SUSPENDED, EXPIRED, TERMINATED] }
        dependents:
          type: array
          items:
            $ref: '#/components/schemas/BenefitDependentItem'

    MyBenefitsResponse:
      type: object
      required: [totalAnnualEntitlement, totalUsedAmount, totalPendingAmount, totalRemainingBalance, enrollments]
      properties:
        totalAnnualEntitlement: { type: number }
        totalUsedAmount: { type: number }
        totalPendingAmount: { type: number }
        totalRemainingBalance: { type: number }
        enrollments:
          type: array
          items:
            $ref: '#/components/schemas/EmployeeBenefitEnrollmentItem'

    BenefitClaimItem:
      type: object
      required: [id, enrollmentId, policyName, categoryName, claimNumber, claimDate, serviceProvider, diagnosisOrReason, claimedAmount, currency, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        enrollmentId: { type: string, format: uuid }
        policyName: { type: string }
        categoryName: { type: string }
        claimNumber: { type: string }
        claimDate: { type: string, format: date }
        dependentName: { type: string, nullable: true }
        serviceProvider: { type: string }
        diagnosisOrReason: { type: string }
        invoiceNumber: { type: string, nullable: true }
        claimedAmount: { type: number }
        approvedAmount: { type: number, nullable: true }
        coPayAmount: { type: number }
        payableAmount: { type: number, nullable: true }
        currency: { type: string }
        status: { type: string, enum: [DRAFT, SUBMITTED, UNDER_REVIEW, APPROVED, REJECTED, PAID, CANCELLED] }
        receiptUrl: { type: string, nullable: true }
        receiptKey: { type: string, nullable: true }
        rejectionReason: { type: string, nullable: true }
        remarks: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    BenefitClaimsResponse:
      type: object
      required: [totalClaimedAmount, totalApprovedAmount, totalPaidAmount, pendingCount, claims]
      properties:
        totalClaimedAmount: { type: number }
        totalApprovedAmount: { type: number }
        totalPaidAmount: { type: number }
        pendingCount: { type: integer }
        claims:
          type: array
          items:
            $ref: '#/components/schemas/BenefitClaimItem'

    BenefitClaimSubmitRequest:
      type: object
      required: [enrollmentId, claimDate, serviceProvider, diagnosisOrReason, claimedAmount]
      properties:
        enrollmentId: { type: string, format: uuid }
        claimDate: { type: string, format: date }
        dependentId: { type: string, format: uuid, nullable: true }
        serviceProvider: { type: string }
        diagnosisOrReason: { type: string }
        invoiceNumber: { type: string, nullable: true }
        claimedAmount: { type: number }
        currency: { type: string, default: 'LKR' }
        receiptUrl: { type: string, nullable: true }
        receiptKey: { type: string, nullable: true }
        remarks: { type: string, nullable: true }

    CancelBenefitClaimRequest:
      type: object
      properties:
        cancellationReason: { type: string, nullable: true }

    CareerMovementItem:
      type: object
      required: [id, employeeId, movementNumber, movementType, status, requestDate, effectiveDate, initiatorId, justification, cascadeApplied, createdAt]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        movementNumber: { type: string }
        movementType: { type: string, enum: [PROMOTION, LATERAL_TRANSFER, DEMOTION, CONFIRMATION, SALARY_REVISION, DESIGNATION_CHANGE, SECONDMENT] }
        status: { type: string, enum: [DRAFT, SUBMITTED, APPROVED, SCHEDULED, APPLIED, REJECTED, CANCELLED, REVERTED] }
        requestDate: { type: string, format: date }
        effectiveDate: { type: string, format: date }
        initiatorId: { type: string, format: uuid }
        approverId: { type: string, format: uuid, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        revertedAt: { type: string, format: date-time, nullable: true }
        reversionReason: { type: string, nullable: true }
        justification: { type: string }
        remarks: { type: string, nullable: true }
        prevDepartmentId: { type: string, format: uuid, nullable: true }
        prevDepartmentName: { type: string, nullable: true }
        prevDesignationId: { type: string, format: uuid, nullable: true }
        prevDesignationName: { type: string, nullable: true }
        prevSalaryGradeId: { type: string, format: uuid, nullable: true }
        prevSalaryGradeCode: { type: string, nullable: true }
        prevLocationId: { type: string, format: uuid, nullable: true }
        prevLocationName: { type: string, nullable: true }
        prevSupervisorId: { type: string, format: uuid, nullable: true }
        prevSupervisorName: { type: string, nullable: true }
        prevBaseSalary: { type: number, nullable: true }
        prevCurrency: { type: string, nullable: true }
        newDepartmentId: { type: string, format: uuid, nullable: true }
        newDepartmentName: { type: string, nullable: true }
        newDesignationId: { type: string, format: uuid, nullable: true }
        newDesignationName: { type: string, nullable: true }
        newSalaryGradeId: { type: string, format: uuid, nullable: true }
        newSalaryGradeCode: { type: string, nullable: true }
        newLocationId: { type: string, format: uuid, nullable: true }
        newLocationName: { type: string, nullable: true }
        newSupervisorId: { type: string, format: uuid, nullable: true }
        newSupervisorName: { type: string, nullable: true }
        newBaseSalary: { type: number, nullable: true }
        newCurrency: { type: string, nullable: true }
        cascadeApplied: { type: boolean }
        cascadeAppliedAt: { type: string, format: date-time, nullable: true }
        cascadeSummary: { type: object, nullable: true }
        createdAt: { type: string, format: date-time }

    CareerMovementListResponse:
      type: object
      required: [totalCount, movements]
      properties:
        totalCount: { type: integer }
        movements:
          type: array
          items:
            $ref: '#/components/schemas/CareerMovementItem'

    CareerTimelineEvent:
      type: object
      required: [id, eventType, title, date, description, isMilestone]
      properties:
        id: { type: string, format: uuid }
        movementId: { type: string, format: uuid, nullable: true }
        eventType: { type: string, enum: [HIRE, CONFIRMATION, PROMOTION, LATERAL_TRANSFER, SALARY_REVISION, DESIGNATION_CHANGE, SECONDMENT, DEMOTION] }
        title: { type: string }
        date: { type: string, format: date }
        departmentName: { type: string, nullable: true }
        designationName: { type: string, nullable: true }
        salaryGradeCode: { type: string, nullable: true }
        baseSalary: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        changePercentage: { type: number, nullable: true }
        description: { type: string }
        isMilestone: { type: boolean }

    CareerTimelineResponse:
      type: object
      required: [employeeId, employeeName, currentDesignation, currentDepartment, currentGrade, joinDate, events]
      properties:
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        currentDesignation: { type: string }
        currentDepartment: { type: string }
        currentGrade: { type: string }
        joinDate: { type: string, format: date }
        confirmationDate: { type: string, format: date, nullable: true }
        events:
          type: array
          items:
            $ref: '#/components/schemas/CareerTimelineEvent'

    CareerMovementProposalRequest:
      type: object
      required: [employeeId, movementType, effectiveDate, justification]
      properties:
        employeeId: { type: string, format: uuid }
        movementType: { type: string, enum: [PROMOTION, LATERAL_TRANSFER, DEMOTION, CONFIRMATION, SALARY_REVISION, DESIGNATION_CHANGE, SECONDMENT] }
        effectiveDate: { type: string, format: date }
        justification: { type: string }
        remarks: { type: string, nullable: true }
        newDepartmentId: { type: string, format: uuid, nullable: true }
        newDesignationId: { type: string, format: uuid, nullable: true }
        newSalaryGradeId: { type: string, format: uuid, nullable: true }
        newLocationId: { type: string, format: uuid, nullable: true }
        newSupervisorId: { type: string, format: uuid, nullable: true }
        newBaseSalary: { type: number, nullable: true }
        newCurrency: { type: string, default: 'LKR' }

    CareerMovementRevertRequest:
      type: object
      required: [reversionReason]
      properties:
        reversionReason: { type: string }

    SalaryHistoryItem:
      type: object
      required: [id, employeeId, effectiveFrom, baseSalary, currency, revisionReason, isCurrent]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        careerMovementId: { type: string, format: uuid, nullable: true }
        effectiveFrom: { type: string, format: date }
        effectiveTo: { type: string, format: date, nullable: true }
        baseSalary: { type: number }
        currency: { type: string }
        changePercentage: { type: number, nullable: true }
        revisionReason: { type: string }
        isCurrent: { type: boolean }

    GrievanceGroundGroupItem:
      type: object
      required: [id, code, name]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }

    GrievanceGroundItem:
      type: object
      required: [id, groupId, code, name, severity, defaultHandlerRole, slaDays]
      properties:
        id: { type: string, format: uuid }
        groupId: { type: string, format: uuid }
        groupName: { type: string, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        severity: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        defaultHandlerRole: { type: string }
        slaDays: { type: integer }

    GrievanceGroundsResponse:
      type: object
      required: [groups, grounds]
      properties:
        groups:
          type: array
          items:
            $ref: '#/components/schemas/GrievanceGroundGroupItem'
        grounds:
          type: array
          items:
            $ref: '#/components/schemas/GrievanceGroundItem'

    GrievanceChannelItem:
      type: object
      required: [id, code, name, isConfidential]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        isConfidential: { type: boolean }

    GrievanceChannelsResponse:
      type: object
      required: [channels]
      properties:
        channels:
          type: array
          items:
            $ref: '#/components/schemas/GrievanceChannelItem'

    GrievanceItem:
      type: object
      required: [id, grievanceNumber, title, groundCode, groundName, channelName, severity, anonymous, status, raisedAt, targetResolutionDate]
      properties:
        id: { type: string, format: uuid }
        grievanceNumber: { type: string }
        title: { type: string }
        groundCode: { type: string }
        groundName: { type: string }
        channelName: { type: string }
        severity: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        anonymous: { type: boolean }
        status: { type: string, enum: [SUBMITTED, ASSIGNED, UNDER_INVESTIGATION, RESOLVED, APPEALED, CLOSED] }
        raisedAt: { type: string, format: date-time }
        targetResolutionDate: { type: string, format: date-time }
        resolvedAt: { type: string, format: date-time, nullable: true }
        resolution: { type: string, nullable: true }
        satisfactionRating: { type: integer, nullable: true }

    GrievanceListResponse:
      type: object
      required: [totalCount, pendingCount, grievances]
      properties:
        totalCount: { type: integer }
        pendingCount: { type: integer }
        grievances:
          type: array
          items:
            $ref: '#/components/schemas/GrievanceItem'

    GrievanceAppealItem:
      type: object
      required: [id, grievanceId, reason, appealedAt, status]
      properties:
        id: { type: string, format: uuid }
        grievanceId: { type: string, format: uuid }
        reason: { type: string }
        appealedAt: { type: string, format: date-time }
        status: { type: string, enum: [PENDING, UPHELD, MODIFIED, DISMISSED] }
        outcome: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }

    GrievanceDetailResponse:
      type: object
      required: [id, grievanceNumber, title, description, ground, channel, anonymous, status, raisedAt, targetResolutionDate]
      properties:
        id: { type: string, format: uuid }
        grievanceNumber: { type: string }
        title: { type: string }
        description: { type: string }
        ground:
          $ref: '#/components/schemas/GrievanceGroundItem'
        channel:
          $ref: '#/components/schemas/GrievanceChannelItem'
        anonymous: { type: boolean }
        raisedByEmployeeName: { type: string, nullable: true }
        status: { type: string, enum: [SUBMITTED, ASSIGNED, UNDER_INVESTIGATION, RESOLVED, APPEALED, CLOSED] }
        raisedAt: { type: string, format: date-time }
        targetResolutionDate: { type: string, format: date-time }
        resolvedAt: { type: string, format: date-time, nullable: true }
        resolution: { type: string, nullable: true }
        satisfactionRating: { type: integer, nullable: true }
        appeal:
          $ref: '#/components/schemas/GrievanceAppealItem'

    GrievanceSubmitRequest:
      type: object
      required: [groundId, channelId, title, description]
      properties:
        groundId: { type: string, format: uuid }
        channelId: { type: string, format: uuid }
        title: { type: string }
        description: { type: string }
        anonymous: { type: boolean, default: false }
        onBehalfOfEmployeeId: { type: string, format: uuid, nullable: true }
        attachmentKeys:
          type: array
          items: { type: string }
          nullable: true

    GrievanceAppealRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string }

    IncidentSubtypeItem:
      type: object
      required: [id, typeId, code, name]
      properties:
        id: { type: string, format: uuid }
        typeId: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }

    IncidentTypeItem:
      type: object
      required: [id, code, name, severity, subtypes]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        severity: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        subtypes:
          type: array
          items:
            $ref: '#/components/schemas/IncidentSubtypeItem'

    IncidentTypesResponse:
      type: object
      required: [types]
      properties:
        types:
          type: array
          items:
            $ref: '#/components/schemas/IncidentTypeItem'

    DisciplinaryIncidentItem:
      type: object
      required: [id, incidentNumber, employeeId, employeeName, reportedByEmployeeId, reportedByEmployeeName, typeCode, typeName, incidentDate, description, severity, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        incidentNumber: { type: string }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        reportedByEmployeeId: { type: string, format: uuid }
        reportedByEmployeeName: { type: string }
        typeCode: { type: string }
        typeName: { type: string }
        subtypeName: { type: string, nullable: true }
        incidentDate: { type: string, format: date }
        location: { type: string, nullable: true }
        description: { type: string }
        severity: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        status: { type: string, enum: [REPORTED, UNDER_INVESTIGATION, ACTION_PROPOSED, ACTION_ISSUED, APPEALED, CONCLUDED] }
        createdAt: { type: string, format: date-time }

    DisciplinaryIncidentListResponse:
      type: object
      required: [totalCount, openCount, incidents]
      properties:
        totalCount: { type: integer }
        openCount: { type: integer }
        incidents:
          type: array
          items:
            $ref: '#/components/schemas/DisciplinaryIncidentItem'

    CorrectiveActionItem:
      type: object
      required: [id, incidentId, actionType, issuedAt, issuedBy, issuedByName, title, details, status]
      properties:
        id: { type: string, format: uuid }
        incidentId: { type: string, format: uuid }
        actionType: { type: string, enum: [ORAL_WARNING, WRITTEN_WARNING, SHOW_CAUSE, CHARGE_SHEET, DOMESTIC_INQUIRY, SUSPENSION, DEMOTION, TERMINATION] }
        issuedAt: { type: string, format: date-time }
        issuedBy: { type: string, format: uuid }
        issuedByName: { type: string }
        title: { type: string }
        details: { type: string }
        responseDueDate: { type: string, format: date, nullable: true }
        employeeResponse: { type: string, nullable: true }
        respondedAt: { type: string, format: date-time, nullable: true }
        outcome: { type: string, nullable: true }
        effectiveFrom: { type: string, format: date, nullable: true }
        effectiveTo: { type: string, format: date, nullable: true }
        status: { type: string, enum: [ISSUED, RESPONDED, UNDER_REVIEW, CONFIRMED, REVOKED, APPEALED] }

    IncidentJournalEntryItem:
      type: object
      required: [id, incidentId, entry, enteredBy, enteredByName, enteredAt]
      properties:
        id: { type: string, format: uuid }
        incidentId: { type: string, format: uuid }
        entry: { type: string }
        enteredBy: { type: string, format: uuid }
        enteredByName: { type: string }
        enteredAt: { type: string, format: date-time }

    DisciplinaryAppealItem:
      type: object
      required: [id, incidentId, correctiveActionId, reason, appealedAt, status]
      properties:
        id: { type: string, format: uuid }
        incidentId: { type: string, format: uuid }
        correctiveActionId: { type: string, format: uuid }
        reason: { type: string }
        appealedAt: { type: string, format: date-time }
        outcome: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { type: string, enum: [SUBMITTED, UPHELD, REDUCED, OVERTURNED] }

    DisciplinaryIncidentDetailResponse:
      type: object
      required: [incident, correctiveActions, journalEntries, appeals]
      properties:
        incident:
          $ref: '#/components/schemas/DisciplinaryIncidentItem'
        correctiveActions:
          type: array
          items:
            $ref: '#/components/schemas/CorrectiveActionItem'
        journalEntries:
          type: array
          items:
            $ref: '#/components/schemas/IncidentJournalEntryItem'
        appeals:
          type: array
          items:
            $ref: '#/components/schemas/DisciplinaryAppealItem'

    DisciplinaryIncidentReportRequest:
      type: object
      required: [employeeId, incidentTypeId, incidentDate, description]
      properties:
        employeeId: { type: string, format: uuid }
        incidentTypeId: { type: string, format: uuid }
        subtypeId: { type: string, format: uuid, nullable: true }
        incidentDate: { type: string, format: date }
        location: { type: string, nullable: true }
        description: { type: string }
        severity: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        witnesses:
          type: array
          items: { type: string }
          nullable: true

    IssueCorrectiveActionRequest:
      type: object
      required: [actionType, title, details]
      properties:
        actionType: { type: string, enum: [ORAL_WARNING, WRITTEN_WARNING, SHOW_CAUSE, CHARGE_SHEET, DOMESTIC_INQUIRY, SUSPENSION, DEMOTION, TERMINATION] }
        title: { type: string }
        details: { type: string }
        responseDueDate: { type: string, format: date, nullable: true }
        effectiveFrom: { type: string, format: date, nullable: true }
        effectiveTo: { type: string, format: date, nullable: true }

    CorrectiveActionResponseRequest:
      type: object
      required: [employeeResponse]
      properties:
        employeeResponse: { type: string }

    AddJournalEntryRequest:
      type: object
      required: [entry]
      properties:
        entry: { type: string }

    DisciplinaryAppealRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string }

    GoalCheckInItem:
      type: object
      required: [id, goalId, previousValue, newValue, progressPercentage, checkedInBy, checkedInByName, createdAt]
      properties:
        id: { type: string, format: uuid }
        goalId: { type: string, format: uuid }
        previousValue: { type: number }
        newValue: { type: number }
        progressPercentage: { type: number }
        note: { type: string, nullable: true }
        checkedInBy: { type: string, format: uuid }
        checkedInByName: { type: string }
        createdAt: { type: string, format: date-time }

    GoalItem:
      type: object
      required: [id, employeeId, employeeName, cycleId, cycleName, title, category, weight, targetValue, currentValue, unit, startDate, dueDate, status, progressPercentage, checkIns]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        cycleId: { type: string, format: uuid }
        cycleName: { type: string }
        parentGoalId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string, enum: [ORGANIZATIONAL, DEPARTMENTAL, INDIVIDUAL, DEVELOPMENTAL] }
        weight: { type: number }
        targetValue: { type: number }
        currentValue: { type: number }
        unit: { type: string }
        startDate: { type: string, format: date }
        dueDate: { type: string, format: date }
        status: { type: string, enum: [NOT_STARTED, IN_PROGRESS, ON_TRACK, AT_RISK, COMPLETED, CANCELLED] }
        progressPercentage: { type: number }
        checkIns:
          type: array
          items:
            $ref: '#/components/schemas/GoalCheckInItem'

    GoalListResponse:
      type: object
      required: [goals, totalCount, completedCount]
      properties:
        goals:
          type: array
          items:
            $ref: '#/components/schemas/GoalItem'
        totalCount: { type: integer }
        completedCount: { type: integer }

    GoalCreateRequest:
      type: object
      required: [employeeId, cycleId, title, category, weight, targetValue, unit, startDate, dueDate]
      properties:
        employeeId: { type: string, format: uuid }
        cycleId: { type: string, format: uuid }
        parentGoalId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string, enum: [ORGANIZATIONAL, DEPARTMENTAL, INDIVIDUAL, DEVELOPMENTAL] }
        weight: { type: number }
        targetValue: { type: number }
        unit: { type: string }
        startDate: { type: string, format: date }
        dueDate: { type: string, format: date }

    GoalCheckInRequest:
      type: object
      required: [newValue]
      properties:
        newValue: { type: number }
        note: { type: string, nullable: true }

    CompetencyGroupItem:
      type: object
      required: [id, code, name]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }

    CompetencyItem:
      type: object
      required: [id, groupId, groupName, code, name, targetLevel]
      properties:
        id: { type: string, format: uuid }
        groupId: { type: string, format: uuid }
        groupName: { type: string }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        targetLevel: { type: integer }

    CompetencyFrameworkResponse:
      type: object
      required: [groups, competencies]
      properties:
        groups:
          type: array
          items:
            $ref: '#/components/schemas/CompetencyGroupItem'
        competencies:
          type: array
          items:
            $ref: '#/components/schemas/CompetencyItem'

    AppraisalSummaryItem:
      type: object
      required: [id, cycleId, cycleName, employeeId, employeeName, managerId, managerName, status, selfReviewDeadline, managerReviewDeadline]
      properties:
        id: { type: string, format: uuid }
        cycleId: { type: string, format: uuid }
        cycleName: { type: string }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        managerId: { type: string, format: uuid }
        managerName: { type: string }
        status: { type: string, enum: [NOT_STARTED, SELF_REVIEW_PENDING, SELF_REVIEW_SUBMITTED, MANAGER_REVIEW_PENDING, MANAGER_REVIEW_SUBMITTED, IN_CALIBRATION, ACKNOWLEDGED, CLOSED] }
        finalScore: { type: number, nullable: true }
        finalRating: { type: string, nullable: true }
        selfReviewDeadline: { type: string, format: date }
        managerReviewDeadline: { type: string, format: date }
        employeeAcknowledgedAt: { type: string, format: date-time, nullable: true }

    AppraisalListResponse:
      type: object
      required: [appraisals]
      properties:
        appraisals:
          type: array
          items:
            $ref: '#/components/schemas/AppraisalSummaryItem'

    AppraisalGoalItem:
      type: object
      required: [goalId, title, category, weight, progressPercentage]
      properties:
        goalId: { type: string, format: uuid }
        title: { type: string }
        category: { type: string }
        weight: { type: number }
        progressPercentage: { type: number }
        selfRating: { type: number, nullable: true }
        selfComments: { type: string, nullable: true }
        managerRating: { type: number, nullable: true }
        managerComments: { type: string, nullable: true }
        weightedScore: { type: number, nullable: true }

    AppraisalCompetencyItem:
      type: object
      required: [competencyId, code, name, groupName, targetLevel]
      properties:
        competencyId: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        groupName: { type: string }
        targetLevel: { type: integer }
        selfProficiencyLevel: { type: integer, nullable: true }
        managerProficiencyLevel: { type: integer, nullable: true }
        selfComments: { type: string, nullable: true }
        managerComments: { type: string, nullable: true }

    MraRelationshipScoreItem:
      type: object
      required: [relationship, respondentCount, averageScore]
      properties:
        relationship: { type: string }
        respondentCount: { type: integer }
        averageScore: { type: number }

    MraSummaryItem:
      type: object
      required: [totalRequests, completedRequests, relationshipBreakdown]
      properties:
        totalRequests: { type: integer }
        completedRequests: { type: integer }
        averageScore: { type: number, nullable: true }
        relationshipBreakdown:
          type: array
          items:
            $ref: '#/components/schemas/MraRelationshipScoreItem'

    AppraisalDetailResponse:
      type: object
      required: [appraisal, goals, competencies]
      properties:
        appraisal:
          $ref: '#/components/schemas/AppraisalSummaryItem'
        goals:
          type: array
          items:
            $ref: '#/components/schemas/AppraisalGoalItem'
        competencies:
          type: array
          items:
            $ref: '#/components/schemas/AppraisalCompetencyItem'
        selfOverallComments: { type: string, nullable: true }
        managerOverallComments: { type: string, nullable: true }
        calibrationNotes: { type: string, nullable: true }
        mraSummary:
          $ref: '#/components/schemas/MraSummaryItem'

    GoalRatingInput:
      type: object
      required: [goalId, rating]
      properties:
        goalId: { type: string, format: uuid }
        rating: { type: number }
        comments: { type: string, nullable: true }

    CompetencyRatingInput:
      type: object
      required: [competencyId, proficiencyLevel]
      properties:
        competencyId: { type: string, format: uuid }
        proficiencyLevel: { type: integer }
        comments: { type: string, nullable: true }

    AppraisalSelfReviewRequest:
      type: object
      required: [goalRatings, competencyRatings]
      properties:
        overallComments: { type: string, nullable: true }
        goalRatings:
          type: array
          items:
            $ref: '#/components/schemas/GoalRatingInput'
        competencyRatings:
          type: array
          items:
            $ref: '#/components/schemas/CompetencyRatingInput'

    AppraisalManagerReviewRequest:
      type: object
      required: [goalRatings, competencyRatings]
      properties:
        overallComments: { type: string, nullable: true }
        goalRatings:
          type: array
          items:
            $ref: '#/components/schemas/GoalRatingInput'
        competencyRatings:
          type: array
          items:
            $ref: '#/components/schemas/CompetencyRatingInput'

    ContinuousFeedbackItem:
      type: object
      required: [id, senderEmployeeId, senderEmployeeName, recipientEmployeeId, recipientEmployeeName, feedbackType, title, content, isPrivate, sharedWithManager, createdAt]
      properties:
        id: { type: string, format: uuid }
        senderEmployeeId: { type: string, format: uuid }
        senderEmployeeName: { type: string }
        recipientEmployeeId: { type: string, format: uuid }
        recipientEmployeeName: { type: string }
        feedbackType: { type: string, enum: [PRAISE, COACHING, ONE_ON_ONE_NOTE, CHECK_IN] }
        title: { type: string }
        content: { type: string }
        isPrivate: { type: boolean }
        sharedWithManager: { type: boolean }
        createdAt: { type: string, format: date-time }

    ContinuousFeedbackListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ContinuousFeedbackItem'

    SendFeedbackRequest:
      type: object
      required: [recipientEmployeeId, feedbackType, title, content]
      properties:
        recipientEmployeeId: { type: string, format: uuid }
        feedbackType: { type: string, enum: [PRAISE, COACHING, ONE_ON_ONE_NOTE, CHECK_IN] }
        title: { type: string }
        content: { type: string }
        isPrivate: { type: boolean, nullable: true }
        sharedWithManager: { type: boolean, nullable: true }

    VacancyItem:
      type: object
      required: [id, jobCode, title, location, employmentType, experienceLevel, currency, description, openPositions, filledPositions, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        jobCode: { type: string }
        title: { type: string }
        departmentId: { type: string, format: uuid, nullable: true }
        departmentName: { type: string, nullable: true }
        location: { type: string }
        employmentType: { type: string }
        experienceLevel: { type: string }
        minSalary: { type: number, nullable: true }
        maxSalary: { type: number, nullable: true }
        currency: { type: string }
        description: { type: string }
        requirements: { type: string, nullable: true }
        openPositions: { type: integer }
        filledPositions: { type: integer }
        targetHireDate: { type: string, format: date, nullable: true }
        closingDate: { type: string, format: date, nullable: true }
        status: { type: string, enum: [DRAFT, OPEN, ON_HOLD, CLOSED, FILLED] }
        createdAt: { type: string, format: date-time }

    VacancyListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/VacancyItem'
        totalCount: { type: integer }

    VacancyCreateRequest:
      type: object
      required: [jobCode, title, location, employmentType, experienceLevel, currency, description, openPositions]
      properties:
        jobCode: { type: string }
        title: { type: string }
        departmentId: { type: string, format: uuid, nullable: true }
        location: { type: string }
        employmentType: { type: string }
        experienceLevel: { type: string }
        minSalary: { type: number, nullable: true }
        maxSalary: { type: number, nullable: true }
        currency: { type: string }
        description: { type: string }
        requirements: { type: string, nullable: true }
        openPositions: { type: integer }
        targetHireDate: { type: string, format: date, nullable: true }
        closingDate: { type: string, format: date, nullable: true }

    CandidateItem:
      type: object
      required: [id, firstName, lastName, email, skills, createdAt]
      properties:
        id: { type: string, format: uuid }
        firstName: { type: string }
        lastName: { type: string }
        email: { type: string }
        phone: { type: string, nullable: true }
        currentCompany: { type: string, nullable: true }
        currentTitle: { type: string, nullable: true }
        yearsOfExperience: { type: number, nullable: true }
        resumeUrl: { type: string, nullable: true }
        linkedinUrl: { type: string, nullable: true }
        skills:
          type: array
          items: { type: string }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    CandidateListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CandidateItem'
        totalCount: { type: integer }

    CandidateCreateRequest:
      type: object
      required: [firstName, lastName, email]
      properties:
        firstName: { type: string }
        lastName: { type: string }
        email: { type: string }
        phone: { type: string, nullable: true }
        currentCompany: { type: string, nullable: true }
        currentTitle: { type: string, nullable: true }
        yearsOfExperience: { type: number, nullable: true }
        resumeUrl: { type: string, nullable: true }
        linkedinUrl: { type: string, nullable: true }
        skills:
          type: array
          items: { type: string }
          nullable: true
        notes: { type: string, nullable: true }

    ApplicationItem:
      type: object
      required: [id, applicationNumber, vacancyId, vacancyTitle, candidateId, candidateName, candidateEmail, stage, status, source, appliedDate, createdAt]
      properties:
        id: { type: string, format: uuid }
        applicationNumber: { type: string }
        vacancyId: { type: string, format: uuid }
        vacancyTitle: { type: string }
        candidateId: { type: string, format: uuid }
        candidateName: { type: string }
        candidateEmail: { type: string }
        stage: { type: string, enum: [APPLIED, SCREENING, INTERVIEW_ROUND_1, INTERVIEW_ROUND_2, TECHNICAL_ASSESSMENT, FINAL_INTERVIEW, OFFER_EXTENDED, OFFER_ACCEPTED, OFFER_DECLINED, REJECTED, WITHDRAWN] }
        status: { type: string, enum: [ACTIVE, ARCHIVED, HIRED, REJECTED] }
        rating: { type: number, nullable: true }
        source: { type: string }
        appliedDate: { type: string, format: date }
        rejectionReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    ApplicationListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationItem'
        totalCount: { type: integer }

    ApplicationCreateRequest:
      type: object
      required: [vacancyId, candidateId]
      properties:
        vacancyId: { type: string, format: uuid }
        candidateId: { type: string, format: uuid }
        source: { type: string, nullable: true }

    ApplicationStageUpdateRequest:
      type: object
      required: [stage]
      properties:
        stage: { type: string, enum: [APPLIED, SCREENING, INTERVIEW_ROUND_1, INTERVIEW_ROUND_2, TECHNICAL_ASSESSMENT, FINAL_INTERVIEW, OFFER_EXTENDED, OFFER_ACCEPTED, OFFER_DECLINED, REJECTED, WITHDRAWN] }
        status: { type: string, enum: [ACTIVE, ARCHIVED, HIRED, REJECTED], nullable: true }
        rejectionReason: { type: string, nullable: true }

    InterviewScorecardItem:
      type: object
      required: [id, interviewId, interviewerEmployeeId, interviewerName, overallRecommendation, submittedAt]
      properties:
        id: { type: string, format: uuid }
        interviewId: { type: string, format: uuid }
        interviewerEmployeeId: { type: string, format: uuid }
        interviewerName: { type: string }
        overallRecommendation: { type: string, enum: [STRONG_HIRE, HIRE, NEUTRAL, NO_HIRE, STRONG_NO_HIRE] }
        technicalSkillRating: { type: number, nullable: true }
        communicationRating: { type: number, nullable: true }
        problemSolvingRating: { type: number, nullable: true }
        culturalFitRating: { type: number, nullable: true }
        strengths: { type: string, nullable: true }
        weaknesses: { type: string, nullable: true }
        summaryNotes: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time }

    ScorecardSubmitRequest:
      type: object
      required: [overallRecommendation]
      properties:
        overallRecommendation: { type: string, enum: [STRONG_HIRE, HIRE, NEUTRAL, NO_HIRE, STRONG_NO_HIRE] }
        technicalSkillRating: { type: number, nullable: true }
        communicationRating: { type: number, nullable: true }
        problemSolvingRating: { type: number, nullable: true }
        culturalFitRating: { type: number, nullable: true }
        strengths: { type: string, nullable: true }
        weaknesses: { type: string, nullable: true }
        summaryNotes: { type: string, nullable: true }

    InterviewItem:
      type: object
      required: [id, applicationId, candidateName, vacancyTitle, interviewRound, title, interviewType, scheduledStart, scheduledEnd, status, interviewerEmployeeIds, scorecards]
      properties:
        id: { type: string, format: uuid }
        applicationId: { type: string, format: uuid }
        candidateName: { type: string }
        vacancyTitle: { type: string }
        interviewRound: { type: integer }
        title: { type: string }
        interviewType: { type: string, enum: [PHONE_SCREEN, TECHNICAL, SYSTEM_DESIGN, BEHAVIORAL, HR, EXECUTIVE] }
        scheduledStart: { type: string, format: date-time }
        scheduledEnd: { type: string, format: date-time }
        locationOrLink: { type: string, nullable: true }
        meetingLink: { type: string, nullable: true }
        status: { type: string, enum: [SCHEDULED, COMPLETED, CANCELLED, RESCHEDULED] }
        interviewerEmployeeIds:
          type: array
          items: { type: string, format: uuid }
        notes: { type: string, nullable: true }
        scorecards:
          type: array
          items:
            $ref: '#/components/schemas/InterviewScorecardItem'

    InterviewListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/InterviewItem'
        totalCount: { type: integer }

    InterviewScheduleRequest:
      type: object
      required: [applicationId, interviewRound, title, interviewType, scheduledStart, scheduledEnd, interviewerEmployeeIds]
      properties:
        applicationId: { type: string, format: uuid }
        interviewRound: { type: integer }
        title: { type: string }
        interviewType: { type: string, enum: [PHONE_SCREEN, TECHNICAL, SYSTEM_DESIGN, BEHAVIORAL, HR, EXECUTIVE] }
        scheduledStart: { type: string, format: date-time }
        scheduledEnd: { type: string, format: date-time }
        locationOrLink: { type: string, nullable: true }
        meetingLink: { type: string, nullable: true }
        interviewerEmployeeIds:
          type: array
          items: { type: string, format: uuid }
        notes: { type: string, nullable: true }

    OfferDetail:
      type: object
      required: [id, applicationId, offerNumber, baseSalary, currency, startDate, expiryDate, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        applicationId: { type: string, format: uuid }
        offerNumber: { type: string }
        baseSalary: { type: number }
        variableBonus: { type: number, nullable: true }
        currency: { type: string }
        startDate: { type: string, format: date }
        expiryDate: { type: string, format: date }
        offerLetterUrl: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, PENDING_APPROVAL, EXTENDED, ACCEPTED, DECLINED, WITHDRAWN] }
        approvedBy: { type: string, format: uuid, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    OfferCreateRequest:
      type: object
      required: [baseSalary, currency, startDate, expiryDate]
      properties:
        baseSalary: { type: number }
        variableBonus: { type: number, nullable: true }
        currency: { type: string }
        startDate: { type: string, format: date }
        expiryDate: { type: string, format: date }
        offerLetterUrl: { type: string, nullable: true }
        decisionNotes: { type: string, nullable: true }

    OnboardingProfileItem:
      type: object
      required: [id, code, name, active]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        departmentId: { type: string, format: uuid, nullable: true }
        departmentName: { type: string, nullable: true }
        description: { type: string, nullable: true }
        active: { type: boolean }

    OnboardingProfileListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OnboardingProfileItem'
        totalCount: { type: integer }

    OnboardingTaskItem:
      type: object
      required: [id, instanceId, title, ownerRole, dueDate, status]
      properties:
        id: { type: string, format: uuid }
        instanceId: { type: string, format: uuid }
        actionId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        ownerRole: { type: string, enum: [NEW_HIRE, BUDDY, MANAGER, IT_OPS, HR_OPS] }
        assigneeEmployeeId: { type: string, format: uuid, nullable: true }
        assigneeName: { type: string, nullable: true }
        dueDate: { type: string, format: date }
        status: { type: string, enum: [PENDING, IN_PROGRESS, COMPLETED, SKIPPED, BLOCKED] }
        completedAt: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
        attachmentUrl: { type: string, nullable: true }

    OnboardingInstanceDetail:
      type: object
      required: [id, employeeId, employeeName, profileId, profileName, joinDate, status, progressPct, tasks]
      properties:
        id: { type: string, format: uuid }
        candidateId: { type: string, format: uuid, nullable: true }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        employeeCode: { type: string, nullable: true }
        profileId: { type: string, format: uuid }
        profileName: { type: string }
        joinDate: { type: string, format: date }
        status: { type: string, enum: [NOT_STARTED, IN_PROGRESS, COMPLETED, CANCELLED] }
        progressPct: { type: number }
        buddyEmployeeId: { type: string, format: uuid, nullable: true }
        buddyName: { type: string, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/OnboardingTaskItem'

    OnboardingInstanceSummary:
      type: object
      required: [id, employeeId, employeeName, profileId, profileName, joinDate, status, progressPct, totalTasks, completedTasks]
      properties:
        id: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        employeeCode: { type: string, nullable: true }
        profileId: { type: string, format: uuid }
        profileName: { type: string }
        joinDate: { type: string, format: date }
        status: { type: string, enum: [NOT_STARTED, IN_PROGRESS, COMPLETED, CANCELLED] }
        progressPct: { type: number }
        buddyEmployeeId: { type: string, format: uuid, nullable: true }
        buddyName: { type: string, nullable: true }
        totalTasks: { type: integer }
        completedTasks: { type: integer }

    OnboardingInstanceListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OnboardingInstanceSummary'
        totalCount: { type: integer }

    OnboardingInstanceCreateRequest:
      type: object
      required: [employeeId, profileId, joinDate]
      properties:
        employeeId: { type: string, format: uuid }
        profileId: { type: string, format: uuid }
        joinDate: { type: string, format: date }
        buddyEmployeeId: { type: string, format: uuid, nullable: true }
        candidateId: { type: string, format: uuid, nullable: true }

    OnboardingTaskCompleteRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }
        attachmentUrl: { type: string, nullable: true }

    ExitReasonItem:
      type: object
      required: [id, code, name, category]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        category: { type: string }

    ExitTypeItem:
      type: object
      required: [id, code, name, voluntary, noticeDays, requiresInterview, requiresClearance, reasons]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        voluntary: { type: boolean }
        noticeDays: { type: integer }
        requiresInterview: { type: boolean }
        requiresClearance: { type: boolean }
        reasons:
          type: array
          items:
            $ref: '#/components/schemas/ExitReasonItem'

    ExitTypeListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ExitTypeItem'

    ExitNoticeItem:
      type: object
      required: [id, noticeNumber, employeeId, employeeName, exitTypeId, exitTypeCode, exitTypeName, noticeDate, requestedLastWorkingDate, status]
      properties:
        id: { type: string, format: uuid }
        noticeNumber: { type: string }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        employeeCode: { type: string, nullable: true }
        exitTypeId: { type: string, format: uuid }
        exitTypeCode: { type: string }
        exitTypeName: { type: string }
        exitReasonId: { type: string, format: uuid, nullable: true }
        exitReasonName: { type: string, nullable: true }
        noticeDate: { type: string, format: date }
        requestedLastWorkingDate: { type: string, format: date }
        approvedLastWorkingDate: { type: string, format: date, nullable: true }
        remarks: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, SUBMITTED, APPROVED, REJECTED, WITHDRAWN, REVERSED] }
        approvedBy: { type: string, format: uuid, nullable: true }
        approvedByName: { type: string, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }

    ExitNoticeListResponse:
      type: object
      required: [items, totalCount]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ExitNoticeItem'
        totalCount: { type: integer }

    ExitNoticeCreateRequest:
      type: object
      required: [exitTypeId, requestedLastWorkingDate]
      properties:
        exitTypeId: { type: string, format: uuid }
        exitReasonId: { type: string, format: uuid, nullable: true }
        noticeDate: { type: string, format: date, nullable: true }
        requestedLastWorkingDate: { type: string, format: date }
        remarks: { type: string, nullable: true }

    ExitNoticeApproveRequest:
      type: object
      required: [approvedLastWorkingDate]
      properties:
        approvedLastWorkingDate: { type: string, format: date }
        remarks: { type: string, nullable: true }

    ClearanceTaskItem:
      type: object
      required: [id, exitNoticeId, employeeId, department, title, status, recoverableAmount]
      properties:
        id: { type: string, format: uuid }
        exitNoticeId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        department: { type: string, enum: [IT_INFRASTRUCTURE, FINANCE_PAYROLL, HR_OPERATIONS, ADMIN_FACILITIES, LINE_MANAGER] }
        title: { type: string }
        assigneeEmployeeId: { type: string, format: uuid, nullable: true }
        assigneeName: { type: string, nullable: true }
        status: { type: string, enum: [PENDING, IN_PROGRESS, CLEARED, WAIVED, REJECTED] }
        clearedAt: { type: string, format: date-time, nullable: true }
        remarks: { type: string, nullable: true }
        recoverableAmount: { type: number }

    ClearanceDetailResponse:
      type: object
      required: [exitNoticeId, employeeId, employeeName, totalTasks, clearedTasks, pendingTasks, totalRecoverableAmount, tasks]
      properties:
        exitNoticeId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        totalTasks: { type: integer }
        clearedTasks: { type: integer }
        pendingTasks: { type: integer }
        totalRecoverableAmount: { type: number }
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/ClearanceTaskItem'

    ClearanceTaskStatusUpdateRequest:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [PENDING, IN_PROGRESS, CLEARED, WAIVED, REJECTED] }
        remarks: { type: string, nullable: true }
        recoverableAmount: { type: number, nullable: true }

    ExitInterviewItem:
      type: object
      required: [id, exitNoticeId, employeeId, employeeName, conductedAt, overallExperienceRating, managementRating, cultureRating, wouldRecommend]
      properties:
        id: { type: string, format: uuid }
        exitNoticeId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        interviewerEmployeeId: { type: string, format: uuid, nullable: true }
        interviewerName: { type: string, nullable: true }
        conductedAt: { type: string, format: date-time }
        overallExperienceRating: { type: integer }
        managementRating: { type: integer }
        cultureRating: { type: integer }
        reasonDetails: { type: string, nullable: true }
        suggestions: { type: string, nullable: true }
        wouldRecommend: { type: boolean }

    ExitInterviewSubmitRequest:
      type: object
      required: [overallExperienceRating, managementRating, cultureRating, wouldRecommend]
      properties:
        overallExperienceRating: { type: integer }
        managementRating: { type: integer }
        cultureRating: { type: integer }
        reasonDetails: { type: string, nullable: true }
        suggestions: { type: string, nullable: true }
        wouldRecommend: { type: boolean }

    DocumentFolderItem:
      type: object
      required: [id, name, accessLevel, documentCount, createdAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string, nullable: true }
        icon: { type: string, nullable: true }
        parentFolderId: { type: string, format: uuid, nullable: true }
        accessLevel: { type: string }
        documentCount: { type: integer }
        createdAt: { type: string, format: date-time }

    DocumentFolderListResponse:
      type: object
      required: [folders]
      properties:
        folders:
          type: array
          items:
            $ref: '#/components/schemas/DocumentFolderItem'

    DocumentFolderCreateRequest:
      type: object
      required: [name, accessLevel]
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        icon: { type: string, nullable: true }
        parentFolderId: { type: string, format: uuid, nullable: true }
        accessLevel: { type: string }

    DocumentTagItem:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        colorHex: { type: string, nullable: true }

    DocumentVersionItem:
      type: object
      required: [id, versionNumber, fileUrl, fileName, fileSizeBytes, mimeType, checksumSha256, uploadedAt]
      properties:
        id: { type: string, format: uuid }
        versionNumber: { type: integer }
        fileUrl: { type: string }
        fileName: { type: string }
        fileSizeBytes: { type: integer, format: int64 }
        mimeType: { type: string }
        checksumSha256: { type: string }
        changeNotes: { type: string, nullable: true }
        uploadedBy: { type: string, format: uuid, nullable: true }
        uploadedByName: { type: string, nullable: true }
        uploadedAt: { type: string, format: date-time }

    CompanyDocumentItem:
      type: object
      required: [id, title, category, documentType, accessLevel, currentVersionNumber, fileUrl, fileName, fileSizeBytes, mimeType, checksumSha256, tags, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        folderId: { type: string, format: uuid, nullable: true }
        folderName: { type: string, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string }
        documentType: { type: string }
        accessLevel: { type: string }
        currentVersionNumber: { type: integer }
        fileUrl: { type: string }
        fileName: { type: string }
        fileSizeBytes: { type: integer, format: int64 }
        mimeType: { type: string }
        checksumSha256: { type: string }
        tags:
          type: array
          items: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    DocumentListResponse:
      type: object
      required: [documents]
      properties:
        documents:
          type: array
          items:
            $ref: '#/components/schemas/CompanyDocumentItem'

    DocumentCreateRequest:
      type: object
      required: [title, category, documentType, accessLevel, fileUrl, fileName, fileSizeBytes, mimeType, checksumSha256]
      properties:
        folderId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string }
        documentType: { type: string }
        accessLevel: { type: string }
        fileUrl: { type: string }
        fileName: { type: string }
        fileSizeBytes: { type: integer, format: int64 }
        mimeType: { type: string }
        checksumSha256: { type: string }
        tags:
          type: array
          items: { type: string }
          nullable: true

    DocumentVersionCreateRequest:
      type: object
      required: [fileUrl, fileName, fileSizeBytes, mimeType, checksumSha256]
      properties:
        fileUrl: { type: string }
        fileName: { type: string }
        fileSizeBytes: { type: integer, format: int64 }
        mimeType: { type: string }
        checksumSha256: { type: string }
        changeNotes: { type: string, nullable: true }

    DocumentDetailResponse:
      type: object
      required: [document, versions]
      properties:
        document:
          $ref: '#/components/schemas/CompanyDocumentItem'
        versions:
          type: array
          items:
            $ref: '#/components/schemas/DocumentVersionItem'

    DocumentTemplateItem:
      type: object
      required: [id, templateCode, name, category, bodyTemplate, variables, isActive, createdAt]
      properties:
        id: { type: string, format: uuid }
        templateCode: { type: string }
        name: { type: string }
        category: { type: string }
        description: { type: string, nullable: true }
        bodyTemplate: { type: string }
        variables:
          type: array
          items: { type: string }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }

    DocumentTemplateListResponse:
      type: object
      required: [templates]
      properties:
        templates:
          type: array
          items:
            $ref: '#/components/schemas/DocumentTemplateItem'

    LetterRequestItem:
      type: object
      required: [id, templateId, templateCode, templateName, employeeId, employeeName, reason, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        templateId: { type: string, format: uuid }
        templateCode: { type: string }
        templateName: { type: string }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        reason: { type: string }
        recipientAddress: { type: string, nullable: true }
        status: { type: string }
        generatedDocumentId: { type: string, format: uuid, nullable: true }
        generatedDocumentUrl: { type: string, nullable: true }
        generatedContent: { type: string, nullable: true }
        rejectionReason: { type: string, nullable: true }
        approvedBy: { type: string, format: uuid, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    LetterRequestListResponse:
      type: object
      required: [requests]
      properties:
        requests:
          type: array
          items:
            $ref: '#/components/schemas/LetterRequestItem'

    LetterRequestCreateRequest:
      type: object
      required: [templateId, employeeId, reason]
      properties:
        templateId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        reason: { type: string }
        recipientAddress: { type: string, nullable: true }

    LetterRequestApproveRequest:
      type: object
      required: [approved]
      properties:
        approved: { type: boolean }
        rejectionReason: { type: string, nullable: true }

    SignatureSignerItem:
      type: object
      required: [id, signerName, signerEmail, role, signingOrder, status]
      properties:
        id: { type: string, format: uuid }
        signerEmployeeId: { type: string, format: uuid, nullable: true }
        signerName: { type: string }
        signerEmail: { type: string }
        role: { type: string }
        signingOrder: { type: integer }
        status: { type: string }
        signatureType: { type: string, nullable: true }
        signatureData: { type: string, nullable: true }
        ipAddress: { type: string, nullable: true }
        signedAt: { type: string, format: date-time, nullable: true }

    SignatureAuditLogItem:
      type: object
      required: [id, action, actorName, timestamp]
      properties:
        id: { type: string, format: uuid }
        action: { type: string }
        actorId: { type: string, format: uuid, nullable: true }
        actorName: { type: string }
        ipAddress: { type: string, nullable: true }
        details: { type: string, nullable: true }
        timestamp: { type: string, format: date-time }

    SignatureRequestItem:
      type: object
      required: [id, title, fileUrl, fileChecksumSha256, status, createdByEmployeeId, createdByName, currentSignerOrder, totalSigners, signedCount, createdAt]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        description: { type: string, nullable: true }
        documentId: { type: string, format: uuid, nullable: true }
        documentTitle: { type: string, nullable: true }
        fileUrl: { type: string }
        fileChecksumSha256: { type: string }
        status: { type: string }
        createdByEmployeeId: { type: string, format: uuid }
        createdByName: { type: string }
        currentSignerOrder: { type: integer }
        totalSigners: { type: integer }
        signedCount: { type: integer }
        expiresAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }

    SignatureRequestDetailResponse:
      type: object
      required: [request, signers, auditLogs]
      properties:
        request:
          $ref: '#/components/schemas/SignatureRequestItem'
        signers:
          type: array
          items:
            $ref: '#/components/schemas/SignatureSignerItem'
        auditLogs:
          type: array
          items:
            $ref: '#/components/schemas/SignatureAuditLogItem'

    SignatureRequestListResponse:
      type: object
      required: [requests]
      properties:
        requests:
          type: array
          items:
            $ref: '#/components/schemas/SignatureRequestItem'

    SignatureSignerInput:
      type: object
      required: [signerName, signerEmail, role, signingOrder]
      properties:
        signerEmployeeId: { type: string, format: uuid, nullable: true }
        signerName: { type: string }
        signerEmail: { type: string }
        role: { type: string }
        signingOrder: { type: integer }

    SignatureRequestCreateRequest:
      type: object
      required: [title, fileUrl, fileChecksumSha256, signers]
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        documentId: { type: string, format: uuid, nullable: true }
        fileUrl: { type: string }
        fileChecksumSha256: { type: string }
        expiresAt: { type: string, format: date-time, nullable: true }
        signers:
          type: array
          items:
            $ref: '#/components/schemas/SignatureSignerInput'

    SignatureSignRequest:
      type: object
      required: [signatureType, signatureData, consentConfirmed]
      properties:
        signerEmployeeId: { type: string, format: uuid, nullable: true }
        signatureType: { type: string }
        signatureData: { type: string }
        consentConfirmed: { type: boolean }
        ipAddress: { type: string, nullable: true }

    TrainingProviderItem:
      type: object
      required: [id, providerName, providerType, isAccredited]
      properties:
        id: { type: string, format: uuid }
        providerName: { type: string }
        providerType: { type: string }
        contactEmail: { type: string, nullable: true }
        contactPhone: { type: string, nullable: true }
        websiteUrl: { type: string, nullable: true }
        isAccredited: { type: boolean }

    ResourcePersonItem:
      type: object
      required: [id, fullName]
      properties:
        id: { type: string, format: uuid }
        providerId: { type: string, format: uuid, nullable: true }
        fullName: { type: string }
        email: { type: string, nullable: true }
        bio: { type: string, nullable: true }
        specialization: { type: string, nullable: true }
        internalEmployeeId: { type: string, format: uuid, nullable: true }

    TrainingCourseItem:
      type: object
      required: [id, courseCode, title, category, deliveryMode, durationHours, maxCapacity, isActive]
      properties:
        id: { type: string, format: uuid }
        providerId: { type: string, format: uuid, nullable: true }
        providerName: { type: string, nullable: true }
        courseCode: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string }
        deliveryMode: { type: string }
        durationHours: { type: number }
        targetAudience: { type: string, nullable: true }
        prerequisites: { type: string, nullable: true }
        maxCapacity: { type: integer }
        competencyId: { type: string, format: uuid, nullable: true }
        isActive: { type: boolean }

    CourseListResponse:
      type: object
      required: [courses]
      properties:
        courses:
          type: array
          items:
            $ref: '#/components/schemas/TrainingCourseItem'

    CourseCreateRequest:
      type: object
      required: [courseCode, title, category, deliveryMode, durationHours, maxCapacity]
      properties:
        providerId: { type: string, format: uuid, nullable: true }
        courseCode: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        category: { type: string }
        deliveryMode: { type: string }
        durationHours: { type: number }
        targetAudience: { type: string, nullable: true }
        prerequisites: { type: string, nullable: true }
        maxCapacity: { type: integer }
        competencyId: { type: string, format: uuid, nullable: true }

    CourseDetailResponse:
      type: object
      required: [course, schedules]
      properties:
        course:
          $ref: '#/components/schemas/TrainingCourseItem'
        provider:
          $ref: '#/components/schemas/TrainingProviderItem'
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/TrainingScheduleItem'

    TrainingScheduleItem:
      type: object
      required: [id, courseId, courseTitle, batchCode, startDate, endDate, totalSeats, enrolledSeats, status, costPerParticipant]
      properties:
        id: { type: string, format: uuid }
        courseId: { type: string, format: uuid }
        courseTitle: { type: string }
        trainerId: { type: string, format: uuid, nullable: true }
        trainerName: { type: string, nullable: true }
        batchCode: { type: string }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time }
        venueName: { type: string, nullable: true }
        virtualMeetingUrl: { type: string, nullable: true }
        totalSeats: { type: integer }
        enrolledSeats: { type: integer }
        status: { type: string }
        costPerParticipant: { type: number }

    TrainingScheduleListResponse:
      type: object
      required: [schedules]
      properties:
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/TrainingScheduleItem'

    TrainingScheduleCreateRequest:
      type: object
      required: [courseId, batchCode, startDate, endDate, totalSeats]
      properties:
        courseId: { type: string, format: uuid }
        trainerId: { type: string, format: uuid, nullable: true }
        batchCode: { type: string }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time }
        venueName: { type: string, nullable: true }
        virtualMeetingUrl: { type: string, nullable: true }
        totalSeats: { type: integer }
        costPerParticipant: { type: number, nullable: true }

    TrainingEnrollmentItem:
      type: object
      required: [id, scheduleId, courseTitle, batchCode, startDate, endDate, employeeId, employeeName, enrollmentType, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        scheduleId: { type: string, format: uuid }
        courseTitle: { type: string }
        batchCode: { type: string }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time }
        employeeId: { type: string, format: uuid }
        employeeName: { type: string }
        enrollmentType: { type: string }
        status: { type: string }
        nominatedBy: { type: string, format: uuid, nullable: true }
        approvalRemarks: { type: string, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        cancellationReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }

    TrainingEnrollmentListResponse:
      type: object
      required: [enrollments]
      properties:
        enrollments:
          type: array
          items:
            $ref: '#/components/schemas/TrainingEnrollmentItem'

    TrainingEnrollmentCreateRequest:
      type: object
      required: [scheduleId, employeeId, enrollmentType]
      properties:
        scheduleId: { type: string, format: uuid }
        employeeId: { type: string, format: uuid }
        enrollmentType: { type: string }
        nominatedBy: { type: string, format: uuid, nullable: true }

    TrainingEnrollmentApproveRequest:
      type: object
      required: [decision]
      properties:
        decision: { type: string }
        remarks: { type: string, nullable: true }

    TrainingAttendanceItem:
      type: object
      required: [id, enrollmentId, sessionDate, checkInTime, attendanceStatus]
      properties:
        id: { type: string, format: uuid }
        enrollmentId: { type: string, format: uuid }
        sessionDate: { type: string, format: date }
        checkInTime: { type: string, format: date-time }
        attendanceStatus: { type: string }
        remarks: { type: string, nullable: true }

    TrainingAttendanceCheckInRequest:
      type: object
      required: [sessionDate]
      properties:
        sessionDate: { type: string, format: date }
        remarks: { type: string, nullable: true }

    TraineeEvaluationItem:
      type: object
      required: [id, enrollmentId, ratingScore, contentRating, instructorRating, submittedAt]
      properties:
        id: { type: string, format: uuid }
        enrollmentId: { type: string, format: uuid }
        ratingScore: { type: integer }
        contentRating: { type: integer }
        instructorRating: { type: integer }
        feedbackComments: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time }

    TraineeEvaluationSubmitRequest:
      type: object
      required: [ratingScore, contentRating, instructorRating]
      properties:
        ratingScore: { type: integer }
        contentRating: { type: integer }
        instructorRating: { type: integer }
        feedbackComments: { type: string, nullable: true }

    TrainingCertificateItem:
      type: object
      required: [id, enrollmentId, courseTitle, employeeName, certificateNumber, issuedDate, verificationHash]
      properties:
        id: { type: string, format: uuid }
        enrollmentId: { type: string, format: uuid }
        courseTitle: { type: string }
        employeeName: { type: string }
        certificateNumber: { type: string }
        issuedDate: { type: string, format: date }
        expiryDate: { type: string, format: date, nullable: true }
        verificationHash: { type: string }
        fileUrl: { type: string, nullable: true }

    TrainingCertificateListResponse:
      type: object
      required: [certificates]
      properties:
        certificates:
          type: array
          items:
            $ref: '#/components/schemas/TrainingCertificateItem'

    TrainingNeedItem:
      type: object
      required: [competencyId, competencyName, currentProficiency, targetProficiency, gap, recommendedCourses]
      properties:
        competencyId: { type: string, format: uuid }
        competencyName: { type: string }
        currentProficiency: { type: number }
        targetProficiency: { type: number }
        gap: { type: number }
        recommendedCourses:
          type: array
          items:
            $ref: '#/components/schemas/TrainingCourseItem'

    TrainingNeedsResponse:
      type: object
      required: [needs]
      properties:
        needs:
          type: array
          items:
            $ref: '#/components/schemas/TrainingNeedItem'

    TimesheetClientItem:
      type: object
      required: [id, clientCode, clientName, status]
      properties:
        id:
          type: string
          format: uuid
        clientCode:
          type: string
          example: ACME-US
        clientName:
          type: string
          example: Acme Global Corp
        status:
          type: string
          example: ACTIVE
        billingCurrency:
          type: string
          example: USD
        contactEmail:
          type: string
          format: email
          nullable: true

    TimesheetClientListResponse:
      type: object
      required: [clients]
      properties:
        clients:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetClientItem'

    TimesheetProjectItem:
      type: object
      required: [id, clientId, projectCode, projectName, billable, status]
      properties:
        id:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        clientName:
          type: string
          example: Acme Global Corp
        projectCode:
          type: string
          example: PRJ-ERP-01
        projectName:
          type: string
          example: ERP Migration Phase 2
        description:
          type: string
          nullable: true
        billable:
          type: boolean
          example: true
        status:
          type: string
          example: ACTIVE
        budgetHours:
          type: number
          format: double
          nullable: true

    TimesheetProjectListResponse:
      type: object
      required: [projects]
      properties:
        projects:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetProjectItem'

    TimesheetProjectCreateRequest:
      type: object
      required: [clientId, projectCode, projectName, billable]
      properties:
        clientId:
          type: string
          format: uuid
        projectCode:
          type: string
        projectName:
          type: string
        description:
          type: string
          nullable: true
        billable:
          type: boolean
          default: true
        budgetHours:
          type: number
          format: double
          nullable: true

    TimesheetActivityItem:
      type: object
      required: [id, projectId, activityCode, activityName, billable, status]
      properties:
        id:
          type: string
          format: uuid
        projectId:
          type: string
          format: uuid
        activityCode:
          type: string
          example: DEV-BE
        activityName:
          type: string
          example: Backend API Development
        billable:
          type: boolean
          example: true
        status:
          type: string
          example: ACTIVE

    TimesheetActivityListResponse:
      type: object
      required: [activities]
      properties:
        activities:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetActivityItem'

    TimesheetListItem:
      type: object
      required: [id, employeeId, employeeName, weekStartDate, weekEndDate, status, totalHours, billableHours, nonBillableHours]
      properties:
        id:
          type: string
          format: uuid
        employeeId:
          type: string
          format: uuid
        employeeName:
          type: string
          example: Kasun Perera
        weekStartDate:
          type: string
          format: date
          example: 2026-09-01
        weekEndDate:
          type: string
          format: date
          example: 2026-09-07
        status:
          type: string
          enum: [DRAFT, SUBMITTED, APPROVED, REJECTED]
          example: SUBMITTED
        totalHours:
          type: number
          format: double
          example: 40.0
        billableHours:
          type: number
          format: double
          example: 36.0
        nonBillableHours:
          type: number
          format: double
          example: 4.0
        submittedAt:
          type: string
          format: date-time
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true

    TimesheetListResponse:
      type: object
      required: [timesheets]
      properties:
        timesheets:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetListItem'

    TimesheetEntryItem:
      type: object
      required: [id, projectId, projectCode, projectName, activityId, activityCode, activityName, entryDate, hours, billable]
      properties:
        id:
          type: string
          format: uuid
        projectId:
          type: string
          format: uuid
        projectCode:
          type: string
          example: PRJ-ERP-01
        projectName:
          type: string
          example: ERP Migration Phase 2
        activityId:
          type: string
          format: uuid
        activityCode:
          type: string
          example: DEV-BE
        activityName:
          type: string
          example: Backend API Development
        entryDate:
          type: string
          format: date
          example: 2026-09-01
        hours:
          type: number
          format: double
          example: 8.0
        billable:
          type: boolean
          example: true
        notes:
          type: string
          nullable: true

    TimesheetDetailResponse:
      type: object
      required: [id, employeeId, employeeName, weekStartDate, weekEndDate, status, totalHours, billableHours, nonBillableHours, entries]
      properties:
        id:
          type: string
          format: uuid
        employeeId:
          type: string
          format: uuid
        employeeName:
          type: string
          example: Kasun Perera
        weekStartDate:
          type: string
          format: date
          example: 2026-09-01
        weekEndDate:
          type: string
          format: date
          example: 2026-09-07
        status:
          type: string
          enum: [DRAFT, SUBMITTED, APPROVED, REJECTED]
          example: DRAFT
        totalHours:
          type: number
          format: double
          example: 40.0
        billableHours:
          type: number
          format: double
          example: 36.0
        nonBillableHours:
          type: number
          format: double
          example: 4.0
        submittedAt:
          type: string
          format: date-time
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        approverId:
          type: string
          format: uuid
          nullable: true
        approverName:
          type: string
          nullable: true
        rejectionReason:
          type: string
          nullable: true
        comments:
          type: string
          nullable: true
        entries:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetEntryItem'

    TimesheetEntryInput:
      type: object
      required: [projectId, activityId, entryDate, hours, billable]
      properties:
        projectId:
          type: string
          format: uuid
        activityId:
          type: string
          format: uuid
        entryDate:
          type: string
          format: date
          example: 2026-09-01
        hours:
          type: number
          format: double
          example: 8.0
        billable:
          type: boolean
          default: true
        notes:
          type: string
          nullable: true

    TimesheetSaveRequest:
      type: object
      required: [weekStartDate, entries]
      properties:
        id:
          type: string
          format: uuid
          nullable: true
        employeeId:
          type: string
          format: uuid
          nullable: true
        weekStartDate:
          type: string
          format: date
          example: 2026-09-01
        comments:
          type: string
          nullable: true
        entries:
          type: array
          items:
            $ref: '#/components/schemas/TimesheetEntryInput'

    TimesheetApproveRequest:
      type: object
      properties:
        comments:
          type: string
          nullable: true

    TimesheetRejectRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          example: Missing detailed notes for overtime hours on Friday.

    DailyReconciliationItem:
      type: object
      required: [date, loggedHours, attendanceHours, varianceHours, status]
      properties:
        date:
          type: string
          format: date
          example: 2026-09-01
        loggedHours:
          type: number
          format: double
          example: 8.0
        attendanceHours:
          type: number
          format: double
          example: 8.0
        varianceHours:
          type: number
          format: double
          example: 0.0
        status:
          type: string
          enum: [MATCHED, UNDER_LOGGED, OVER_LOGGED, NO_ATTENDANCE]
          example: MATCHED

    TimesheetReconciliationResponse:
      type: object
      required: [employeeId, weekStartDate, weekEndDate, totalLoggedHours, totalAttendanceHours, totalVarianceHours, dailyBreakdown]
      properties:
        employeeId:
          type: string
          format: uuid
        weekStartDate:
          type: string
          format: date
          example: 2026-09-01
        weekEndDate:
          type: string
          format: date
          example: 2026-09-07
        totalLoggedHours:
          type: number
          format: double
          example: 40.0
        totalAttendanceHours:
          type: number
          format: double
          example: 40.0
        totalVarianceHours:
          type: number
          format: double
          example: 0.0
        dailyBreakdown:
          type: array
          items:
            $ref: '#/components/schemas/DailyReconciliationItem'

    TimesheetCopyPreviousRequest:
      type: object
      required: [targetWeekStartDate]
      properties:
        employeeId:
          type: string
          format: uuid
          nullable: true
        targetWeekStartDate:
          type: string
          format: date
          example: 2026-09-08

    PayGroupItem:
      type: object
      required: [id, code, name, countryCode, currency, payFrequency, standardDaysPerMonth, isActive]
      properties:
        id:
          type: string
        code:
          type: string
        name:
          type: string
        countryCode:
          type: string
        currency:
          type: string
        payFrequency:
          type: string
        standardDaysPerMonth:
          type: number
          format: double
        isActive:
          type: boolean

    PayGroupsResponse:
      type: object
      required: [payGroups]
      properties:
        payGroups:
          type: array
          items:
            $ref: '#/components/schemas/PayGroupItem'

    PayPeriodItem:
      type: object
      required: [id, payGroupId, code, startDate, endDate, paymentDate, status]
      properties:
        id:
          type: string
        payGroupId:
          type: string
        code:
          type: string
        startDate:
          type: string
        endDate:
          type: string
        paymentDate:
          type: string
        status:
          type: string

    PayPeriodsResponse:
      type: object
      required: [payPeriods]
      properties:
        payPeriods:
          type: array
          items:
            $ref: '#/components/schemas/PayPeriodItem'

    PayrollRunItem:
      type: object
      required: [id, payGroupId, payPeriodId, runNumber, status, totalGross, totalStatutoryEmployee, totalStatutoryEmployer, totalTax, totalNet, totalEmployees]
      properties:
        id:
          type: string
        payGroupId:
          type: string
        payPeriodId:
          type: string
        runNumber:
          type: integer
        status:
          type: string
        totalGross:
          type: number
          format: double
        totalStatutoryEmployee:
          type: number
          format: double
        totalStatutoryEmployer:
          type: number
          format: double
        totalTax:
          type: number
          format: double
        totalNet:
          type: number
          format: double
        totalEmployees:
          type: integer
        calculatedAt:
          type: string
          nullable: true
        approvedAt:
          type: string
          nullable: true
        committedAt:
          type: string
          nullable: true

    PayrollRunsResponse:
      type: object
      required: [runs]
      properties:
        runs:
          type: array
          items:
            $ref: '#/components/schemas/PayrollRunItem'

    CalculatePayrollRequest:
      type: object
      required: [payGroupId, payPeriodId]
      properties:
        payGroupId:
          type: string
        payPeriodId:
          type: string
        includeLossOfPay:
          type: boolean
          nullable: true

    PayrollResultItem:
      type: object
      required: [id, payrollRunId, employeeId, employeeCode, employeeName, department, designation, currency, basicSalary, grossPay, totalStatutoryEmployee, totalStatutoryEmployer, taxDeductions, otherDeductions, netPay]
      properties:
        id:
          type: string
        payrollRunId:
          type: string
        employeeId:
          type: string
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        designation:
          type: string
        currency:
          type: string
        basicSalary:
          type: number
          format: double
        grossPay:
          type: number
          format: double
        totalStatutoryEmployee:
          type: number
          format: double
        totalStatutoryEmployer:
          type: number
          format: double
        taxDeductions:
          type: number
          format: double
        otherDeductions:
          type: number
          format: double
        netPay:
          type: number
          format: double

    PayrollResultLineItem:
      type: object
      required: [id, category, itemCode, itemName, amount, isStatutory]
      properties:
        id:
          type: string
        category:
          type: string
        itemCode:
          type: string
        itemName:
          type: string
        amount:
          type: number
          format: double
        rate:
          type: number
          format: double
          nullable: true
        hours:
          type: number
          format: double
          nullable: true
        isStatutory:
          type: boolean
        calculationTrace:
          type: string
          nullable: true

    PayrollResultsResponse:
      type: object
      required: [results]
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/PayrollResultItem'

    PayrollResultDetailResponse:
      type: object
      required: [result, lines]
      properties:
        result:
          $ref: '#/components/schemas/PayrollResultItem'
        lines:
          type: array
          items:
            $ref: '#/components/schemas/PayrollResultLineItem'

    PayrollVarianceAnomalyItem:
      type: object
      required: [employeeId, employeeCode, employeeName, department, previousGross, currentGross, varianceAmount, variancePercent, reasons]
      properties:
        employeeId:
          type: string
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        previousGross:
          type: number
          format: double
        currentGross:
          type: number
          format: double
        varianceAmount:
          type: number
          format: double
        variancePercent:
          type: number
          format: double
        reasons:
          type: array
          items:
            type: string

    PayrollVarianceResponse:
      type: object
      required: [grossVariancePercent, netVariancePercent, headcountDifference, anomalies]
      properties:
        grossVariancePercent:
          type: number
          format: double
        netVariancePercent:
          type: number
          format: double
        headcountDifference:
          type: integer
        anomalies:
          type: array
          items:
            $ref: '#/components/schemas/PayrollVarianceAnomalyItem'

    BankAdviceRequest:
      type: object
      required: [format, companyAccount, paymentDate]
      properties:
        format:
          type: string
        companyAccount:
          type: string
        paymentDate:
          type: string

    BankAdviceResponse:
      type: object
      required: [filename, content, totalRecords, totalAmount, batchHash, mimeType]
      properties:
        filename:
          type: string
        content:
          type: string
        totalRecords:
          type: integer
        totalAmount:
          type: number
          format: double
        batchHash:
          type: string
        mimeType:
          type: string

    TenantModuleStatus:
      type: object
      required: [moduleKey, enabled]
      properties:
        moduleKey:
          type: string
        enabled:
          type: boolean
        config:
          type: object
          additionalProperties: true
        updatedAt:
          type: string
          format: date-time

    TenantDetail:
      type: object
      required: [id, code, name, countryCode, timezone, defaultCurrency, locale, dataRegion, isolationTier, status, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        legalName:
          type: string
        countryCode:
          type: string
        timezone:
          type: string
        defaultCurrency:
          type: string
        locale:
          type: string
        dataRegion:
          type: string
        isolationTier:
          type: string
          enum: [SHARED, DEDICATED_SCHEMA, DEDICATED_DATABASE]
        status:
          type: string
          enum: [PROVISIONING, ACTIVE, SUSPENDED, ARCHIVED]
        subscriptionPlan:
          type: string
        adminEmail:
          type: string
        modules:
          type: array
          items:
            $ref: '#/components/schemas/TenantModuleStatus'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    TenantListResponse:
      type: object
      required: [tenants]
      properties:
        tenants:
          type: array
          items:
            $ref: '#/components/schemas/TenantDetail'

    CreateTenantPayload:
      type: object
      required: [code, name, countryCode, defaultCurrency]
      properties:
        code:
          type: string
        name:
          type: string
        legalName:
          type: string
        countryCode:
          type: string
        timezone:
          type: string
        defaultCurrency:
          type: string
        locale:
          type: string
        dataRegion:
          type: string
        isolationTier:
          type: string
          enum: [SHARED, DEDICATED_SCHEMA, DEDICATED_DATABASE]
        subscriptionPlan:
          type: string
        adminEmail:
          type: string
        modules:
          type: object
          additionalProperties:
            type: boolean

    UpdateTenantPayload:
      type: object
      properties:
        name:
          type: string
        legalName:
          type: string
        timezone:
          type: string
        defaultCurrency:
          type: string
        locale:
          type: string
        dataRegion:
          type: string
        isolationTier:
          type: string
          enum: [SHARED, DEDICATED_SCHEMA, DEDICATED_DATABASE]
        subscriptionPlan:
          type: string
        status:
          type: string
          enum: [PROVISIONING, ACTIVE, SUSPENDED, ARCHIVED]
        adminEmail:
          type: string

    UpdateTenantModulesPayload:
      type: object
      required: [modules]
      properties:
        modules:
          type: object
          additionalProperties:
            type: boolean

    TenantModulesResponse:
      type: object
      required: [modules]
      properties:
        modules:
          type: array
          items:
            $ref: '#/components/schemas/TenantModuleStatus'

    RoleSummary:
      type: object
      required: [id, code, name, description, isSystem, permissions]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        description:
          type: string
        isSystem:
          type: boolean
        permissions:
          type: array
          items:
            type: string
        assignedUserCount:
          type: integer

    RoleListResponse:
      type: object
      required: [roles]
      properties:
        roles:
          type: array
          items:
            $ref: '#/components/schemas/RoleSummary'

    CreateRolePayload:
      type: object
      required: [name, code]
      properties:
        name:
          type: string
        code:
          type: string
        description:
          type: string
        permissions:
          type: array
          items:
            type: string

    UpdateRolePayload:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        permissions:
          type: array
          items:
            type: string

    RoleActionSuccess:
      type: object
      required: [success]
      properties:
        success:
          type: boolean

    PermissionItem:
      type: object
      required: [key, domain, label, description]
      properties:
        key:
          type: string
        domain:
          type: string
        label:
          type: string
        description:
          type: string

    PermissionListResponse:
      type: object
      required: [permissions]
      properties:
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/PermissionItem'

    UserSummary:
      type: object
      required: [id, username, role, status, mfaEnabled, createdAt, mustChangePassword]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
        employeeCode:
          type: string
        employeeName:
          type: string
        role:
          type: string
        status:
          type: string
        mfaEnabled:
          type: boolean
        lastLoginAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        mustChangePassword:
          type: boolean

    UserListResponse:
      type: object
      required: [users]
      properties:
        users:
          type: array
          items:
            $ref: '#/components/schemas/UserSummary'

    CreateUserPayload:
      type: object
      required: [username, role]
      properties:
        username:
          type: string
        email:
          type: string
        role:
          type: string
        employeeCode:
          type: string
        mustChangePassword:
          type: boolean
        temporaryPassword:
          type: string

    UpdateUserPayload:
      type: object
      properties:
        status:
          type: string
        role:
          type: string

    ResetPasswordPayload:
      type: object
      properties:
        temporaryPassword:
          type: string
        mustChangePassword:
          type: boolean

    ResetPasswordResponse:
      type: object
      required: [success, message]
      properties:
        success:
          type: boolean
        message:
          type: string

    UserDeviceSummary:
      type: object
      required: [id, isCurrent, mfaEnrolled]
      properties:
        id:
          type: string
          format: uuid
        deviceType:
          type: string
        name:
          type: string
        ipAddress:
          type: string
        lastSeenAt:
          type: string
          format: date-time
        isCurrent:
          type: boolean
        mfaEnrolled:
          type: boolean

    UserDeviceListResponse:
      type: object
      required: [devices]
      properties:
        devices:
          type: array
          items:
            $ref: '#/components/schemas/UserDeviceSummary'

    RevokeDeviceResponse:
      type: object
      required: [success]
      properties:
        success:
          type: boolean

    RevokeAllDevicesResponse:
      type: object
      required: [success, revokedCount]
      properties:
        success:
          type: boolean
        revokedCount:
          type: integer

    ShiftAdmin:
      type: object
      required: [id, code, name, shiftType, startTime, endTime, breakMinutes, crossesMidnight, workingMinutes, graceInMinutes, graceOutMinutes, halfDayThresholdMinutes, otEligible, otStartAfterMinutes, minOtMinutes, color, isActive]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        shiftType:
          type: string
        startTime:
          type: string
        endTime:
          type: string
        breakMinutes:
          type: integer
        crossesMidnight:
          type: boolean
        workingMinutes:
          type: integer
        graceInMinutes:
          type: integer
        graceOutMinutes:
          type: integer
        halfDayThresholdMinutes:
          type: integer
        otEligible:
          type: boolean
        otStartAfterMinutes:
          type: integer
        minOtMinutes:
          type: integer
        color:
          type: string
        isActive:
          type: boolean

    ShiftListResponse:
      type: object
      required: [shifts]
      properties:
        shifts:
          type: array
          items:
            $ref: '#/components/schemas/ShiftAdmin'

    RosterWeekDay:
      type: object
      required: [date, dayOfWeek, isWeekend, isHoliday]
      properties:
        date:
          type: string
          format: date
        dayOfWeek:
          type: integer
        isWeekend:
          type: boolean
        isHoliday:
          type: boolean
        holidayName:
          type: string

    ShiftScheduleItem:
      type: object
      required: [id, employeeId, workDate, isRestDay, isHoliday, source]
      properties:
        id:
          type: string
          format: uuid
        employeeId:
          type: string
          format: uuid
        workDate:
          type: string
          format: date
        shiftId:
          type: string
          format: uuid
        shiftCode:
          type: string
        shiftName:
          type: string
        shiftColor:
          type: string
        startTime:
          type: string
        endTime:
          type: string
        isRestDay:
          type: boolean
        isHoliday:
          type: boolean
        source:
          type: string

    RosterEmployeeItem:
      type: object
      required: [id, code, name, department]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        department:
          type: string

    RosterResponse:
      type: object
      required: [days, schedules, employees]
      properties:
        days:
          type: array
          items:
            $ref: '#/components/schemas/RosterWeekDay'
        schedules:
          type: object
          additionalProperties:
            type: array
            items:
              $ref: '#/components/schemas/ShiftScheduleItem'
        employees:
          type: array
          items:
            $ref: '#/components/schemas/RosterEmployeeItem'

    AssignShiftPayload:
      type: object
      required: [employeeId, workDate]
      properties:
        employeeId:
          type: string
          format: uuid
        workDate:
          type: string
          format: date
        shiftId:
          type: string
          format: uuid
        isRestDay:
          type: boolean

    RawPunchAdmin:
      type: object
      required: [id, employeeId, employeeCode, employeeName, department, punchedAt, punchType, source, geofenceStatus, isMockLocation, recordedOffline, syncedAt]
      properties:
        id:
          type: string
          format: uuid
        employeeId:
          type: string
          format: uuid
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        punchedAt:
          type: string
          format: date-time
        punchType:
          type: string
        source:
          type: string
        deviceId:
          type: string
        locationName:
          type: string
        geofenceStatus:
          type: string
        isMockLocation:
          type: boolean
        recordedOffline:
          type: boolean
        syncedAt:
          type: string
          format: date-time

    RawPunchListResponse:
      type: object
      required: [punches]
      properties:
        punches:
          type: array
          items:
            $ref: '#/components/schemas/RawPunchAdmin'

    IngestPunchAdminPayload:
      type: object
      required: [employeeId, punchedAt, punchType, source]
      properties:
        employeeId:
          type: string
          format: uuid
        punchedAt:
          type: string
          format: date-time
        punchType:
          type: string
        source:
          type: string
        deviceId:
          type: string
        locationName:
          type: string
        isMockLocation:
          type: boolean
        geofenceStatus:
          type: string

    DailyAttendanceAdmin:
      type: object
      required: [id, employeeId, employeeCode, employeeName, department, workDate, grossDurationMinutes, breakMinutes, netWorkedMinutes, lateMinutes, earlyLeaveMinutes, overtimeMinutesNormal, overtimeMinutesRestDay, overtimeMinutesHoliday, dayStatus, leaveDays, anomalyFlags, computedAt]
      properties:
        id:
          type: string
          format: uuid
        employeeId:
          type: string
          format: uuid
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        workDate:
          type: string
          format: date
        shiftCode:
          type: string
        shiftName:
          type: string
        firstInAt:
          type: string
          format: date-time
        lastOutAt:
          type: string
          format: date-time
        grossDurationMinutes:
          type: integer
        breakMinutes:
          type: integer
        netWorkedMinutes:
          type: integer
        lateMinutes:
          type: integer
        earlyLeaveMinutes:
          type: integer
        overtimeMinutesNormal:
          type: integer
        overtimeMinutesRestDay:
          type: integer
        overtimeMinutesHoliday:
          type: integer
        dayStatus:
          type: string
        leaveTypeName:
          type: string
        leaveDays:
          type: number
        anomalyFlags:
          type: array
          items:
            type: string
        calculationTrace:
          type: string
        computedAt:
          type: string
          format: date-time

    DailyAttendanceSummaryMetrics:
      type: object
      required: [totalEmployees, presentCount, halfDayCount, absentCount, restDayCount, holidayCount, anomaliesCount, totalOtHours, totalLateMinutes]
      properties:
        totalEmployees:
          type: integer
        presentCount:
          type: integer
        halfDayCount:
          type: integer
        absentCount:
          type: integer
        restDayCount:
          type: integer
        holidayCount:
          type: integer
        anomaliesCount:
          type: integer
        totalOtHours:
          type: number
        totalLateMinutes:
          type: integer

    DailyAttendanceListResponse:
      type: object
      required: [records, summary]
      properties:
        records:
          type: array
          items:
            $ref: '#/components/schemas/DailyAttendanceAdmin'
        summary:
          $ref: '#/components/schemas/DailyAttendanceSummaryMetrics'

    RecomputeAttendancePayload:
      type: object
      properties:
        date:
          type: string
          format: date
        employeeId:
          type: string
          format: uuid

    RecomputeAttendanceResponse:
      type: object
      required: [recomputedCount]
      properties:
        recomputedCount:
          type: integer

    AttendancePayrollSummaryItem:
      type: object
      required: [employeeId, employeeCode, employeeName, department, normalOtMinutes, restDayOtMinutes, holidayOtMinutes, otGrossEarnings, lateMinutesTotal, latePenaltyDeduction, unpaidAbsenceDays]
      properties:
        employeeId:
          type: string
          format: uuid
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        normalOtMinutes:
          type: integer
        restDayOtMinutes:
          type: integer
        holidayOtMinutes:
          type: integer
        otGrossEarnings:
          type: number
        lateMinutesTotal:
          type: integer
        latePenaltyDeduction:
          type: number
        unpaidAbsenceDays:
          type: number

    AttendancePayrollSummaryResponse:
      type: object
      required: [payPeriodId, totalNormalOtHours, totalRestDayOtHours, totalHolidayOtHours, totalOtEarningsAmount, totalLatePenaltyDeductionAmount, totalUnpaidAbsenceDays, employeeCountWithOt, employeeCountWithLatePenalty, employeeCountWithUnpaidAbsence, items]
      properties:
        payPeriodId:
          type: string
        totalNormalOtHours:
          type: number
        totalRestDayOtHours:
          type: number
        totalHolidayOtHours:
          type: number
        totalOtEarningsAmount:
          type: number
        totalLatePenaltyDeductionAmount:
          type: number
        totalUnpaidAbsenceDays:
          type: number
        employeeCountWithOt:
          type: integer
        employeeCountWithLatePenalty:
          type: integer
        employeeCountWithUnpaidAbsence:
          type: integer
        items:
          type: array
          items:
            $ref: '#/components/schemas/AttendancePayrollSummaryItem'

    TeamMemberLeave:
      type: object
      required: [employeeId, employeeCode, employeeName, leaveTypeId, leaveTypeName, portion, status]
      properties:
        employeeId:
          type: string
          format: uuid
        employeeCode:
          type: string
        employeeName:
          type: string
        leaveTypeId:
          type: string
        leaveTypeName:
          type: string
        portion:
          type: string
        status:
          type: string

    TeamCalendarDay:
      type: object
      required: [date, dayOfWeek, isWeekend, isHoliday, absences, hasConflict]
      properties:
        date:
          type: string
          format: date
        dayOfWeek:
          type: integer
        isWeekend:
          type: boolean
        isHoliday:
          type: boolean
        holidayName:
          type: string
        absences:
          type: array
          items:
            $ref: '#/components/schemas/TeamMemberLeave'
        hasConflict:
          type: boolean

    TeamCalendarResponse:
      type: object
      required: [days]
      properties:
        days:
          type: array
          items:
            $ref: '#/components/schemas/TeamCalendarDay'

    BiometricDeviceItem:
      type: object
      required: [id, name, serialNumber, vendor, port, direction, status, totalPunchesLogged]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        serialNumber:
          type: string
        vendor:
          type: string
        modelName:
          type: [string, "null"]
        ipAddress:
          type: [string, "null"]
        port:
          type: integer
        locationName:
          type: [string, "null"]
        direction:
          type: string
        status:
          type: string
        lastHeartbeatAt:
          type: [string, "null"]
        lastSyncAt:
          type: [string, "null"]
        totalPunchesLogged:
          type: integer
        firmwareVersion:
          type: [string, "null"]

    BiometricDeviceListResponse:
      type: object
      required: [devices]
      properties:
        devices:
          type: array
          items:
            $ref: '#/components/schemas/BiometricDeviceItem'

    CreateBiometricDevicePayload:
      type: object
      required: [name, serialNumber]
      properties:
        name:
          type: string
        serialNumber:
          type: string
        vendor:
          type: string
        modelName:
          type: string
        ipAddress:
          type: string
        port:
          type: integer
        locationName:
          type: string
        direction:
          type: string

    UpdateBiometricDevicePayload:
      type: object
      properties:
        name:
          type: string
        modelName:
          type: string
        ipAddress:
          type: string
        port:
          type: integer
        locationName:
          type: string
        direction:
          type: string
        status:
          type: string

    HardwarePunchItem:
      type: object
      required: [deviceSerialNumber, deviceUserId]
      properties:
        deviceSerialNumber:
          type: string
        deviceUserId:
          type: string
        punchedAt:
          type: string
        punchType:
          type: string
        verificationType:
          type: string
        temperature:
          type: number
        maskWorn:
          type: boolean
        rawLogId:
          type: string

    HardwarePunchBatchPayload:
      type: object
      required: [deviceSerialNumber, punches]
      properties:
        deviceSerialNumber:
          type: string
        punches:
          type: array
          items:
            $ref: '#/components/schemas/HardwarePunchItem'

    HardwarePunchResult:
      type: object
      required: [totalProcessed, totalAccepted, totalDuplicates, totalRejected, message]
      properties:
        totalProcessed:
          type: integer
        totalAccepted:
          type: integer
        totalDuplicates:
          type: integer
        totalRejected:
          type: integer
        message:
          type: string

    BiometricLivePunchItem:
      type: object
      required: [id, employeeId, employeeCode, employeeName, department, punchedAt, punchType, verificationType, deviceSerialNumber, deviceName, locationName]
      properties:
        id:
          type: string
        employeeId:
          type: string
        employeeCode:
          type: string
        employeeName:
          type: string
        department:
          type: string
        punchedAt:
          type: string
        punchType:
          type: string
        verificationType:
          type: string
        deviceSerialNumber:
          type: string
        deviceName:
          type: string
        locationName:
          type: string

    BiometricLiveStreamResponse:
      type: object
      required: [punches]
      properties:
        punches:
          type: array
          items:
            $ref: '#/components/schemas/BiometricLivePunchItem'















